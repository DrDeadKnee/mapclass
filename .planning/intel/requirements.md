# Requirements

No PRDs in the current ingest set.

The mapclass project does not yet have requirement-style PRDs. Functional intent is expressed through the four-phase project plan in `README.md` (classified as SPEC). Downstream tools (`gsd-roadmapper`) will derive requirements from the SPEC constraints in `constraints.md` and the SYNTHESIS notes; this file remains a placeholder until PRDs are introduced.
