---
phase: 01-pinned-environment-gcs-mirrors-and-a-verified-single-slice-a
plan: 03
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified:
  - src/mapclass/attribution.py
  - src/mapclass/overlay.py
  - tests/test_attribution_shape.py
  - tests/test_overlay_grid.py
  - notebooks/01_single_slice.ipynb
autonomous: false
requirements: [ATTR-01, ATTR-02, ATTR-03, VIZ-01]
user_setup: []

must_haves:
  truths:
    - "D-06: For the locked slice (manifest[-1]) + the locked control query 'a river', the engine runs SigLIP-2 forward + dynamic LRP against logits_per_image[0,0] with detached text and returns relevance shaped exactly like the input pixel tensor"
    - "D-07: Per-patch relevance reconstructs to a 27x27 grid with the right/bottom 6-px edge discarded, and the bare 27x27 cell placement + excluded 6-px band are demonstrated explicitly (no guaranteed landmark; alignment best-effort)"
    - "D-09 (signed-relevance, user-flagged 2026-05-18): LRP relevance is SIGNED. `to_patch_grid` reduces over channels with a signed sum (NOT `.abs()`) and does NOT min-max to [0,1]; the composite renders on a ZERO-CENTERED diverging colormap (symmetric vmin/vmax via TwoSlopeNorm so white==0) so negative relevance (evidence AGAINST the query) is visible. A separate non-negative |relevance| magnitude view MAY additionally be rendered for the 'where it concentrates' read and as the alpha channel. Established and verified in notebooks/00_vit_repro.ipynb cells 8-9."
    - "A heatmap overlay aligned to the source map (un-squashed to original PIL aspect) renders inline in 01_single_slice.ipynb"
    - "D-03: All three sanity controls (query-swap, model-randomization, occlusion) are implemented and rendered side-by-side for pure visual eyeball judgment (no printed scalars / asserts / quantitative thresholds as the gate)"
    - "D-06: The locked query-swap query is exactly 'a xylophone' (clearly unrelated → strong query-swap contrast against the locked control query 'a river')"
    - "D-02: Phase 1 is DONE only when BOTH all three controls visually PASS (D-03) AND the full 1,544 mirror + outcome manifest from Plan 02 is confirmed complete at the blocking completion checkpoint"
    - "Peak VRAM for the single so400m + LRP attribution is measured and recorded in the notebook"
  artifacts:
    - path: "src/mapclass/attribution.py"
      provides: "SigLIP-2 forward + LRPEngine in ONE function scope → input-shaped relevance"
      contains: "logits_per_image"
    - path: "src/mapclass/overlay.py"
      provides: "729 → 27x27 → un-squash → alpha-blended composite on the source map"
      contains: "reshape(27"
    - path: "notebooks/01_single_slice.ipynb"
      provides: "Phase 1 finish line: overlay + three controls side-by-side for eyeball gate"
  key_links:
    - from: "src/mapclass/attribution.py"
      to: "third_party/dynamicLRP/src/lrp_engine"
      via: "from lrp_engine import LRPEngine; engine.run(target)"
      pattern: "LRPEngine"
    - from: "src/mapclass/attribution.py"
      to: "src/mapclass/data_loader.py"
      via: "same requires_grad img_tensor into forward AND params_to_interpret"
      pattern: "params_to_interpret"
    - from: "notebooks/01_single_slice.ipynb"
      to: "src/mapclass/overlay.py"
      via: "to_patch_grid + composite, displayed inline"
      pattern: "overlay"
    - from: "notebooks/01_single_slice.ipynb"
      to: "src/mapclass/attribution.py"
      via: "from mapclass.attribution import attribute; used in control run + three sanity control cells"
      pattern: "attribute"
---

<objective>
Build and PROVE CORRECT the single-slice attribution primitive: SigLIP-2 forward
+ dynamic LRP against the `logits_per_image[0,0]` image–text similarity scalar
(detached text), reduced to a 27×27 per-patch relevance grid, un-squashed and
alpha-blended onto the source map, displayed inline in JupyterLab — and gated by
the three mandatory sanity controls (query-swap, model-randomization, occlusion)
judged by visual eyeball.

Purpose: Satisfies ROADMAP Success Criteria 4 and 5 — the project's central
technical risk. The dynamicLRP paper validates graph coverage, NOT faithfulness
for SigLIP-2 as a contrastive MAP-pool encoder; the three controls ARE the
verification (D-03/D-04 accepted risk — visual, no quantitative thresholds).
Output: src/mapclass/{attribution,overlay}.py, shape/grid units,
notebooks/01_single_slice.ipynb (the Phase 1 finish line).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-pinned-environment-gcs-mirrors-and-a-verified-single-slice-a/01-CONTEXT.md
@.planning/phases/01-pinned-environment-gcs-mirrors-and-a-verified-single-slice-a/01-RESEARCH.md
@CLAUDE.md
@src/mapclass/config.py
@src/mapclass/manifest.py
@src/mapclass/data_loader.py
@src/mapclass/model_loader.py

<interfaces>
<!-- Verified LRPEngine API + SigLIP-2 adaptation (RESEARCH.md Pattern 1/2, Code Examples) -->

LRPEngine.__init__(params_to_interpret=None, no_recompile=False, use_gamma=False,
  relevance_filter=1.0, dtype=torch.float32, use_attn_lrp=False, ...)
LRPEngine.run(target) -> (checkpoint_vals, param_node_vals)   # 2-tuple of lists
@staticmethod LRPEngine.get_model_operations(model_output)    # coverage probe

# Attribution — forward + LRP in ONE function scope (Pattern 2 tensor-identity invariant):
output = model(pixel_values=img_tensor, input_ids=..., attention_mask=...)
target = output.logits_per_image[0, 0]          # detached-text similarity scalar (Pitfall B)
engine = LRPEngine(use_gamma=False, no_recompile=True, relevance_filter=0.5)  # CLAUDE.md knobs
engine.params_to_interpret = [img_tensor]       # SAME object as forward input
ckpt_vals, param_vals = engine.run(target)
relevance = param_vals[0]                        # shape == img_tensor (1,3,384,384) [A3]
# Open Q1/A5: if run() rejects a 0-dim scalar, fall back to logits_per_image[:1,:1] or .reshape(1)
# torch.cuda.empty_cache() after (Pitfall E); measure peak VRAM (STATE.md blocker)

# Overlay — 27x27 grid, 6-px discard, un-squash (Pitfall D, D-07):
# floor(384/14)=27 → 27x27=729 patches; padding="valid" discards last 6 px R/B
r_signed = relevance.detach().sum(1)[0]          # (384,384) SIGNED (D-09, no abs)
r_mag    = relevance.detach().abs().sum(1)[0]    # (384,384) magnitude (>=0)
vs = r_signed[:378, :378]; vm = r_mag[:378, :378]   # discard last 6 px R/B
grid     = vs.reshape(27,14,27,14).mean((1,3))   # (27,27) SIGNED per-patch
grid_mag = vm.reshape(27,14,27,14).mean((1,3))   # (27,27) |relevance| per-patch
# D-09: NO min-max to [0,1]. Render signed grid on a zero-centered diverging
# cmap: M=max(abs(grid)); TwoSlopeNorm(vmin=-M,vcenter=0,vmax=M) → white==0.
# grid_mag (>=0) is the optional magnitude/alpha channel.

# data_loader.load_slice(entry, query, device) -> (img_tensor, input_ids, attention_mask, pil)
# manifest.entry_by_index(m, -1) -> the locked slice (D-05), id RUMSEY~8~1~344476~90112460

# Locked queries (D-06): control "a river"; query-swap "a xylophone"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Attribution engine (forward + LRP in one scope) and the 27x27 overlay</name>
  <files>src/mapclass/attribution.py, src/mapclass/overlay.py, tests/test_attribution_shape.py, tests/test_overlay_grid.py</files>
  <read_first>
    - .planning/phases/01-pinned-environment-gcs-mirrors-and-a-verified-single-slice-a/01-RESEARCH.md (Pattern 1/2; Code Examples §Attribution / §Overlay; Pitfall B/C/D/E; Empirical Risks 1-3; Assumptions A3/A5; Fallback Ladder)
    - .planning/phases/01-pinned-environment-gcs-mirrors-and-a-verified-single-slice-a/01-CONTEXT.md (D-06 locked queries, D-07 grid-placement demonstration requirement)
    - third_party/dynamicLRP/src/lrp_engine/lrp.py (confirm LRPEngine.__init__/run/get_model_operations signatures match the <interfaces> block)
    - src/mapclass/data_loader.py (the requires_grad tensor contract — must flow unchanged into forward + params_to_interpret)
    - src/mapclass/model_loader.py (singleton model+processor source)
  </read_first>
  <action>
    Create `src/mapclass/attribution.py` with `attribute(model, img_tensor,
    input_ids, attention_mask)` doing forward AND LRP in ONE function scope
    (Pattern 2 — never split across modules, never clone/detach the tensor):
    `output = model(pixel_values=img_tensor, input_ids=input_ids,
    attention_mask=attention_mask)`; `target = output.logits_per_image[0, 0]`
    (detached-text similarity scalar — NOT get_image_features, NOT image-embedding
    norm; Pitfall B); `engine = LRPEngine(use_gamma=False, no_recompile=True,
    relevance_filter=0.5)`; `engine.params_to_interpret = [img_tensor]` (the SAME
    object passed to the forward); `ckpt_vals, param_vals = engine.run(target)`;
    `relevance = param_vals[0]`. Handle Empirical Risk 1 / Assumption A5: if
    `run()` rejects a 0-dim scalar, retry with `output.logits_per_image[:1,:1]`
    or `target.reshape(1)` and record which form worked. Call
    `torch.cuda.empty_cache()` after (Pitfall E) and capture
    `torch.cuda.max_memory_allocated()` so the notebook can report peak VRAM
    (STATE.md blocker). Expose a `coverage_probe(model, ...)` helper wrapping
    `LRPEngine.get_model_operations(output)`. Document the Fallback Ladder
    (custom Promise → use_attn_lrp / pre-pool logits → vendored LXT → captum IG)
    as a docstring decision path — implement only the as-is path now; fallbacks
    are a documented deviation route, not pre-built.

    Create `src/mapclass/overlay.py` (D-09 — SIGNED, zero-centered; NOT abs/[0,1]):
    `to_patch_grid(relevance)` returns BOTH a signed grid and a magnitude grid →
    `r_signed = relevance.detach().sum(1)[0]` and `r_mag = relevance.detach().abs().sum(1)[0]`
    (384,384) → `[:378,:378]` (discard last 6 px right/bottom, `padding="valid"`;
    Pitfall D) → each `.reshape(27,14,27,14).mean((1,3))` → (27,27) signed +
    (27,27) magnitude numpy arrays. **No min-max [0,1] normalization** — the
    signed grid keeps physical units; scaling is a render concern.
    Add `composite(pil, grid_signed, grid_mag=None)` that nearest-neighbour
    upsamples the 27×27 grid, un-squashes the 384×384 square back to the original
    PIL aspect ratio (invert the processor square-squash), and alpha-blends the
    signed grid on a **zero-centered diverging cmap** — `M=max(abs(grid_signed))`,
    `matplotlib.colors.TwoSlopeNorm(vmin=-M, vcenter=0, vmax=M)`, `cmap="bwr"` so
    white==0 and negative relevance is visible; if `grid_mag` is given use it
    (normalized) as the alpha channel. Primary view `interpolation='nearest'`
    (honest patch resolution); a secondary smoothed view explicitly labeled
    "interpolated".
    Add `draw_patch_grid(pil)` that overlays the bare 27×27 cell boundaries on
    the slice map and visibly marks the excluded right/bottom 6-px band (D-07 —
    demonstrate cell placement + edge exclusion explicitly).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_attribution_shape.py tests/test_overlay_grid.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `attribution.py` target is literally `output.logits_per_image[0, 0]` (grep finds it); NO `get_image_features` and NO image-embedding-norm target anywhere (grep finds none)
    - `attribution.py` sets `engine.params_to_interpret = [img_tensor]` where `img_tensor` is the same parameter passed to `model(pixel_values=img_tensor, ...)` — no `.clone()`/`.detach()`/re-`.to()` between (grep + read confirms identity)
    - `tests/test_attribution_shape.py`: a synthetic forward-graph fixture asserts `param_vals[0].shape == img_tensor.shape` (input-shaped relevance, A3) and that the 0-dim-vs-≥1-d target fallback path exists
    - `tests/test_overlay_grid.py`: `to_patch_grid` on a `(1,3,384,384)` tensor returns the signed grid + magnitude grid each shaped exactly `(27,27)`; asserts the valid slice used is `[:378,:378]` (6-px discard applied); asserts NO `.abs()` on the signed path and NO min-max-to-`[0,1]` (D-09 — a fixture with known-negative relevance must yield a signed grid that contains negative values; the magnitude grid is `>= 0`)
    - `overlay.py` provides `draw_patch_grid` that marks the excluded right/bottom 6-px band (D-07 explicit demonstration helper present)
    - `attribution.py` calls `torch.cuda.empty_cache()` and exposes peak-VRAM capture (grep finds `max_memory_allocated`)
    - The Fallback Ladder is documented in `attribution.py` as a docstring (custom Promise → use_attn_lrp/pre-pool → LXT → captum IG)
  </acceptance_criteria>
  <done>attribution runs forward+LRP in one scope against logits_per_image[0,0] returning input-shaped relevance; overlay reduces to a 6-px-discarded 27x27 grid + un-squashed composite + explicit grid-placement demo; units pass</done>
</task>

<task type="auto">
  <name>Task 2: 01_single_slice.ipynb — overlay + three sanity controls rendered side-by-side</name>
  <files>notebooks/01_single_slice.ipynb</files>
  <read_first>
    - .planning/phases/01-pinned-environment-gcs-mirrors-and-a-verified-single-slice-a/01-RESEARCH.md (§Sanity Controls table — exact procedures + visual PASS/FAIL; D-03/D-04 visual-only no-thresholds; Pitfall C/E; System Architecture Diagram single-slice loop)
    - .planning/phases/01-pinned-environment-gcs-mirrors-and-a-verified-single-slice-a/01-CONTEXT.md (D-02 phase-done definition, D-03/D-04 visual eyeball + accepted risk + no-quantitative-thresholds prohibition, D-05 slice, D-06 queries, D-07 grid demo)
    - src/mapclass/attribution.py (the attribute / coverage_probe API created in Task 1)
    - src/mapclass/overlay.py (to_patch_grid / composite / draw_patch_grid)
    - src/mapclass/manifest.py (entry_by_index(-1) — the locked slice)
    - src/mapclass/data_loader.py (load_slice signature)
  </read_first>
  <action>
    Author `notebooks/01_single_slice.ipynb` — the Phase 1 finish line. Cells in
    order: (1) sys.path wiring (third_party/dynamicLRP/src + src), imports, seed
    set + record (torch/numpy/cuda), assert `torch.cuda.is_available()`. (2)
    `m = load_manifest(); entry = entry_by_index(m, -1)` (the locked slice
    RUMSEY~8~1~344476~90112460, D-05) — show the resolved id. (3) Load the
    singleton model+processor; run `coverage_probe` once and print the op count
    (coverage artifact). (4) Control run: `load_slice(entry, "a river", device)`
    → `attribute(...)` → `to_patch_grid` → `composite` displayed inline aligned
    to the source map; also render `draw_patch_grid` demonstrating the 27×27 cell
    placement and the excluded right/bottom 6-px band (D-07). Print and record
    peak VRAM for this single attribution (STATE.md blocker — gates Phase 2 sizing).
    (5) CONTROL A query-swap: re-run attribution with `"a xylophone"` (D-06);
    render the `"a river"` and `"a xylophone"` overlays DIRECTLY ADJACENT for
    eyeball contrast. (6) CONTROL B model-randomization: re-init the SigLIP-2
    VISION tower weights randomly (`model.vision_model.apply(reinit_fn)` — keep
    the text tower; deep-copy or reload the model so the trained run is not
    clobbered), re-run attribution on the slice with `"a river"`, render the
    randomized overlay NEXT TO the trained overlay. (7) CONTROL C occlusion: on
    the slice + `"a river"`, occlude (mean-fill) the top-k highest-relevance 14×14
    patches and recompute `logits_per_image[0,0]`; separately occlude k RANDOM
    patches and recompute; display original / top-k-occluded / random-occluded
    maps side by side with their similarity values RENDERED INTO the figure as
    captions (a rendered visual annotation, NOT a printed scalar / assert —
    consistent with D-03). (8) A markdown summary cell laying out all three
    controls for the human pass/fail eyeball. Do NOT add any quantitative
    pass/fail threshold or `assert` on the three controls (D-04 explicit
    prohibition) — the judgment is purely visual. The notebook MUST run
    top-to-bottom without cell errors.
  </action>
  <verify>
    <automated>.venv/bin/jupyter nbconvert --to notebook --execute --ExecutePreprocessor.timeout=3600 --output /tmp/01_single_slice_executed.ipynb notebooks/01_single_slice.ipynb && .venv/bin/python -c "import json,sys; nb=json.load(open('/tmp/01_single_slice_executed.ipynb')); imgs=sum(1 for c in nb['cells'] if c.get('cell_type')=='code' for o in c.get('outputs',[]) if o.get('output_type')=='display_data' and 'image/png' in o.get('data',{})); assert imgs>=4, f'expected >=4 rendered figures (overlay+3 controls), got {imgs}'; src=' '.join(''.join(c.get('source',[])) for c in nb['cells']); assert 'get_image_features' not in src, 'forbidden target'; print('figures',imgs)"</automated>
  </verify>
  <acceptance_criteria>
    - `notebooks/01_single_slice.ipynb` executes top-to-bottom via nbconvert with zero cell exceptions (exit code 0)
    - The executed notebook contains at least 4 rendered `image/png` figures: the control overlay, the query-swap pair, the model-randomization pair, the occlusion triptych
    - The slice is resolved via `entry_by_index(m, -1)` and the displayed id is `RUMSEY~8~1~344476~90112460` (D-05 — no per-id user input, no richness sort)
    - Control query is exactly `"a river"` and query-swap query is exactly `"a xylophone"` (D-06 — grep the notebook source)
    - Model-randomization re-inits `model.vision_model` (vision tower) only, with the trained model preserved for the side-by-side (both overlays rendered in one figure)
    - Occlusion control renders original / top-k-occluded / random-occluded side by side with similarity values drawn as figure captions (no bare printed scalar/assert as the gate)
    - NO `assert` and NO numeric pass/fail threshold is applied to any of the three controls; `get_image_features` does not appear in the notebook (D-04 prohibition + correct target)
    - The notebook prints and records the peak VRAM for the single so400m + LRP attribution (STATE.md blocker resolved)
    - `draw_patch_grid` is rendered showing 27×27 cell placement + the excluded 6-px right/bottom band (D-07)
  </acceptance_criteria>
  <done>01_single_slice.ipynb runs end-to-end and renders the aligned overlay + all three controls side-by-side for visual judgment, with peak VRAM recorded and no quantitative thresholds added</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify the three sanity controls visually PASS (Phase 1 correctness gate)</name>
  <action>Present the executed `notebooks/01_single_slice.ipynb` (overlay + query-swap, model-randomization, occlusion controls side-by-side) to the user for the mandatory visual-eyeball correctness judgment (D-03/D-04). This is a human-only gate — no quantitative threshold or assert is applied. Pause and wait for the resume signal; on FAIL, the documented Fallback Ladder in attribution.py is the response path.</action>
  <what-built>
    The verified single-slice attribution primitive: SigLIP-2 forward + dynamic
    LRP against `logits_per_image[0,0]` on the locked slice (manifest[-1]) +
    `"a river"`, reconstructed to a 27×27 overlay aligned to the source map, plus
    the three mandatory sanity controls (query-swap vs `"a xylophone"`,
    vision-tower randomization, top-k vs random occlusion) rendered side-by-side
    in `01_single_slice.ipynb`. This is the Phase 1 correctness gate (D-03/D-04 —
    judged by your eyes, not by thresholds).
  </what-built>
  <how-to-verify>
    1. Start JupyterLab on the VM (`.venv/bin/jupyter lab`), open
       `notebooks/01_single_slice.ipynb`, Restart Kernel & Run All on the
       `mapclass (.venv)` kernel. Confirm zero cell errors.
    2. Overlay/alignment: the `"a river"` heatmap overlays the source map aligned
       to its content; the `draw_patch_grid` cell shows the 27×27 cells and the
       excluded right/bottom 6-px band (alignment is best-effort per D-07).
    3. CONTROL A (query-swap): the `"a river"` and `"a xylophone"` heatmaps are
       directly adjacent — they must look SUBSTANTIALLY DIFFERENT (different
       regions). If near-identical → FAIL (image-saliency, not query-driven).
    4. CONTROL B (model-randomization): the randomized-vision overlay sits next
       to the trained one — it must COLLAPSE TO STRUCTURELESS NOISE vs. the
       structured trained map. If still structured → FAIL.
    5. CONTROL C (occlusion): original / top-k-occluded / random-occluded shown
       side by side with similarity captions — occluding TOP-relevance patches
       must visibly drop similarity MORE than occluding random patches. If
       top-k ≈ random → FAIL (MAP-pool relevance not faithful).
    6. Confirm the recorded peak VRAM is printed (gates Phase 2 sizing).
    Also confirm (Plan 02 gate) the full 1,544 mirror + outcome manifest is
    complete — Phase 1 is DONE only when BOTH the mirror is complete (D-02) AND
    all three controls PASS by eye (D-03).
    Expected: all three controls visually PASS and the overlay is aligned.
  </how-to-verify>
  <resume-signal>Type "approved" only if all three controls visually PASS and the overlay is aligned. Otherwise describe which control failed and how (e.g. "query-swap heatmaps look identical", "randomized still structured", "top-k drop ≈ random") so the Fallback Ladder can be applied</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vendored dynamicLRP → in-process autograd traversal | Engine executes against the live model graph; integrity rests on the pinned SHA (Plan 01) |
| Locally-cached Rumsey image bytes → model forward | Bytes were validated at ingest (Plan 02); re-decoded here from the trusted local cache |
| Notebook → GPU compute | Arbitrary Python on a single-user private VM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC3 | Tampering | Vendored dynamicLRP engine executed in attribution | mitigate | Engine is the SHA-pinned vendored copy from Plan 01 (T-01-SC); any Fallback-Ladder custom Promise patch is a documented, recorded deviation from the vendored SHA |
| T-01-09 | Tampering | Locally-cached Rumsey image decoded for the forward | accept | Bytes were content-type/size/decodability-validated before GCS write in Plan 02 (T-01-04/05); local cache is trusted derived state, no re-validation control warranted |
| T-01-10 | DoS | so400m + retained LRP graph VRAM blowup | mitigate | Batch size 1 always; `torch.cuda.empty_cache()` after the attribution; peak VRAM measured and recorded before any Phase 2 sweep sizing (Pitfall E, STATE.md blocker) |
| T-01-11 | Repudiation | Silent-wrong attribution (faithfulness unvalidated) | mitigate | The three mandatory sanity controls are the correctness gate (visual per D-03/D-04); the blocking human checkpoint refuses approval unless all three visually PASS; Fallback Ladder is the documented response path |
| T-01-12 | Elevation | Notebook arbitrary code execution | accept | Single-researcher private GCP VM, no served surface, inputs are trusted local/derived state |
</threat_model>

<verification>
- `attribute()` runs forward + LRP in one scope against `logits_per_image[0,0]` (detached text) and returns relevance shaped like the input tensor.
- `to_patch_grid` returns a fixed-normalized `(27,27)` grid with the 6-px right/bottom discard applied; `draw_patch_grid` demonstrates cell placement + edge exclusion (D-07).
- `01_single_slice.ipynb` runs end-to-end and renders the aligned overlay + all three controls side-by-side; peak VRAM recorded; no quantitative thresholds added (D-04).
- Human visually judges all three controls PASS (D-03) and confirms the full 1,544 mirror is complete (D-02) — Phase 1 DONE only on both.
</verification>

<success_criteria>
ROADMAP Success Criteria 4 and 5 met: for the locked (slice, query) the engine
runs SigLIP-2 forward + dynamic LRP against `logits_per_image[0,0]` and returns
a per-patch relevance grid reconstructed as a correctly-aligned 2D heatmap
(27×27, 6-px discard, aspect un-squash), rendered inline in JupyterLab, that has
visually PASSED query-swap, model-randomization, and occlusion — and the full
1,544 mirror is complete (D-02). Phase 1 finish line reached.
</success_criteria>

<output>
Create `.planning/phases/01-pinned-environment-gcs-mirrors-and-a-verified-single-slice-a/01-03-SUMMARY.md` when done
</output>
</content>
