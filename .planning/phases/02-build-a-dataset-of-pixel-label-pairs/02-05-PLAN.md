---
phase: 02-build-a-dataset-of-pixel-label-pairs
plan: 05
type: execute
wave: 3
depends_on: ["02-01", "02-02", "02-03", "02-04"]
files_modified:
  - scripts/gcs_io.py
  - scripts/build_dataset.py
  - scripts/build_historical_dataset.py
  - scripts/build_satellite_dataset.py
  - scripts/finetune_seg.py
  - scripts/evaluate_seg.py
  - tests/test_gcs_io.py
  - tests/test_seg_training.py
  - tests/test_seg_eval.py
autonomous: false
requirements: [PHASE-02, EVAL-01]
nyquist_compliant: true
user_setup:
  - service: gcs
    why: "User must regenerate ~100 Azgaar maps and upload to the canonical bucket (lost inputs are unrecoverable)"
    env_vars: []
    dashboard_config:
      - task: "Re-create ~100 Azgaar maps, name <template>_<NN>.geojson, author raw/manifest.json, upload to gs://mapclass-training-northeast1/data/synthetic/raw/"
        location: "Azgaar Fantasy Map Generator + gsutil/console"

must_haves:
  truths:
    - "finetune_seg.py pulls the train subset from GCS, verifies it, then trains from local scratch"
    - "evaluate_seg.py pulls the synthetic test subset from GCS, verifies it, then evaluates from local scratch"
    - "pull-once is idempotent and composes with the existing gcs_checkpoint resume"
    - "a _BUILD_COMPLETE sentinel is written as the last write of every map-dir and checked before skip (Open Question 1)"
    - "the merged-vs-family-rooted GCS layout is decided, documented, and encoded (Open Question 2)"
    - "the user-facing Azgaar regeneration + manifest + upload gate is a blocking human checkpoint"
  artifacts:
    - path: "scripts/finetune_seg.py"
      provides: "pull-once + verify before carve_train_val (RW-03)"
      contains: "pull_dataset_from_gcs"
    - path: "scripts/evaluate_seg.py"
      provides: "pull-once + verify before load_test_pyramid_dirs (RW-03)"
      contains: "pull_dataset_from_gcs"
    - path: "scripts/gcs_io.py"
      provides: "_BUILD_COMPLETE sentinel helpers + finalized layout constants"
      contains: "_BUILD_COMPLETE"
  key_links:
    - from: "scripts/finetune_seg.py"
      to: "scripts/gcs_io.py"
      via: "pull_dataset_from_gcs + verify_pull at job start"
      pattern: "from gcs_io import pull_dataset_from_gcs"
    - from: "scripts/evaluate_seg.py"
      to: "scripts/gcs_io.py"
      via: "pull_dataset_from_gcs + verify_pull at job start"
      pattern: "from gcs_io import pull_dataset_from_gcs"
    - from: "scripts/build_dataset.py"
      to: "_BUILD_COMPLETE sentinel"
      via: "last-write sentinel + skip-only-if-present guard"
      pattern: "_BUILD_COMPLETE"
---

<objective>
Close the consumer side of the rework: `finetune_seg.py` and
`evaluate_seg.py` bulk-fetch their split subset from GCS to local scratch,
verify against `split.json`, then train/eval from disk (RW-03), composing
with the existing `gcs_checkpoint.py` preemption resume. Resolve the two
non-blocking Open Questions — add a `_BUILD_COMPLETE` per-map-dir sentinel
(OQ1) and decide + encode the merged-vs-family-rooted GCS layout (OQ2).
Represent the long-pole human dependency (re-create ~100 Azgaar maps, author
`raw/manifest.json`, upload to GCS) as a blocking human gate.

Purpose: Without the consumer pull-once the GCS-canonical dataset is unusable
by Phase 4; without the human gate the build hard-fails on an empty/mis-named
raw set. This plan makes the rework end-to-end.
Output: pull-once-enabled finetune/evaluate, sentinel guard, finalized
layout, and the 02-HUMAN-UAT gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-build-a-dataset-of-pixel-label-pairs/02-CONTEXT.md
@.planning/phases/02-build-a-dataset-of-pixel-label-pairs/02-RESEARCH.md
@.planning/phases/02-build-a-dataset-of-pixel-label-pairs/02-PATTERNS.md
@.planning/phases/02-build-a-dataset-of-pixel-label-pairs/02-01-SUMMARY.md
@.planning/phases/02-build-a-dataset-of-pixel-label-pairs/02-02-SUMMARY.md
@.planning/phases/02-build-a-dataset-of-pixel-label-pairs/02-03-SUMMARY.md
@.planning/phases/02-build-a-dataset-of-pixel-label-pairs/02-04-SUMMARY.md

<interfaces>
<!-- Contracts from Wave 0-2. Consume; do not reinvent. -->

From scripts/gcs_io.py (02-01):
- pull_dataset_from_gcs(subset, local_scratch, gcs_prefix=...) -> Path  (idempotent)
- verify_pull(local_root, split_json, subset) -> None  (raises RuntimeError)
- GCS_PROJECT="narrative-campaign", DATA_PREFIX="mapclass-training-northeast1/data"

finetune_seg.py touch points (verified via PATTERNS.md):
- carve_train_val call ~L206 (unchanged signature; point at local scratch root)
- gcs_latest_checkpoint resume block ~L220-231 (EXISTING — pull-once inserts BEFORE it)

evaluate_seg.py touch points:
- load_test_pyramid_dirs(split_json: Path, data_root: Path) ~L103-138 (unchanged;
  reads local Paths AFTER pull-once stages them)

RESEARCH.md Open Question resolutions (planner decisions, now LOCKED):
- OQ1: ADD the _BUILD_COMPLETE sentinel (cheap preemption safeguard, Pitfall R-2).
- OQ2 DECISION: family-rooted GCS layout
  gs://.../data/{synthetic,historical,satellite}/{train,test}/ with a MERGED
  local scratch root after pull-once. Rationale: each build script already owns
  its family prefix (02-02/03/04); merging only at pull time keeps build scripts
  decoupled and lets evaluate_seg pull only the synthetic test subset
  (EVAL-01 hold-out is synthetic-only). split.json stays synthetic-only at
  gs://.../data/synthetic/split.json (EVAL-01 unchanged from D-15/D-16).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: _BUILD_COMPLETE sentinel (OQ1) + finalized family-rooted layout (OQ2)</name>
  <read_first>
    - scripts/gcs_io.py (post-02-01 — extend with sentinel helpers + layout constants)
    - scripts/build_dataset.py (post-02-02 — collision/SKIP guard + tiling call site)
    - scripts/build_historical_dataset.py (post-02-03 — _process_one tiling site)
    - scripts/build_satellite_dataset.py (post-02-04 — per-scene tiling site)
    - .planning/phases/02-build-a-dataset-of-pixel-label-pairs/02-RESEARCH.md (Pitfall R-2 lines ~742-754; Open Questions lines ~852-868)
    - tests/test_gcs_io.py (post-02-01 — extend)
  </read_first>
  <behavior>
    - test_build_complete_written_last: after a successful map-dir build the sentinel object exists in the mock store keyed under the map-dir prefix
    - test_skip_only_if_sentinel_present: a map-dir prefix that exists but lacks the sentinel is NOT skipped (rebuilt); one WITH it is skipped
    - test_layout_constants: gcs_io exposes family_subset_prefix resolving (family, subset) -> "mapclass-training-northeast1/data/<family>/<subset>"
  </behavior>
  <action>
    Extend `scripts/gcs_io.py`: add `_BUILD_COMPLETE = "_BUILD_COMPLETE"` and
    helpers `mark_build_complete(fs, map_dir_prefix)` (`fs.pipe_file(
    f"{map_dir_prefix}/{_BUILD_COMPLETE}", b"1")` — written LAST, after all
    pyramid objects) and `is_build_complete(fs, map_dir_prefix) -> bool`
    (`fs.exists(...)`). Add `family_subset_prefix(family, subset) -> str`
    returning `f"{DATA_PREFIX}/{family}/{subset}"` (OQ2 family-rooted layout).
    Record the OQ2 decision (family-rooted GCS, merged local scratch,
    synthetic-only split.json/EVAL-01) in the gcs_io.py module docstring.

    In build_dataset.py, build_historical_dataset.py,
    build_satellite_dataset.py: after `tiling.tile(...)` completes for a
    map-dir call `mark_build_complete(fs, <that map-dir GCS prefix>)`. Change
    the pre-tile skip guard so a map-dir is skipped ONLY if
    `is_build_complete(...)` is True; if the prefix exists but the sentinel is
    absent, treat as partial/aborted and rebuild (Pitfall R-2).
  </action>
  <verify>
    <automated>cd /home/drdreadknee/mapclass && pytest tests/test_gcs_io.py -k "build_complete or sentinel or layout" -x && grep -n "_BUILD_COMPLETE" scripts/build_dataset.py scripts/build_historical_dataset.py scripts/build_satellite_dataset.py</automated>
  </verify>
  <acceptance_criteria>
    - gcs_io.py defines _BUILD_COMPLETE, mark_build_complete, is_build_complete, family_subset_prefix
    - all three build scripts call mark_build_complete after tiling and gate skip on is_build_complete
    - gcs_io.py module docstring records the OQ2 family-rooted + merged-scratch decision
    - `pytest tests/test_gcs_io.py -k "build_complete or sentinel or layout" -x` exits 0
    - grep finds _BUILD_COMPLETE in all three build scripts
  </acceptance_criteria>
  <done>
    Sentinel written last + checked before skip across all three families;
    OQ2 layout finalized and documented; tests green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RW-03 pull-once + verify in finetune_seg.py and evaluate_seg.py</name>
  <read_first>
    - scripts/finetune_seg.py (full file — carve_train_val ~L206, gcs_latest_checkpoint resume ~L220-231, argparse block)
    - scripts/evaluate_seg.py (full file — load_test_pyramid_dirs ~L103-138, argparse block)
    - scripts/gcs_io.py (pull_dataset_from_gcs + verify_pull + family_subset_prefix contracts)
    - scripts/seg/gcs_checkpoint.py (gcs_latest_checkpoint composition — pull-once step 1, resume step 3)
    - .planning/phases/02-build-a-dataset-of-pixel-label-pairs/02-PATTERNS.md (finetune_seg.py section lines 290-331; evaluate_seg.py section lines 333-368)
    - .planning/phases/02-build-a-dataset-of-pixel-label-pairs/02-RESEARCH.md (RW-03 call-site design lines ~403-446)
    - tests/test_seg_training.py and tests/test_seg_eval.py (current state — extend; offline ImportError fallback path)
  </read_first>
  <behavior>
    - test_finetune_pull_once_before_resume: pull_dataset_from_gcs + verify_pull invoked before gcs_latest_checkpoint; carve_train_val receives the local scratch root
    - test_evaluate_pull_once_before_load: pull + verify + local split.json write precede load_test_pyramid_dirs
    - test_offline_importerror_fallback: when gcs_io import raises ImportError both scripts fall back to existing --train-root / --split-json local path (no crash)
  </behavior>
  <action>
    Modify `scripts/finetune_seg.py`: add `--scratch-dir` (type=Path, default
    `Path("/tmp/mapclass_data")`). Before `carve_train_val` (~L206), wrap in
    `try/except ImportError` (same guard idiom as the existing
    gcs_latest_checkpoint block): `from gcs_io import pull_dataset_from_gcs,
    verify_pull`; `local_root = pull_dataset_from_gcs("train",
    args.scratch_dir)`; read split.json once via `fs.cat(
    f"{DATA_PREFIX}/synthetic/split.json")`; `verify_pull(local_root,
    split_json, "train")`; then `carve_train_val(local_root, ...)` pointing at
    the pulled scratch root. The existing gcs_latest_checkpoint resume block
    stays at its current position (step 3 — unchanged). On ImportError fall
    back to existing `args.train_root`.

    Modify `scripts/evaluate_seg.py`: add `--scratch-dir` (same pattern).
    Before `load_test_pyramid_dirs`: `local_root =
    pull_dataset_from_gcs("test", args.scratch_dir)`; read split.json from GCS;
    `verify_pull(local_root, split_json_dict, "test")`; write split.json to
    `args.scratch_dir / "split.json"` so the unchanged
    `load_test_pyramid_dirs(local_split, local_root)` consumes it as a Path.
    Same `try/except ImportError` offline fallback. evaluate pulls ONLY the
    synthetic test subset (EVAL-01 hold-out is synthetic-only — OQ2).

    Do NOT change carve_train_val or load_test_pyramid_dirs internals — they
    operate on local Paths post-pull, unchanged.
  </action>
  <verify>
    <automated>cd /home/drdreadknee/mapclass && pytest tests/test_seg_training.py tests/test_seg_eval.py tests/test_gcs_io.py -x && python -c "import ast; [ast.parse(open(f).read()) for f in ('scripts/finetune_seg.py','scripts/evaluate_seg.py')]; print('syntax ok')"</automated>
  </verify>
  <acceptance_criteria>
    - finetune_seg.py and evaluate_seg.py both import pull_dataset_from_gcs, verify_pull from gcs_io
    - both add a --scratch-dir argparse arg
    - in finetune_seg.py the pull/verify call lines precede the gcs_latest_checkpoint resume line
    - in evaluate_seg.py the pull/verify lines precede the load_test_pyramid_dirs call
    - both wrap the gcs_io import in try/except ImportError with a documented local fallback
    - `pytest tests/test_seg_training.py tests/test_seg_eval.py tests/test_gcs_io.py -x` exits 0
    - `python -c "import ast; ..."` prints "syntax ok"
  </acceptance_criteria>
  <done>
    finetune/evaluate pull-once + verify before train/eval, composing with
    checkpoint resume; offline fallback intact; tests green.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Human gate — Azgaar regeneration, manifest authoring, GCS upload</name>
  <action>
    Write `.planning/phases/02-build-a-dataset-of-pixel-label-pairs/02-HUMAN-UAT.md`
    documenting this blocking gate: the user-only Azgaar regeneration steps, the
    RW-02 naming/manifest contract, the GCS upload target
    (gs://mapclass-training-northeast1/data/synthetic/raw/), the ADC
    pre-condition, and the post-upload `build_dataset.py build` + non-empty
    bucket acceptance run. Then pause for the user — re-creating ~100 Azgaar
    maps in an external GUI is human-only and unrecoverable; it blocks all
    downstream Phase 4 consumption.
  </action>
  <what-built>
    The full GCS-canonical Phase 2 pipeline (Waves 0-3) is implemented and
    offline-verified. The end-to-end build CANNOT run until the user
    re-creates the lost Azgaar source maps and uploads them to the canonical
    bucket. This is an unrecoverable human-only dependency (the original
    synthetic dataset + raw .geojson inputs were destroyed by ephemeral
    compute; full manual regeneration was accepted by the user in
    02-CONTEXT.md). The new EVAL-01 hold-out WILL differ from the original
    Phase 4 plans — this is an accepted, logged consequence.

    This task also writes `.planning/phases/02-build-a-dataset-of-pixel-label-pairs/02-HUMAN-UAT.md`
    capturing the gate steps and the post-upload build/verify acceptance run.
  </what-built>
  <how-to-verify>
    1. In the Azgaar Fantasy Map Generator, re-create ~100 maps across ~12
       continent templates.
    2. Export each as GeoJSON named `<template>_<NN>.geojson` (lowercase,
       zero-padded NN, e.g. `europe_01.geojson`) — must match
       `^[a-z][a-z0-9_]*_[0-9]{2}\.geojson$`.
    3. Author `raw/manifest.json` (schema: `{"version":"1","entries":{
       "<file>.geojson":{"template":"<template>"}}}`) listing EVERY uploaded
       file; template must equal the part before `_NN`.
    4. Upload all .geojson + manifest.json to
       `gs://mapclass-training-northeast1/data/synthetic/raw/`.
    5. Ensure ADC on the build host: `gcloud auth application-default login`
       (same requirement as the existing gcs_checkpoint.py path — no new auth).
    6. Run the synthetic build:
       `python scripts/build_dataset.py build` (defaults are gs:// — no
       `--local-ok`). Expected: either a CLEAN run that freezes
       `gs://.../data/synthetic/split.json` and writes pyramids +
       `_BUILD_COMPLETE` sentinels, OR a LOUD `FATAL: manifest validation
       failed` `sys.exit(1)` if any filename/manifest mismatch — fix and re-run.
    7. Confirm non-empty output:
       `python -c "import sys;sys.path.insert(0,'scripts');import gcsfs,gcs_io;fs=gcsfs.GCSFileSystem(project=gcs_io.GCS_PROJECT);print(len(fs.ls(gcs_io.DATA_PREFIX+'/synthetic/train')))"`
       returns > 0, and `fs.exists(gcs_io.DATA_PREFIX+'/synthetic/split.json')`
       is True.
  </how-to-verify>
  <resume-signal>
    Type "approved" once the build completed cleanly and
    `gs://.../data/synthetic/{train,test}/` + `split.json` are populated, or
    describe the manifest/upload error to fix.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-authored raw/.geojson + manifest.json → build_dataset.py | untrusted input driving the EVAL-01 split |
| GCS bucket → finetune/evaluate local scratch | pulled dataset trusted only after verify_pull |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-40 | Tampering | partial GCS upload silently consumed by training | mitigate | _BUILD_COMPLETE sentinel gates skip; verify_pull spot-checks pyramid completeness before training |
| T-02-41 | Tampering | pull-once false-pass on truncated data | mitigate | verify_pull membership+presence+10% file-count spot-check; DataLoader pyramid.json parse fails loud (Pitfall R-5) |
| T-02-42 | Spoofing | GCS auth on fresh GPU host | mitigate | ADC-only; human gate step 5 requires gcloud auth application-default login (same as gcs_checkpoint.py, no new secret) |
| T-02-43 | Tampering | mis-named hand-created file skews EVAL-01 | mitigate | RW-02 hard-fail in build (02-02) precedes split; human gate step 6 surfaces the FATAL exit |
| T-02-44 | Repudiation | differing Phase 4 EVAL-01 hold-out | accept | Accepted + logged consequence of unrecoverable source loss (02-CONTEXT.md); new split freezes on regenerated sources |
| T-02-SC | Tampering | pip installs | accept | No new packages — gcsfs/fsspec already audited [OK]; no install task |
</threat_model>

<verification>
- `pytest tests/test_seg_training.py tests/test_seg_eval.py tests/test_gcs_io.py -x` green
- `grep -n "pull_dataset_from_gcs" scripts/finetune_seg.py scripts/evaluate_seg.py` non-empty
- `grep -n "_BUILD_COMPLETE" scripts/build_dataset.py scripts/build_historical_dataset.py scripts/build_satellite_dataset.py` non-empty
- Human gate completed: `gs://.../data/synthetic/{train,test}/` + split.json populated
</verification>

<success_criteria>
- finetune_seg.py / evaluate_seg.py pull-once + verify before train/eval, composing with checkpoint resume
- _BUILD_COMPLETE sentinel + family-rooted layout (OQ1/OQ2) encoded and tested
- Offline ImportError fallback preserved
- Human Azgaar-regeneration + manifest + upload gate represented as blocking checkpoint; 02-HUMAN-UAT.md written
</success_criteria>

<output>
Create `.planning/phases/02-build-a-dataset-of-pixel-label-pairs/02-05-SUMMARY.md` when done
</output>
