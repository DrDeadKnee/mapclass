---
phase: 03-build-a-dense-semantic-segmentation-pipeline
plan: 02
type: execute
wave: 1
depends_on: [03-01]
files_modified:
  - scripts/seg/backbones.py
autonomous: true
requirements: [PHASE-03, EVAL-03]
nyquist_compliant: true

must_haves:
  truths:
    - "SigLIP backbone loads the Phase-1 PEFT LoRA adapter and yields (B,C,h,w) feature grids for 224/448/896 (PHASE-03)"
    - "No Gemma/language_model parameter or module is reachable from any backbone (PHASE-03 SC#2)"
    - "DINOv2 and Swin satisfy the same Backbone protocol with declared strides/channels (EVAL-03)"
    - "Variant-B widened 15-ch patch-embed exists for all 3 backbones, extras zero-init, RGB weights preserved, construction-only (D-03a/D-06a)"
  artifacts:
    - path: "scripts/seg/backbones.py"
      provides: "Backbone Protocol + SiglipBackbone/Dinov2Backbone/SwinBackbone + Variant-B widened patch-embed"
      contains: "feature_strides"
  key_links:
    - from: "scripts/seg/backbones.py"
      to: "scripts/finetune_paligemma.py"
      via: "mirror apply_lora load order: PaliGemma base + PeftModel.from_pretrained(adapter_dir)"
      pattern: "PeftModel\\.from_pretrained"
    - from: "scripts/seg/backbones.py"
      to: "transformers vision_tower"
      via: "defensive accessor (.vision_tower vs .model.vision_tower), vision tower only"
      pattern: "vision_tower"
---

<objective>
Wave 1: implement `scripts/seg/backbones.py` — the unified `Backbone` Protocol
(D-05) and its three implementations (SigLIP primary, DINOv2, Swin benchmarks),
plus the Variant-B widened 15-channel patch-embed structural modification for all
three backbones. This is the D-05 seam and the PHASE-03 SC#2 Gemma-exclusion
guarantee.

Purpose: The backbone is the only place ViT-token→grid reshape, PEFT-adapter
loading, Gemma-exclusion, and timm hierarchy differences live (RESEARCH
Architectural Responsibility Map). The shared decoder (03-04) and recursive
orchestrator (03-05) depend on its `feature_strides`/`feature_channels`
contract.
Output: `scripts/seg/backbones.py` satisfying `tests/test_seg_backbones.py`
(shape/stride contract, Gemma-absence for both variants, Variant-B widened
patch-embed across all 3 backbones, DINOv2/Swin protocol conformance).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-build-a-dense-semantic-segmentation-pipeline/03-CONTEXT.md
@.planning/phases/03-build-a-dense-semantic-segmentation-pipeline/03-RESEARCH.md
@.planning/phases/03-build-a-dense-semantic-segmentation-pipeline/03-PATTERNS.md
@.planning/phases/03-build-a-dense-semantic-segmentation-pipeline/03-VALIDATION.md

<interfaces>
<!-- Backbone Protocol (RESEARCH Pattern 5) -->
class Backbone(Protocol):
    feature_strides: list[int]      # ViT e.g. [16]*k ; Swin [4,8,16,32]
    feature_channels: list[int]
    def forward(self, x) -> list[Tensor]: ...   # one (B,C,h,w) per stride

<!-- SiglipBackbone (RESEARCH Code Examples + finetune_paligemma.py apply_lora) -->
load order: PaliGemmaForConditionalGeneration.from_pretrained(model_id)
            then PeftModel.from_pretrained(base, adapter_dir)  # binds Phase-1 LoRA
            then .eval().requires_grad_(False)                 # D-06 frozen
defensive accessor: inner=getattr(model,"base_model",model);
            pg=getattr(inner,"model",inner);
            vt=getattr(pg,"vision_tower",None) or pg.model.vision_tower
forward: vt(pixel_values, interpolate_pos_encoding=True, output_hidden_states=True)
         tap layers ~(8,17,26); tok (B,N,1152) NO CLS; h=w=int(round(N**0.5))
         feat = tok.transpose(1,2).reshape(B,-1,h,w)
SigLIP-So400m/14: hidden 1152, patch 14, 27 layers, no CLS (VERIFY config A1)

<!-- timm benchmarks (RESEARCH Code Examples) -->
DINOv2: timm.create_model("vit_*_patch14_*dinov2*", pretrained, num_classes=0)
        .get_intermediate_layers(x, n=k, reshape=True) -> list (B,C,h,w)
Swin:   timm.create_model("swin_*", features_only=True, out_indices=(0,1,2,3))
        feature_info.reduction() -> strides ; feature_info.channels() -> channels

<!-- finetune_paligemma.py:191-193 Gemma-freeze idiom (extend to ASSERTION) -->
for name,param in peft_model.named_parameters():
    if "language_model" in name: param.requires_grad_(False)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Backbone Protocol + SiglipBackbone (PEFT load, Gemma-exclusion, ViT reshape)</name>
  <files>scripts/seg/backbones.py</files>
  <read_first>
    - tests/test_seg_backbones.py (the contract this task must satisfy — written in 03-01)
    - scripts/finetune_paligemma.py lines 1-31 (docstring convention), 160-203 (apply_lora: load order, lines 178/191-193 Gemma-freeze idiom), 308-325 (--model-id/--adapter-dir argparse style)
    - 03-RESEARCH.md Code Examples (SiglipBackbone load + siglip_features), Pattern 1 (vision-tower-only), Pattern 2 (ViT reshape), Pitfall 1/2/3
    - 03-PATTERNS.md section scripts/seg/backbones.py (Gemma-isolation copy target, divergence note)
  </read_first>
  <behavior>
    - SiglipBackbone(model_id, adapter_dir) loads PaliGemma + PeftModel.from_pretrained(adapter_dir), reaches vision_tower via the defensive accessor, holds the vision tower ONLY.
    - forward(x) for x at 224/448/896 returns a list of (B,1152,h,w) grids (h=w=S/14), tap layers ~(8,17,26), interpolate_pos_encoding=True.
    - feature_strides / feature_channels attributes declared (channels [1152]*k).
    - No `language_model` param-name and no `gemma` module-type reachable from the backbone (PHASE-03 SC#2).
    - At plan time assert config.vision_config hidden_size/patch_size/num_hidden_layers (RESEARCH A1) before sizing channels; fall back to stub_vision_config when checkpoint absent.
  </behavior>
  <action>
    Define a `Backbone` `typing.Protocol` with `feature_strides: list[int]`,
    `feature_channels: list[int]`, `forward(x) -> list[Tensor]` (one (B,C,h,w)
    per declared stride) — RESEARCH Pattern 5. Implement `SiglipBackbone`:
    mirror `finetune_paligemma.py` apply_lora load order exactly
    (`PaliGemmaForConditionalGeneration.from_pretrained(model_id)` then
    `PeftModel.from_pretrained(base, adapter_dir)` then
    `.eval().requires_grad_(False)` — D-06 frozen-by-default), reach the SigLIP
    tower via the defensive accessor from RESEARCH Code Examples (4.x
    `.vision_tower` vs 5.x `.model.vision_tower`; assert the resolved module is a
    SigLIP type, Pitfall 3), and hold a reference to the vision tower ONLY —
    never the projector or `language_model` (Pitfall 1; PATTERNS divergence
    note: stop one level earlier than apply_lora's projector reach). forward
    calls `vision_tower(pixel_values, interpolate_pos_encoding=True,
    output_hidden_states=True)`, taps evenly-spaced blocks (~8/17/26 — OQ3
    discretion: 3-4 taps for boundary detail), reshapes each
    `(B,N,1152)` → `(B,1152,h,w)` with `h=w=int(round(N**0.5))` (no CLS strip —
    SigLIP has none). Declare `feature_strides`/`feature_channels`. Verify
    `config.vision_config` hidden_size/patch_size/num_hidden_layers at load
    (RESEARCH A1) and size channels from it; when the adapter/checkpoint is
    absent accept a `vision_config`-like stub so offline shape tests run.
    `adapter_dir`/`model_id` are constructor parameters (OQ1 RESOLVED; argparse
    style mirrors finetune_paligemma.py 308-325 if a CLI entry is added). Open
    the module with the project decision-referenced docstring convention.
    NO training loop / optimizer / `.backward()` / unfreeze hook (D-06).
  </action>
  <verify>
    <automated>python -m pytest tests/test_seg_backbones.py -k "siglip or no_gemma or protocol" -x -q -m "not integration"</automated>
  </verify>
  <acceptance_criteria>
    - `from seg.backbones import Backbone, SiglipBackbone` succeeds
    - SigLIP backbone yields (B,1152,h,w) grids at declared strides for 224/448/896 (test_seg_backbones siglip cases green)
    - `test_no_gemma` green: no `language_model` param-name, no `gemma` module-type reachable
    - `feature_strides`/`feature_channels` are declared list[int] attributes
  </acceptance_criteria>
  <done>Backbone protocol + Gemma-free SigLIP backbone implemented and shape-verified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DINOv2 + Swin timm backbones + Variant-B widened 15-ch patch-embed</name>
  <files>scripts/seg/backbones.py</files>
  <read_first>
    - tests/test_seg_backbones.py (DINOv2/Swin protocol cases + Variant-B widened-patch-embed cases)
    - 03-RESEARCH.md Code Examples (timm DINOv2/Swin), Pattern 5, Pitfall 4 (Swin-vs-ViT stride)
    - 03-CONTEXT.md D-03a (Variant B: all 3 backbones first conv/patch-embed widened to 15-ch, extras zero-init, pretrained RGB kept, widened patch-embed trainable) + D-06a (construction-only, NOT trained)
    - scripts/seg/backbones.py (the SiglipBackbone + Protocol from Task 1)
  </read_first>
  <behavior>
    - Dinov2Backbone via timm `get_intermediate_layers(x, n=k, reshape=True)` -> list (B,C,h,w); declared stride 14, channels from model.
    - SwinBackbone via timm features_only out_indices=(0,1,2,3); feature_strides=feature_info.reduction(), feature_channels=feature_info.channels() (4 native levels).
    - Variant-B builder widens the first conv / patch-embed of ALL THREE backbones from 3 to 15 input channels: 12 extra channels zero-initialised, original 3 RGB weights copied verbatim, widened patch-embed marked trainable (`requires_grad_(True)`) while the rest of the backbone stays per its variant's freeze policy. Construction-only — no training.
    - Both variants still satisfy the same Backbone protocol (declared strides/channels; forward returns the same shape contract).
  </behavior>
  <action>
    Add `Dinov2Backbone` and `SwinBackbone` to scripts/seg/backbones.py
    conforming to the `Backbone` protocol. DINOv2: `timm.create_model` a
    `vit_*_patch14_*dinov2*` tag (pick the final tag at execution via
    `timm.list_models("*dinov2*")` — D-05 discretion), `num_classes=0`,
    `.eval().requires_grad_(False)`; forward = `get_intermediate_layers(x, n=k,
    reshape=True)`; declare stride 14 and channels from the model. Swin:
    `timm.create_model("swin_*", features_only=True, out_indices=(0,1,2,3))`
    (final tag via `timm.list_models("swin*")`), declare
    `feature_strides=list(model.feature_info.reduction())` and
    `feature_channels=list(model.feature_info.channels())` — never hard-code
    level count (Pitfall 4: decoder reads these dynamically). Implement the
    Variant-B widened patch-embed for ALL THREE backbones (D-03a Variant B,
    D-06a construction-only): locate each backbone's first conv / patch-embed
    (SigLIP `vision_tower` patch embedding conv; DINOv2 timm `patch_embed.proj`;
    Swin timm `patch_embed.proj`), construct a new Conv with in_channels=15,
    copy the pretrained 3 RGB kernel weights into channels 0:3 verbatim, zero-init
    channels 3:15 (the 12 prior channels), and set the widened patch-embed
    `requires_grad_(True)` (the ONLY trainable param under Variant B; everything
    else stays frozen per its policy). Do NOT run any optimizer/backward — this
    is the structural modification only (D-06a). Provide a clear constructor
    flag / factory selecting Variant A (unmodified, frozen) vs Variant B
    (widened-trainable patch-embed) per backbone, since the same backbone class
    backs both. Keep the decision-referenced module docstring updated.
  </action>
  <verify>
    <automated>python -m pytest tests/test_seg_backbones.py -k "dino or swin or variant_b or protocol" -x -q -m "not integration"</automated>
  </verify>
  <acceptance_criteria>
    - `from seg.backbones import Dinov2Backbone, SwinBackbone` succeeds; both expose declared list `feature_strides`/`feature_channels`
    - DINOv2 returns reshaped (B,C,h,w) lists; Swin returns 4 levels at strides from `feature_info.reduction()`
    - Variant-B widened patch-embed for ALL 3 backbones: `in_channels == 15`, channels 3:15 are exactly zero at construction, channels 0:3 equal the original pretrained RGB weights, widened patch-embed `requires_grad is True`
    - No optimizer/`.backward()`/training loop introduced (D-06/D-06a construction-only)
    - `pytest tests/test_seg_backbones.py -x -q -m "not integration"` fully green
  </acceptance_criteria>
  <done>All 3 backbones conform to D-05; both D-03a variants structurally constructed (untrained).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HF/timm model hub → process | Pretrained DINOv2/Swin checkpoints + Phase-1 PEFT adapter deserialized into the process (untrusted-pickle surface) |
| Phase-1 adapter dir → SiglipBackbone | LoRA adapter directory loaded via peft (torch.load/pickle path) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Elevation of Privilege | timm/HF checkpoint deserialization | mitigate | Prefer safetensors-format weights where the timm/HF API supports it; document the trust assumption (checkpoints come from official `timm`/HF hub or the user's own Phase-1 run) in the module docstring; `adapter_dir`/`model_id` are explicit user-supplied params, never auto-fetched from untrusted URLs |
| T-03-05 | Tampering | Phase-1 PEFT adapter load | mitigate | Mirror `finetune_paligemma.py` exact load order; assert resolved module is a SigLIP type (Pitfall 3) so a swapped/mismatched adapter fails loudly instead of silently degrading |
| T-03-06 | Spoofing | Gemma leaking into seg forward graph | mitigate | Parameter-graph assertion: no `language_model` param-name / `gemma` module-type reachable (PHASE-03 SC#2); backbone holds vision tower only |
</threat_model>

<verification>
- `pytest tests/test_seg_backbones.py -x -q -m "not integration"` fully green
- `pytest -q -m "not integration"` full offline suite green (no regression)
- Manual grep: no `optim`, `.backward(`, or training loop in scripts/seg/backbones.py (D-06)
</verification>

<success_criteria>
- `Backbone` Protocol + SiglipBackbone + Dinov2Backbone + SwinBackbone implemented
- SigLIP loads Phase-1 PEFT adapter via mirrored apply_lora load order; Gemma provably absent (SC#2)
- DINOv2/Swin conform to the same protocol with declared strides/channels (EVAL-03 seam)
- Variant-B 15-ch widened patch-embed constructed for all 3 backbones, zero-init extras, RGB preserved, trainable, UNTRAINED (D-03a/D-06a)
</success_criteria>

<output>
After completion, create `.planning/phases/03-build-a-dense-semantic-segmentation-pipeline/03-02-SUMMARY.md`
</output>
