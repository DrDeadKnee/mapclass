---
phase: 02-build-a-dataset-of-pixel-label-pairs
plan: 03
type: execute
wave: 1
depends_on: [01]
files_modified:
  - scripts/label.py
  - scripts/build_dataset.py
  - scripts/synthetic_weights.py
  - tests/test_render.py
  - tests/test_split.py
  - tests/test_synthetic_weights.py
autonomous: false
requirements: [PHASE-02, EVAL-01]

must_haves:
  truths:
    - "Each (Azgaar source map × render style) is its own map dir <azgaar_id>__<style>/ with one image.png, sharing land_cover.png/topography.png across styles"
    - "Every synthetic per-map dir ships image.png + land_cover.png + topography.png + sample_weights.json"
    - "A seeded stratified-by-template split writes split.json once and is frozen on every subsequent build"
    - "data/synthetic/test/ and data/synthetic/train/ share zero map IDs"
    - "All styles of a held-out Azgaar source map go entirely to test (no cross-style leakage)"
  artifacts:
    - path: "scripts/build_dataset.py"
      provides: "Sub-command scaffold + per-(map×style) build + seeded stratified split + frozen split.json"
      contains: "split.json"
    - path: "scripts/synthetic_weights.py"
      provides: "SYNTHETIC_LC_WEIGHTS dict (locked-shape) + writer"
      contains: "SYNTHETIC_LC_WEIGHTS"
    - path: "scripts/label.py"
      provides: "writes image.png + sample_weights.json alongside land_cover/topography"
      contains: "sample_weights.json"
  key_links:
    - from: "scripts/build_dataset.py"
      to: "data/synthetic/split.json"
      via: "first-run write, subsequent-run read (frozen)"
      pattern: "split\\.json"
    - from: "scripts/build_dataset.py"
      to: "data/synthetic/train|test"
      via: "filesystem-level routing by split ID list"
      pattern: "train|test"
    - from: "scripts/label.py"
      to: "scripts/synthetic_weights.py"
      via: "SYNTHETIC_LC_WEIGHTS → sample_weights.json"
      pattern: "synthetic_weights|SYNTHETIC_LC_WEIGHTS"
---

<objective>
Restructure the synthetic pipeline so each (Azgaar source × render style) is its own
canonical per-map directory (resolved decision A4 — option (a), ~3× volume), add the
locked-shape synthetic loss weights, and implement the seeded stratified-by-template
train/test split with a frozen `split.json` (D-15..D-18) that guarantees EVAL-01's
zero-leakage held-out test set.

Purpose: This is the synthetic source family for PHASE-02 and the entire mechanism
for EVAL-01 (the Phase 4 held-out test set). The split must be whole-Azgaar-source-map
so all styles of a held-out source go to test — no cross-style leakage.

Output: refactored `label.py` + `build_dataset.py`, new `synthetic_weights.py`,
deterministic-split + frozen-manifest + no-intersection tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-build-a-dataset-of-pixel-label-pairs/02-CONTEXT.md
@.planning/phases/02-build-a-dataset-of-pixel-label-pairs/02-RESEARCH.md
@.planning/phases/02-build-a-dataset-of-pixel-label-pairs/02-PATTERNS.md
@.planning/phases/02-build-a-dataset-of-pixel-label-pairs/02-VALIDATION.md
@scripts/build_dataset.py
@scripts/label.py
@scripts/render.py
@scripts/biome_mapping.py
@scripts/historical/label.py
@scripts/build_historical_dataset.py

<interfaces>
<!-- EXISTING render.render_map(geojson_path, out, styles=("flat","illustrated","satellite")) -->
<!--   currently writes flat.png/illustrated.png/satellite.png into out/ -->
<!-- EXISTING label.make_labels(geojson_path, output_dir) -> writes land_cover.png + -->
<!--   topography.png ONLY (no image.png, no sample_weights.json yet). 255=NODATA, -->
<!--   255=WATER_TOPO. dims inferred from feature bbox. -->
<!-- LOCKED sample_weights.json shape (from historical/label.py): -->
<!--   {"land_cover_weights": {<class>: float,...}, "topography_weight": float, -->
<!--    "source": "synthetic", "map_file": "<name>"} -->
<!-- LANDCOVER_CLASSES keys come from scripts/biome_mapping.py (same 9 names as -->
<!--   HISTORICAL_LC_WEIGHTS: water, trees, shrubland, grassland, cropland, built_up, -->
<!--   bare_sparse, flooded_wetland, snow_ice). -->
<!-- COPY build_historical_dataset.py argparse sub-command scaffold (main 124-164, -->
<!--   add_common 133-135) and threadpool _process_one/cmd_build (73-117). -->
<!-- Azgaar template name location is UNKNOWN — Task 1 must inspect a real exported -->
<!--   GeoJSON (data/synthetic/raw/*.geojson) to find whether it lives in feature -->
<!--   properties or a top-level metadata block. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inspect Azgaar GeoJSON for template name; restructure per-(map×style) output + image.png</name>
  <read_first>
    - scripts/label.py (WHOLE — make_labels, _bbox, _rings; NODATA/WATER_TOPO sentinels)
    - scripts/render.py (render_map signature + how it names flat/illustrated/satellite outputs)
    - scripts/build_dataset.py (WHOLE — the render_map→make_labels per-geojson loop 36-42)
    - scripts/biome_mapping.py (LANDCOVER_CLASSES names; h_to_landcover/h_to_topo)
    - scripts/historical/label.py (canonical sample_weights.json shape lines 38-51, 95-149)
    - .planning/phases/02-build-a-dataset-of-pixel-label-pairs/02-PATTERNS.md (build_dataset.py + Per-Map Output Schema sections; Pitfall 6 option (a))
    - .planning/phases/02-build-a-dataset-of-pixel-label-pairs/02-CONTEXT.md (resolved A4 in planning_context: <azgaar_id>__<style>/ sharing labels, one image.png per dir)
  </read_first>
  <action>
    First, inspect an actual exported file: list `data/synthetic/raw/*.geojson` (or wherever
    the raw Azgaar exports live; if none present, inspect the sample fixture from Plan 01's
    conftest and document the gap in the SUMMARY). Determine where the heightmap template
    name lives — feature `properties`, a top-level `metadata`/`info` block, or absent. Record
    the finding. If the template name is NOT exposed anywhere, fall back to deriving a
    stratification key from the source filename prefix or a `template` arg, and note this in
    the SUMMARY for user awareness.
    Refactor the synthetic output per resolved decision A4 (option (a)): for each Azgaar
    source `<azgaar_id>.geojson` and each render style S in the configured styles, produce a
    map directory `<azgaar_id>__<S>/` containing exactly one `image.png` (the S-style render),
    plus `land_cover.png` and `topography.png` (identical across styles for the same source —
    generate once, copy/hardlink into each style dir), plus `sample_weights.json`. Modify
    `scripts/label.py` `make_labels` to additionally accept the rendered image and write it
    as `image.png`, and to write `sample_weights.json` using `synthetic_weights` (Task 2).
    Keep the existing `_bbox`/`_rings`/polygon-fill logic and the NODATA/WATER_TOPO sentinels
    unchanged. Fill `tests/test_render.py::test_output_dimensions_match` (offline, fixture
    GeoJSON): `land_cover.png` and `topography.png` have identical dimensions, and
    `image.png` matches them.
  </action>
  <verify>
    <automated>pytest tests/test_render.py -x -q --ignore=tests/integration</automated>
  </verify>
  <acceptance_criteria>
    - The template-name location finding is recorded (SUMMARY documents: properties / metadata block / absent+fallback)
    - For a fixture source with 2 styles, the build produces two dirs `<id>__<styleA>/` and `<id>__<styleB>/`, each with `image.png` + `land_cover.png` + `topography.png` + `sample_weights.json` (test asserts file presence)
    - `land_cover.png` is byte-identical between the two style dirs of the same source (shared label — test asserts)
    - `land_cover.png`, `topography.png`, `image.png` dimensions match within a style dir (test_output_dimensions_match)
    - `pytest tests/test_render.py -x -q --ignore=tests/integration` passes
  </acceptance_criteria>
  <done>Synthetic output conforms to the locked per-map schema with per-(source×style) dirs sharing labels; image.png + sample_weights.json present.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Checkpoint: Approve per-source loss-weight values</name>
  <decision>Approve the synthetic per-source loss-weight values for sample_weights.json</decision>
  <context>
    Only the historical source has locked loss weights (`HISTORICAL_LC_WEIGHTS`,
    `scripts/historical/label.py:40` — frozen). The synthetic stream needs its own
    `SYNTHETIC_LC_WEIGHTS` of identical dict shape (CONTEXT "Future iterations within
    Phase 2": planner proposes, user approves). Synthetic labels are ground-truth by
    construction, so the natural default is uniform 1.0. The dict shape and topography
    weight are locked; only the per-class float values need sign-off.
  </context>
  <options>
    <option id="uniform">
      <label>Uniform 1.0 for all 9 land-cover classes + topography_weight 1.0</label>
      <pros>Synthetic labels are exact by construction — no temporal-drift discount applies; symmetric, simplest, matches CONTEXT's stated expectation ("synthetic might be uniform-1.0")</pros>
      <cons>No class-imbalance correction at the weight layer (deferred to the DataLoader/sampler in Phase 3/4 instead)</cons>
    </option>
    <option id="rarity-weighted">
      <label>Uniform 1.0 except mild up-weight (1.3) for cropland/built_up/flooded_wetland</label>
      <pros>Nudges training toward the three classes synthetic under-produces and PROJECT.md up-weights</pros>
      <cons>Mixes class-balance concerns into the per-source weight that PROJECT.md scopes to temporal reliability; risks double-counting with satellite stratification</cons>
    </option>
  </options>
  <action>Checkpoint only — present the options below to the user, collect the decision, and record the chosen per-class float values. No code is written in this task; the approved values feed the weights module task that follows.</action>
  <verify>User has selected one option (or supplied explicit per-class floats); the choice is recorded in the plan SUMMARY.</verify>
  <done>A single weight option is approved and recorded for the downstream weights task.</done>
  <resume-signal>Select: uniform, rarity-weighted, or specify exact per-class floats</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Create synthetic_weights.py with the approved locked-shape weights</name>
  <read_first>
    - scripts/historical/label.py (HISTORICAL_LC_WEIGHTS + HISTORICAL_TOPO_WEIGHT shape, lines 38-51 — the schema to mirror exactly)
    - scripts/biome_mapping.py (LANDCOVER_CLASSES — the exact 9 key names)
    - scripts/label.py (post-Task-1 — where sample_weights.json is written)
    - .planning/phases/02-build-a-dataset-of-pixel-label-pairs/02-PATTERNS.md (Per-Map Output Schema — locked sample_weights.json dict)
  </read_first>
  <action>
    Create `scripts/synthetic_weights.py` exporting `SYNTHETIC_LC_WEIGHTS: dict[str,float]`
    (keys = the exact 9 LANDCOVER_CLASSES names: water, trees, shrubland, grassland,
    cropland, built_up, bare_sparse, flooded_wetland, snow_ice) and
    `SYNTHETIC_TOPO_WEIGHT: float`, populated with the values approved in the checkpoint.
    Add `write_sample_weights(output_dir, map_file)` writing the locked-shape JSON:
    `{"land_cover_weights": SYNTHETIC_LC_WEIGHTS, "topography_weight":
    SYNTHETIC_TOPO_WEIGHT, "source": "synthetic", "map_file": map_file}`. Wire
    `label.make_labels` (Task 1) to call it. Fill
    `tests/test_synthetic_weights.py`: assert the dict has exactly the 9 canonical keys
    (set-equal to HISTORICAL_LC_WEIGHTS keys), all values are floats, and the written JSON
    round-trips with `"source": "synthetic"` and the locked top-level keys.
    Do NOT modify `historical/label.py` or `HISTORICAL_LC_WEIGHTS`.
  </action>
  <verify>
    <automated>pytest tests/test_synthetic_weights.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/synthetic_weights.py` exports `SYNTHETIC_LC_WEIGHTS` with key set equal to `HISTORICAL_LC_WEIGHTS` keys (test asserts set equality)
    - written `sample_weights.json` has exactly the locked top-level keys (`land_cover_weights`, `topography_weight`, `source`, `map_file`) and `source == "synthetic"`
    - the approved values from the checkpoint are reflected (source assertion against the chosen option)
    - `scripts/historical/label.py` is unmodified (git diff empty for that file)
    - `pytest tests/test_synthetic_weights.py -x -q` passes
  </acceptance_criteria>
  <done>Synthetic weights module exists with approved values; every synthetic per-map dir gets a locked-shape sample_weights.json.</done>
</task>

<task type="auto">
  <name>Task 3: Seeded stratified train/test split with frozen split.json (D-15..D-18)</name>
  <read_first>
    - scripts/build_dataset.py (post-Task-1 — the per-source build loop)
    - scripts/build_historical_dataset.py (argparse sub-command scaffold main 124-164, add_common; threadpool 73-117 — copy the scaffold shape)
    - .planning/phases/02-build-a-dataset-of-pixel-label-pairs/02-CONTEXT.md (D-15..D-18 verbatim; A7 N=50 target)
    - .planning/phases/02-build-a-dataset-of-pixel-label-pairs/02-PATTERNS.md (build_dataset.py "ADD per D-15..D-18" section)
    - .planning/phases/02-build-a-dataset-of-pixel-label-pairs/02-VALIDATION.md (split test names: test_seeded_split_deterministic, test_no_train_test_intersection, test_split_manifest_frozen)
  </read_first>
  <action>
    Upgrade `build_dataset.py` to the `build_historical_dataset.py` argparse sub-command
    scaffold (`add_subparsers(dest="command", required=True)` + `add_common`). Keep the
    `render_map`→`make_labels` per-source loop. Add a seeded stratified splitter:
    - Stratify by Azgaar template (using the key location found in Task 1; if absent, the
      documented fallback key).
    - Seed is fixed (constant, e.g. `_SPLIT_SEED = 42`) for reproducibility (D-16).
    - Hold out ~15% of source maps, each template contributing proportionally (D-16).
    - The split is at the WHOLE Azgaar source-map level — ALL styles / pyramids / tiles of a
      held-out source go to test (D-15). Zero cross-style leakage by construction.
    - On the FIRST build, compute the split and WRITE `data/synthetic/split.json` listing
      the test-set source-map IDs (D-18). On EVERY subsequent build, READ `split.json`;
      IDs in the list → `data/synthetic/test/`, everything else (incl. newly generated
      sources) → `data/synthetic/train/` (D-17, D-18). split.json is FROZEN after first
      write — never recompute or mutate it.
    - Filesystem-level separation: write into sibling `data/synthetic/train/<id>__<style>/`
      and `data/synthetic/test/<id>__<style>/` with identical internal structure (D-17).
    Fill `tests/test_split.py`:
      - `test_seeded_split_deterministic` (offline): two splitter invocations on the same
        source-id set yield identical test-id lists.
      - `test_no_train_test_intersection` (offline, builds into tmp dirs): the set of map IDs
        under `test/` and under `train/` is disjoint (EVAL-01).
      - `test_split_manifest_frozen` (offline): write split.json, add new source IDs, rebuild
        → split.json test-id list is byte-unchanged; new IDs land in train/ (EVAL-01, D-18).
  </action>
  <verify>
    <automated>pytest tests/test_split.py -x -q --ignore=tests/integration</automated>
  </verify>
  <acceptance_criteria>
    - `build_dataset.py` uses `add_subparsers` and writes `data/synthetic/split.json` on first run only (grep `split.json`; test_split_manifest_frozen asserts immutability)
    - the splitter uses a fixed seed constant (grep for `seed` / a constant int) and is deterministic (test_seeded_split_deterministic passes)
    - `data/synthetic/train/` and `data/synthetic/test/` map-ID sets are disjoint (test_no_train_test_intersection passes — EVAL-01)
    - all styles of a held-out source go to test/ (test asserts both `<id>__styleA` and `<id>__styleB` of a held-out id are under test/, none under train/ — D-15)
    - newly generated sources after the first build land in train/ and do not alter split.json (test_split_manifest_frozen passes — D-18)
    - `pytest tests/test_split.py -x -q --ignore=tests/integration` passes
  </acceptance_criteria>
  <done>A frozen seeded stratified split routes whole Azgaar sources into train/ vs test/ with zero leakage; EVAL-01 is satisfied by construction.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Azgaar GeoJSON file → parser | Locally-authored export; low trust concern but malformed-JSON resilience still applies |
| Source map id → train/test directory name | Externally-named file stem used in a filesystem path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Tampering | EVAL-01 leakage — a held-out source's tiles reaching train/ | mitigate | Filesystem-level separation (D-17): train DataLoader points at train/ and cannot see test/. `test_no_train_test_intersection` + `test_split_manifest_frozen` enforce zero overlap and immutability |
| T-02-08 | Tampering | Azgaar `<id>` file stem → `data/synthetic/{train,test}/<id>__<style>/` path | mitigate | Sanitize the source stem (replace non-`[\w-]` with `_`) before path join, same pattern as the historical pipeline |
| T-02-09 | Denial of Service (self) | Malformed Azgaar GeoJSON (missing `properties.biome`/`height`) crashing the batch | mitigate | Per-source try/except in the build loop returns the source as failed (threadpool `_process_one` pattern) instead of aborting the whole build |
</threat_model>

<verification>
- `pytest tests/test_render.py tests/test_split.py tests/test_synthetic_weights.py -x -q --ignore=tests/integration` green
- `test_no_train_test_intersection` and `test_split_manifest_frozen` pass (EVAL-01 gate)
- Manual: confirm `data/synthetic/split.json` is written once and unchanged on rebuild
</verification>

<success_criteria>
- Each (Azgaar source × style) is a canonical per-map dir with the full locked output schema
- Synthetic sample_weights.json uses approved locked-shape values
- A frozen seeded stratified split guarantees the EVAL-01 held-out test set with zero leakage
</success_criteria>

<output>
After completion, create `.planning/phases/02-build-a-dataset-of-pixel-label-pairs/02-03-SUMMARY.md`
</output>
