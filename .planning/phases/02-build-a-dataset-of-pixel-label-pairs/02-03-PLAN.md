---
phase: 02-build-a-dataset-of-pixel-label-pairs
plan: 03
type: execute
wave: 2
depends_on: ["02-01", "02-02"]
files_modified:
  - scripts/build_historical_dataset.py
  - tests/test_historical.py
autonomous: true
requirements: [PHASE-02]
nyquist_compliant: true

must_haves:
  truths:
    - "build_historical_dataset.py search writes unregistered_manifest.json to gs://.../data/historical/raw/unregistered_manifest.json"
    - "build_historical_dataset.py build writes the dataset (pyramids) to gs://.../data/historical/dataset/ via _GCSWriter"
    - "raw Rumsey GeoTIFF downloads remain local scratch (ephemeral-safe per RW-04)"
    - "the existing ThreadPoolExecutor build path passes the shared fs through, thread-safe"
    - "no canonical local dataset copy; --out-dir defaults to a gs:// URI"
  artifacts:
    - path: "scripts/build_historical_dataset.py"
      provides: "GCS-canonical historical outputs (RW-04)"
      contains: "_GCSWriter"
    - path: "tests/test_historical.py"
      provides: "manifest-written-to-GCS + GCS dataset write coverage"
      contains: "def test_manifest_written_to_gcs"
  key_links:
    - from: "scripts/build_historical_dataset.py"
      to: "scripts/gcs_io.py"
      via: "import _GCSWriter; tiling.tile(out_root=_GCSWriter(...))"
      pattern: "from gcs_io import"
    - from: "scripts/build_historical_dataset.py"
      to: "gs://.../data/historical/raw/unregistered_manifest.json"
      via: "fs.pipe_file of the emitted manifest"
      pattern: "unregistered_manifest"
---

<objective>
Bring the historical pipeline under GCS-canonical persistence (RW-04):
`build_historical_dataset.py search` writes `unregistered_manifest.json`
(the v2 hand-off artifact) to GCS, and `build` streams the built dataset to
`gs://.../data/historical/dataset/` via the `_GCSWriter` shim. Raw Rumsey
GeoTIFF downloads stay local scratch (re-downloadable from LUNA — RW-04
research finding A-R5).

Purpose: Historical is one of the three families that must survive ephemeral
compute. The synthetic root-cause defect applies identically here.
Output: GCS-writing build_historical_dataset.py + offline GCS-write tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-build-a-dataset-of-pixel-label-pairs/02-CONTEXT.md
@.planning/phases/02-build-a-dataset-of-pixel-label-pairs/02-RESEARCH.md
@.planning/phases/02-build-a-dataset-of-pixel-label-pairs/02-PATTERNS.md
@.planning/phases/02-build-a-dataset-of-pixel-label-pairs/02-01-SUMMARY.md
@.planning/phases/02-build-a-dataset-of-pixel-label-pairs/02-02-SUMMARY.md

<interfaces>
<!-- Contracts from 02-01 (gcs_io) and the 02-02 tiling.py refactor. -->

From scripts/gcs_io.py: class _GCSWriter(fs, prefix); GCS_PROJECT,
BUCKET="mapclass-training-northeast1", DATA_PREFIX="mapclass-training-northeast1/data"

From scripts/tiling.py (post-02-02): tile(map_dir, out_root=_GCSWriter(...),
max_workers=32) writes pyramids to GCS; local-Path path unchanged.

build_historical_dataset.py touch points (verified against source):
- L52: manifest_path = raw_dir / "unregistered_manifest.json"
- L76: rumsey.emit_manifest(unregistered, manifest_path)
- L96: def _process_one(tif: Path, out_dir: Path)
- L110: tiling.tile(sample_dir)
- L127: out_dir.mkdir(parents=True, exist_ok=True)
- L138-144: ThreadPoolExecutor(max_workers=workers) + as_completed (already present)
- L168 --raw-dir default Path("data/historical/raw"); L179/186 --out-dir default Path("data/historical/dataset")
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: build_historical_dataset.py — GCS manifest + GCS dataset write (RW-04)</name>
  <read_first>
    - scripts/build_historical_dataset.py (full file — manifest emit L52/76, _process_one L96, tiling L110, out_dir.mkdir L127, ThreadPoolExecutor L138-144, argparse L168-188)
    - scripts/gcs_io.py (_GCSWriter contract from 02-01)
    - scripts/tiling.py (post-02-02 _GCSWriter-aware tile() signature)
    - scripts/seg/gcs_checkpoint.py (fs.pipe_file write idiom lines 127-133; lazy-import lines 37-39)
    - .planning/phases/02-build-a-dataset-of-pixel-label-pairs/02-PATTERNS.md (build_historical_dataset.py MODIFY section lines 206-242)
    - .planning/phases/02-build-a-dataset-of-pixel-label-pairs/02-RESEARCH.md (RW-04 Historical Pipeline table lines ~459-471; A-R5 assumption)
    - tests/test_historical.py (current state if present — extend/create; mirror tests/test_seg_gcs.py mock pattern)
  </read_first>
  <behavior>
    - test_manifest_written_to_gcs: search path emits unregistered_manifest.json keyed under mapclass-training-northeast1/data/historical/raw/ in the mock GCS store, NOT to a local path
    - test_historical_dataset_written_to_gcs: build path routes tiling.tile through _GCSWriter at .../data/historical/dataset/<sample>/pyramids; mock store receives the pyramid objects
    - test_raw_geotiff_stays_local: raw Rumsey GeoTIFF download target remains a local scratch Path (no GCS write of raw .tif)
  </behavior>
  <action>
    Modify `scripts/build_historical_dataset.py`. Apply the gcs_checkpoint.py
    lazy-import block and instantiate `fs =
    gcsfs.GCSFileSystem(project=GCS_PROJECT)` once per entrypoint; import
    `_GCSWriter` from `gcs_io`.

    Manifest (L52/L76): keep `rumsey.emit_manifest` writing to a local scratch
    Path (do NOT modify rumsey.py — PATTERNS.md option (b)), then read the
    emitted bytes and `fs.pipe_file(f"{DATA_PREFIX}/historical/raw/
    unregistered_manifest.json", manifest_bytes)`. This is the v2 hand-off
    artifact and must land in GCS.

    Dataset write: at the L110 `tiling.tile(sample_dir)` call inside
    `_process_one`, pass `out_root=_GCSWriter(fs,
    f"{DATA_PREFIX}/historical/dataset/{sample_dir.name}/pyramids")`;
    `sample_dir` stays local scratch for reads. Thread the shared `fs` (and the
    GCS prefix) through `_process_one`'s signature so the existing
    `ThreadPoolExecutor(max_workers=workers)` loop at L138-144 passes one
    shared thread-safe `gcsfs.GCSFileSystem` (each `_GCSWriter` per-sample is
    constructed inside the worker — not shared across threads). At L127 replace
    `out_dir.mkdir(...)` with a no-op for GCS prefixes (keep local mkdir only
    when out_dir is a local Path / --local-ok).

    Raw Rumsey GeoTIFF downloads: keep writing to a local scratch dir
    (re-downloadable from LUNA per RW-04 A-R5) — do NOT upload raw .tif to GCS.

    Argparse (L168/179/186): change `--out-dir` default to
    `"gs://mapclass-training-northeast1/data/historical/dataset"`; the search
    `--raw-dir` stays a local scratch default (raw TIF staging) but the emitted
    manifest is force-written to GCS regardless. Add a `--local-ok` flag
    consistent with build_dataset.py: a non-`gs://` --out-dir without it
    hard-errors (Pitfall R-1 guard).
  </action>
  <verify>
    <automated>cd /home/drdreadknee/mapclass && pytest tests/test_historical.py -x</automated>
  </verify>
  <acceptance_criteria>
    - build_historical_dataset.py imports _GCSWriter from gcs_io
    - the unregistered_manifest.json write target is "mapclass-training-northeast1/data/historical/raw/unregistered_manifest.json" via fs.pipe_file
    - tiling.tile call inside _process_one passes out_root=_GCSWriter(...) with prefix under data/historical/dataset
    - raw GeoTIFF download path is a local Path (no fs.pipe_file / _GCSWriter for raw .tif)
    - --out-dir default starts with "gs://"; --local-ok present
    - `pytest tests/test_historical.py::test_manifest_written_to_gcs -x` exits 0
    - `pytest tests/test_historical.py -x` exits 0
  </acceptance_criteria>
  <done>
    Historical search writes manifest to GCS; build streams dataset to GCS via
    _GCSWriter; raw TIFs stay ephemeral local; tests green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build_historical_dataset.py → GCS bucket | ADC write of manifest + dataset pyramids |
| Rumsey/LUNA + Allmaps IIIF → local scratch | network input, ephemeral by design |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-20 | Information disclosure | silent local-only dataset write (root-cause defect) | mitigate | --out-dir defaults gs://; non-gs:// requires --local-ok (Pitfall R-1) |
| T-02-21 | Tampering | partial pyramid upload on preemption | mitigate | pipe_file atomic per object; missing-file SKIP guard precedes tile(); 02-05 verify_pull spot-checks |
| T-02-22 | Spoofing | GCS auth | mitigate | ADC-only lazy-gcsfs pattern from gcs_checkpoint.py; no secrets in source |
| T-02-23 | Repudiation | raw TIF loss treated as data loss | accept | Rumsey GeoTIFFs re-downloadable from LUNA (RW-04 A-R5); only built dataset is GCS-canonical |
| T-02-SC | Tampering | pip installs | accept | No new packages — gcsfs/fsspec already audited [OK]; no install task |
</threat_model>

<verification>
- `pytest tests/test_historical.py -x` green
- `grep -n "_GCSWriter" scripts/build_historical_dataset.py` non-empty
- `grep -n "historical/raw/unregistered_manifest.json" scripts/build_historical_dataset.py` non-empty
</verification>

<success_criteria>
- search writes unregistered_manifest.json to GCS
- build streams dataset pyramids to GCS via _GCSWriter, shared fs through ThreadPoolExecutor
- raw Rumsey TIFs remain local scratch (ephemeral-safe)
- --out-dir gs:// default + --local-ok guard
</success_criteria>

<output>
Create `.planning/phases/02-build-a-dataset-of-pixel-label-pairs/02-03-SUMMARY.md` when done
</output>
