Pillow
numpy
rasterio
pyproj
requests
gcsfs  # GCS-canonical persistence (scripts/gcs_io.py)
h3>=4  # H3 hex grid for the terrain prior dataset (scripts/hexprior/)
pystac-client>=0.9
pytest>=8
pytest-mock>=3
matplotlib  # notebooks — dataset visualization
jupyterlab  # notebook server for dataset inspection
ipykernel   # registers the .venv kernel for Jupyter
nbconvert   # headless notebook execution / output stripping
# torch — reintroduce with the masked-hex prior model (not needed by data pipeline)
