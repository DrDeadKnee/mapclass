---
phase: 03-build-a-dense-semantic-segmentation-pipeline
plan: 05
type: execute
wave: 3
depends_on: [03-01, 03-02, 03-03, 03-04]
files_modified:
  - scripts/seg/model.py
  - scripts/seg/recursive.py
autonomous: true
requirements: [PHASE-03, EVAL-03]
nyquist_compliant: true

must_haves:
  truths:
    - "SegModel assembles backbone + shared decoder + 2 heads and accepts a 12-ch prior; both D-03a variants constructed (A frozen+decoder-prior, B widened-trainable+input-prior), neither trained (D-06/D-06a)"
    - "Recursive coarse-to-fine inference walks pyramid.json parent->child literally, cropping the exact quadrant and zero-priming the 896 cold-start (D-03/D-04)"
    - "The assembled model accepts a regional map tile and outputs dense (B,9,H,W)+(B,3,H,W) tensors with no Gemma reachable (PHASE-03 SC#1/SC#2/SC#3)"
    - "DINOv2 and Swin variants run on the same input pipeline + decoder + orchestrator as benchmark baselines (EVAL-03)"
  artifacts:
    - path: "scripts/seg/model.py"
      provides: "SegModel = backbone + decoder + 2 heads + prior injection; Variant A/B switch"
      contains: "class SegModel"
    - path: "scripts/seg/recursive.py"
      provides: "coarse-to-fine orchestrator walking pyramid.json (D-03/D-04)"
      contains: "pyramid.json"
  key_links:
    - from: "scripts/seg/model.py"
      to: "scripts/seg/backbones.py + decoder.py + heads.py"
      via: "compose Backbone -> shared decoder -> 2 heads; prior enters per Variant A/B"
      pattern: "Backbone|decoder|head"
    - from: "scripts/seg/recursive.py"
      to: "scripts/tiling.py pyramid.json"
      via: "literal parent->child index walk + quadrant box crop (D-04)"
      pattern: "children|crop|interpolate"
---

<objective>
Wave 3: assemble the pipeline. `scripts/seg/model.py` composes
backbone → shared decoder → two heads and injects the 12-channel recursive prior,
constructing BOTH D-03a variants (A: frozen backbones + decoder-level prior;
B: widened-trainable 15-ch patch-embed + input-level prior) — neither trained
(D-06/D-06a). `scripts/seg/recursive.py` is the coarse-to-fine orchestrator that
walks `pyramid.json` parent→child literally, crops the exact quadrant, and
zero-primes the 896 cold-start (D-03/D-04). The smoke + gated integration tests
(scaffolded in 03-01) become the Phase-3 deliverable proof.

Purpose: This is the Inference-orchestrator tier and the final wiring — RESEARCH:
"Phase 3's only genuinely new logic is the wiring." It closes PHASE-03 SC#1/SC#3
and the EVAL-03 swappable-backbone deliverable.
Output: `scripts/seg/model.py` + `scripts/seg/recursive.py` satisfying
`tests/test_seg_recursive.py` and `tests/test_seg_smoke.py` (both variants), with
`tests/integration/test_seg_online.py` green or skipped-with-reason.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-build-a-dense-semantic-segmentation-pipeline/03-CONTEXT.md
@.planning/phases/03-build-a-dense-semantic-segmentation-pipeline/03-RESEARCH.md
@.planning/phases/03-build-a-dense-semantic-segmentation-pipeline/03-PATTERNS.md
@.planning/phases/03-build-a-dense-semantic-segmentation-pipeline/03-VALIDATION.md

<interfaces>
<!-- composed contracts from prior waves -->
seg.backbones: Backbone protocol; SiglipBackbone/Dinov2Backbone/SwinBackbone;
  feature_strides/feature_channels; Variant-A (frozen) vs Variant-B (15-ch widened-trainable patch-embed)
seg.decoder: shared UPerNet PPM+FPN decoder(feature_strides,feature_channels) -> (B,C',H,W)
seg.heads:   two thin 1x1-conv heads -> (B,9,H,W) LC + (B,3,H,W) topo logits
seg.dataset: PyramidDataset (train/-only) surfacing image/lc/topo/sample_weights

<!-- D-03a prior injection -->
prior = 12 raw-softmax channels (9 LC + 3 topo), bilinearly resized (NOT argmax/one-hot, NOT logits)
Variant A: frozen backbones; tiny trainable prior-encoder injects 12-ch at the
           DECODER working resolution (summed/concatenated). Backbone-agnostic.
Variant B: 15-ch widened patch-embed (3 RGB + 12 prior) at the INPUT; widened
           patch-embed trainable; pretrained RGB weights kept; extras zero-init.
Both share decoder/heads/orchestration/Backbone-protocol. Phase 3 builds both; trains NEITHER (D-06a).

<!-- recursive walk (RESEARCH Code Examples; scripts/tiling.py:84-114) -->
manifest tiles[].{id,x,y,size,children}; root id "896", children "448_{ci}", grand "224_{ci}_{gi}"
child prior crop: parent_prob[..., cy-oy : cy-oy+s, cx-ox : cx-ox+s] then F.interpolate to child grid
896 cold-start: prior = zeros(1,12,S,S). Use manifest boxes literally; do NOT recompute geometry (D-04).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SegModel assembly with both D-03a prior-injection variants</name>
  <files>scripts/seg/model.py</files>
  <read_first>
    - tests/test_seg_backbones.py + tests/test_seg_smoke.py (the SegModel + both-variant contract — written in 03-01)
    - scripts/seg/backbones.py, scripts/seg/decoder.py, scripts/seg/heads.py (the composed contracts from 03-02/03-04)
    - 03-CONTEXT.md D-02 (shared trunk), D-03 (explicit recursive prior), D-03a (BOTH variants — A frozen+decoder-prior, B widened-trainable+input-prior), D-06/D-06a (construction-only; build B's structural mod but do NOT train)
    - 03-RESEARCH.md Pattern 4 (prior encoding = 12 raw-softmax ch; Variant-A decoder-level injection rationale), Pattern 1 (Gemma exclusion), Anti-Patterns (no argmax/one-hot prior, no training loop)
    - 03-PATTERNS.md section model.py (spec-driven novel; Variant A/B switch has no analog — follow CONTEXT D-03a verbatim) + Shared Patterns (Provable Gemma exclusion)
  </read_first>
  <behavior>
    - SegModel composes a chosen Backbone -> shared decoder -> two heads, returning (B,9,H,W) LC logits + (B,3,H,W) topo logits at full tile resolution.
    - forward(rgb, prior) accepts a 12-channel prior tensor (9 LC + 3 topo softmax probs); a zero prior is valid (896 cold-start).
    - Variant A: any (frozen) backbone takes RGB only; a tiny trainable prior-encoder maps the 12-ch prior to the decoder working resolution and is summed/concatenated into the decoder. Backbone-agnostic.
    - Variant B: the backbone's widened 15-ch patch-embed (from 03-02) takes concat([rgb, prior_resized_to_input]); prior enters at input.
    - Both variants share the SAME decoder/heads; a constructor flag selects A vs B. No `language_model`/`gemma` reachable from EITHER variant (SC#2). NO training loop / optimizer / `.backward()` (D-06/D-06a).
  </behavior>
  <action>
    Create `scripts/seg/model.py` with the decision-referenced docstring.
    Define `class SegModel(nn.Module)` composing a `Backbone` (passed in /
    selected by name — SigLIP primary, DINOv2/Swin benchmarks behind D-05) into
    the shared decoder (`scripts/seg/decoder.py`, constructed from
    `backbone.feature_strides`/`feature_channels`) into the two thin heads
    (`scripts/seg/heads.py`). `forward(rgb, prior)` takes the 12-channel prior
    (9 LC + 3 topo RAW softmax probabilities — NOT argmax/one-hot, NOT logits;
    RESEARCH Pattern 4 / Anti-Patterns) where an all-zero prior is the valid
    896 cold-start. Implement BOTH D-03a variants behind a constructor flag /
    factory (variant A or B):
      - Variant A: backbone consumes RGB only (stays frozen); a small TRAINABLE
        prior-encoder (e.g. a couple of conv layers) maps the 12-ch prior to the
        decoder working resolution and is summed/concatenated into the decoder
        (RESEARCH Pattern 4 recommended injection point). Backbone-agnostic —
        works identically for SigLIP/DINOv2/Swin.
      - Variant B: route concat([rgb, F.interpolate(prior to input HxW)])
        (15 channels) through the backbone's widened patch-embed built in 03-02
        (the widened patch-embed is the only trainable param; rest frozen).
    Both variants MUST share the identical decoder + heads instances/topology
    (D-02; the EVAL-03 apples-to-apples requirement). Add the Gemma-absence
    hardening (Shared Patterns / Pitfall 1): SegModel must expose its parameters
    such that no `language_model` param-name and no `gemma` module-type is
    reachable from EITHER variant. Construction-only: NO optimizer, NO
    `.backward()`, NO training loop, NO unfreeze hook beyond the deliberate
    Variant-B widened-patch-embed / Variant-A prior-encoder trainable params
    (D-06/D-06a). Decoder/head/backbone discretion choices were fixed upstream —
    reuse them, do not re-litigate.
  </action>
  <verify>
    <automated>python -m pytest tests/test_seg_backbones.py tests/test_seg_decoder.py -k "variant or no_gemma or model" -x -q -m "not integration"</automated>
  </verify>
  <acceptance_criteria>
    - `from seg.model import SegModel` succeeds
    - SegModel variant A and variant B both build for SigLIP, DINOv2, and Swin backbones
    - `forward(rgb, prior)` returns `(B,9,H,W)` + `(B,3,H,W)` at full tile resolution for a zero prior AND a non-zero 12-ch prior, no NaN
    - Independent assertion per variant: no `language_model` param-name / `gemma` module-type reachable from EITHER (PHASE-03 SC#2)
    - Variant A backbone receives 3-ch RGB (frozen); Variant B backbone receives 15-ch input via the widened patch-embed; both share the same decoder+heads topology
    - No optimizer/`.backward()`/training loop present (D-06/D-06a)
  </acceptance_criteria>
  <done>SegModel assembled; both D-03a variants constructed (untrained) for all 3 backbones; Gemma provably absent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Recursive c2f orchestrator (pyramid.json walk) + smoke/integration close</name>
  <files>scripts/seg/recursive.py</files>
  <read_first>
    - tests/test_seg_recursive.py + tests/test_seg_smoke.py + tests/integration/test_seg_online.py (the contracts — written in 03-01)
    - scripts/seg/model.py (SegModel forward(rgb, prior) from Task 1)
    - scripts/seg/dataset.py (PyramidDataset tile reading from 03-03)
    - scripts/tiling.py lines 84-114, 170-196 (pyramid.json schema, stable IDs, quadrant order, manifest assembly)
    - tests/test_tiling.py lines 50-74, 105-135 (_load_manifest / _box / test_nested_alignment quadrant geometry)
    - 03-RESEARCH.md Code Examples (recursive_predict literal walk), Pattern 4 (spatial alignment D-04), Pitfall 5 (prior-channel quadrant misalignment), Anti-Patterns (no geometry recompute, no training)
    - 03-CONTEXT.md D-03 / D-04 (literal parent->child walk; zeros at 896 cold-start) + D-03a (both variants share this orchestration)
    - 03-PATTERNS.md section recursive.py (manifest vocabulary copy target; quadrant-box crop idiom)
  </read_first>
  <behavior>
    - recursive_predict(model, pyramid_dir, ...) loads pyramid.json via `json.loads((dir/"pyramid.json").read_text())`, indexes tiles by id, finds the size-896 root.
    - Pass 1: 896 root, prior = zeros(1,12,S,S) cold-start -> softmax probs cached by tile id.
    - Pass 2+: for each child, crop the parent prob map at the EXACT manifest quadrant box `parent_prob[..., cy-oy:cy-oy+s, cx-ox:cx-ox+s]` then `F.interpolate` to the child grid; feed as the 12-ch prior; recurse 896->448->224 via the stored `children` indices (D-04 literal walk; no geometry recompute).
    - Works for either D-03a variant (the orchestrator only calls `model(rgb, prior)`; variant differences are inside SegModel).
    - A known one-hot blob placed in a parent quadrant lands in the correct child (Pitfall 5 alignment test); the 896 cold-start prior is exactly zeros.
  </behavior>
  <action>
    Create `scripts/seg/recursive.py` with the decision-referenced docstring.
    Implement the coarse-to-fine orchestrator mirroring RESEARCH Code Examples
    `recursive_predict` structure and the scripts/tiling.py manifest vocabulary
    EXACTLY (D-04: read `tiles[].{id,x,y,size,children}`, index by id, find the
    size-896 root; use the manifest box arithmetic `(child.x-parent.x,
    child.y-parent.y, +size)` — the `tests/test_tiling.py::_box` idiom — never
    recompute pyramid geometry; Anti-Patterns). Pass 1: run `model(rgb,
    zeros(1,12,S,S))` on the 896 root (cold-start zeros prior), softmax the
    LC+topo logits into a 12-channel probability map, cache by tile id. Pass 2:
    for each child id in `tile["children"]`, crop the parent probability map at
    the exact quadrant `parent_prob[..., cy-oy:cy-oy+s, cx-ox:cx-ox+s]`, then
    `F.interpolate(..., mode="bilinear", align_corners=False)` to the child tile
    grid, pass as the 12-ch prior, recurse 896->448->224. The orchestrator is
    variant-agnostic (it only calls `model(rgb, prior)`; A vs B differences live
    in SegModel). Manifest read idiom: `json.loads((pyramid_dir /
    "pyramid.json").read_text())` (mirror test_tiling.py:50-51). Use the
    PyramidDataset / tile-reading from 03-03 for RGB tile loading. Forward-only:
    wrap in `torch.no_grad()`; NO optimizer / `.backward()` / training loop /
    overfit run (D-06; RESEARCH Anti-Patterns — the smoke test is forward-only).
    This task also closes the Phase-3 deliverable: ensure
    `tests/test_seg_smoke.py` (both variants on the `mini_pyramid` fixture) and
    `tests/test_seg_recursive.py` (quadrant alignment + 896 cold-start zeros)
    pass, and that `tests/integration/test_seg_online.py` either passes on a host
    with the Phase-1 adapter + Phase-2 data or skips-with-reason (OQ1 RESOLVED).
  </action>
  <verify>
    <automated>python -m pytest tests/test_seg_recursive.py tests/test_seg_smoke.py tests/integration/test_seg_online.py -q -rs</automated>
  </verify>
  <acceptance_criteria>
    - `from seg.recursive import recursive_predict` succeeds
    - On the `mini_pyramid` fixture the orchestrator runs 896->448->224 and produces dense (B,9,H,W)+(B,3,H,W) per finest tile, no NaN, for BOTH D-03a variants (test_seg_smoke green)
    - Pitfall-5 alignment case green: a one-hot blob in a parent quadrant lands in the correct child; 896 cold-start prior is exactly zeros (test_seg_recursive green)
    - No pyramid geometry recomputed — only `pyramid.json` boxes used (grep: `children` indexed, no sliding-window recompute)
    - `tests/integration/test_seg_online.py` passes OR reports skip-with-reason (`-rs` shows the reason) when Phase-1 adapter / Phase-2 data absent
    - No optimizer/`.backward()`/training loop present; orchestrator under `torch.no_grad()` (D-06)
  </acceptance_criteria>
  <done>Recursive c2f orchestrator walks pyramid.json literally; Phase-3 smoke deliverable green for both variants; integration gated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local Phase-2 pyramid.json/PNG → orchestrator | Untrusted local manifest + image parsing during the recursive walk |
| Phase-1 adapter + base checkpoint → SegModel (integration) | Pickle/torch.load deserialization surface (real-tile path) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-12 | Tampering | recursive prior quadrant misalignment | mitigate | Crop parent prob with the literal `pyramid.json` box (`cy-oy:cy-oy+s, cx-ox:cx-ox+s`) THEN resize; never recompute geometry (D-04); Pitfall-5 alignment test (one-hot blob lands in correct child) is a blocking gate |
| T-03-13 | Spoofing | Gemma reachable through assembled SegModel forward | mitigate | Per-variant parameter-graph assertion: no `language_model` param-name / `gemma` module-type reachable from either variant (PHASE-03 SC#2) — enforced in test_seg_backbones + smoke |
| T-03-14 | Elevation of Privilege | adapter/base checkpoint deserialization (integration) | mitigate | `--adapter-dir`/`--model-id` are explicit user-supplied paths (never auto-fetched from untrusted URLs); prefer safetensors/`weights_only` where the loader API allows; integration test skips-with-reason rather than fetching when artifacts absent |
| T-03-15 | Denial of Service | unbounded recursion / huge prob cache | accept | Pyramid depth is fixed at 3 (896/448/224, 21 tiles) by the Phase-2 contract; cache is bounded by manifest size; single-process research run |
</threat_model>

<verification>
- `pytest tests/test_seg_recursive.py tests/test_seg_smoke.py -x -q -m "not integration"` green for BOTH variants
- `pytest -q -m "not integration"` full offline suite green (Phase-2 tests included — no regression)
- `pytest -q -m integration -rs` green OR explicitly skipped-with-reason (Phase-1/Phase-2 artifacts)
- Manual grep: no `optim`/`.backward(`/training loop in scripts/seg/model.py or recursive.py (D-06/D-06a)
</verification>

<success_criteria>
- SegModel composes backbone + shared decoder + 2 heads; both D-03a variants constructed (A frozen+decoder-prior, B widened-trainable+input-prior), NEITHER trained (D-06/D-06a)
- Assembled model outputs dense (B,9,H,W)+(B,3,H,W) with provably no Gemma in the forward graph (PHASE-03 SC#1/SC#2)
- Recursive c2f inference walks pyramid.json parent->child literally, zero cold-start at 896 (PHASE-03 SC#3, D-03/D-04)
- DINOv2 + Swin run the same pipeline as benchmark baselines (EVAL-03 seam closed for Phase-4)
- Phase-3 smoke deliverable green for both variants; integration test gated/skips-with-reason
</success_criteria>

<output>
After completion, create `.planning/phases/03-build-a-dense-semantic-segmentation-pipeline/03-05-SUMMARY.md`
</output>
