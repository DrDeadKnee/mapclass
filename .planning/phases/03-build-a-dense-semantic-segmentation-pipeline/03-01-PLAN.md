---
phase: 03-build-a-dense-semantic-segmentation-pipeline
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - requirements.txt
  - tests/conftest.py
  - tests/test_seg_backbones.py
  - tests/test_seg_decoder.py
  - tests/test_seg_recursive.py
  - tests/test_seg_dataset.py
  - tests/test_seg_smoke.py
  - tests/integration/__init__.py
  - tests/integration/test_seg_online.py
  - scripts/seg/__init__.py
autonomous: true
requirements: [PHASE-03, EVAL-03]
nyquist_compliant: true

must_haves:
  truths:
    - "timm is a declared dependency so DINOv2/Swin benchmark backbones can be built (EVAL-03)"
    - "transformers attribute path (4.x vs 5.x vision_tower) is resolved deterministically"
    - "Every Phase-3 seg behavior has a failing-but-importable pytest scaffold to verify against"
    - "A synthetic mini-pyramid fixture exists so all seg tests run fully offline under tmp_path"
  artifacts:
    - path: "requirements.txt"
      provides: "timm pin + resolved transformers pin"
      contains: "timm"
    - path: "scripts/seg/__init__.py"
      provides: "importable seg package (pythonpath=scripts already set)"
    - path: "tests/conftest.py"
      provides: "mini_pyramid + stub_vision_config fixtures"
      contains: "mini_pyramid"
    - path: "tests/test_seg_backbones.py"
      provides: "backbone shape/stride + Gemma-absence + Variant-B scaffolds"
    - path: "tests/test_seg_decoder.py"
      provides: "decoder/head output-shape scaffolds"
    - path: "tests/test_seg_recursive.py"
      provides: "prior quadrant-alignment + Variant-A injection scaffolds"
    - path: "tests/test_seg_dataset.py"
      provides: "split-safety + weight-surfacing scaffolds"
    - path: "tests/test_seg_smoke.py"
      provides: "both-variant end-to-end forward scaffold"
    - path: "tests/integration/test_seg_online.py"
      provides: "gated real-adapter real-tile scaffold"
  key_links:
    - from: "tests/conftest.py"
      to: "scripts/tiling.py"
      via: "mini_pyramid fixture calls tiling.tile() on a synthesized _make_map_dir"
      pattern: "tiling\\.(tile|enumerate)"
    - from: "tests/test_seg_*.py"
      to: "scripts/seg"
      via: "from seg.X import ... (pythonpath=scripts)"
      pattern: "from seg\\."
---

<objective>
Wave 0: establish the segmentation test infrastructure and resolve dependency
gaps before any `scripts/seg/` module is written. This plan adds the missing
`timm` dependency, resolves the transformers 4.x vs 5.x PaliGemma attribute-path
break, creates the shared offline mini-pyramid fixture, and lands every seg test
file as an importable scaffold so all downstream tasks (03-02..03-05) have a
concrete `<automated>` command to verify against (Nyquist).

Purpose: Phase-3 RESEARCH flags `timm` as a hard-missing dependency (no
fallback) and the transformers PaliGemma submodule path moved from
`.vision_tower` (4.x) to `.model.vision_tower` (5.x). Without Wave 0, no
downstream seg task has a test to satisfy and the DINOv2/Swin EVAL-03 backbones
cannot be built.
Output: `requirements.txt` updated, `scripts/seg/__init__.py`, shared
`mini_pyramid` + `stub_vision_config` fixtures in `tests/conftest.py`, and 6
test scaffolds (5 unit + 1 gated integration) that import cleanly and fail with
clear assertions/skips until their target modules land.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-build-a-dense-semantic-segmentation-pipeline/03-CONTEXT.md
@.planning/phases/03-build-a-dense-semantic-segmentation-pipeline/03-RESEARCH.md
@.planning/phases/03-build-a-dense-semantic-segmentation-pipeline/03-PATTERNS.md
@.planning/phases/03-build-a-dense-semantic-segmentation-pipeline/03-VALIDATION.md

<interfaces>
<!-- pyramid.json schema the fixture must produce (scripts/tiling.py:84-95, 176-185) -->
Per-tile entry: id, x, y, size, children,
  image="{id}_image.png", land_cover="{id}_land_cover.png", topography="{id}_topography.png"
Manifest top-level: pyramid_id, origin=[ox,oy], source_map, source_size=[w,h],
  scales=[896,448,224], stride=448, tiles=[...]
Stable IDs: root "896"; children "448_{ci}" ci 0..3 quadrant order
  ((0,0),(1,0),(0,1),(1,1)); grandchildren "224_{ci}_{gi}".
_REQUIRED_FILES = ("image.png","land_cover.png","topography.png","sample_weights.json")

<!-- Test conventions to mirror -->
tests/test_tiling.py:40-47  _make_map_dir(base,w,h): RGB+L PNGs + sample_weights.json
tests/test_tiling.py:50-74  _load_manifest / _box / _contains / _disjoint helpers
tests/test_tiling.py:85-103 test_pyramid_tile_count: _make_map_dir then tiling.tile(src)
tests/conftest.py:144-172   tiny_geotiff: write-under-tmp_path / return-path idiom
pytest.ini: testpaths=tests, pythonpath=scripts, marker integration

<!-- transformers path break (RESEARCH Pitfall 3) -->
4.x: PaliGemmaForConditionalGeneration.vision_tower
5.x: PaliGemmaForConditionalGeneration.model.vision_tower
Defensive accessor required; resolved transformers version must be pinned.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve and pin dependencies (timm + transformers path)</name>
  <files>requirements.txt, scripts/seg/__init__.py</files>
  <read_first>
    - requirements.txt (the file being modified; current pins: transformers>=4.41, peft>=0.10, NO timm)
    - 03-RESEARCH.md sections Standard Stack + Environment Availability + Pitfall 3
    - 03-PATTERNS.md section requirements.txt (append-only edit convention)
  </read_first>
  <action>
    Append `timm>=1.0.27` to requirements.txt following the existing one-per-line
    `pkg>=ver` convention (RESEARCH Standard Stack: timm 1.0.27 PyPI-verified
    2026-05-15; backs DINOv2/Swin behind D-05 for EVAL-03). Resolve the
    transformers 4.x vs 5.x PaliGemma path break (Pitfall 3): determine the
    importable transformers version in the execution environment via
    `python -c "import transformers; print(transformers.__version__)"`; tighten
    the requirements.txt transformers line to a lower bound matching the resolved
    major (e.g. `transformers>=5.8` if 5.x is present, else keep `>=4.41`) and
    add an inline comment recording which PaliGemma submodule path
    (`.vision_tower` vs `.model.vision_tower`) that resolution implies. Do NOT
    change peft/torch/pytest pins. Create `scripts/seg/__init__.py` as an empty
    package marker (optionally `__all__ = []`) so `from seg.X import ...`
    resolves under pytest.ini `pythonpath = scripts`. If neither transformers nor
    timm is importable in the environment, still make the requirements.txt edits
    and record the install-gap + exact resolution decision in the SUMMARY
    (downstream offline tests use stub configs — RESEARCH Environment
    Availability fallback).
  </action>
  <verify>
    <automated>grep -v '^#' requirements.txt | grep -c timm</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^#' requirements.txt | grep -c timm` returns a count >= 1
    - requirements.txt transformers line is consistent with the resolved/importable major version and carries an inline comment recording the implied PaliGemma submodule path
    - `scripts/seg/__init__.py` exists and `python -c "import seg"` exits 0, OR the SUMMARY documents the environment install-gap with the exact resolution decision
  </acceptance_criteria>
  <done>timm declared; transformers pin resolved + documented; seg package importable.</done>
</task>

<task type="auto">
  <name>Task 2: Shared offline mini-pyramid + stub-config fixtures</name>
  <files>tests/conftest.py</files>
  <read_first>
    - tests/conftest.py (the file being modified; mirror tiny_geotiff lines 144-172 write-under-tmp_path/return-path idiom)
    - tests/test_tiling.py lines 40-47 (_make_map_dir) and 85-103 (test_pyramid_tile_count)
    - scripts/tiling.py lines 40-114, 170-196 (pyramid.json producer + sample_weights propagation)
    - 03-PATTERNS.md section Test files (synthetic builder analog)
  </read_first>
  <action>
    Add a `mini_pyramid` pytest fixture to tests/conftest.py that synthesizes a
    real Phase-2 pyramid tree under `tmp_path`: build a small per-map dir (RGB
    `image.png`, L-mode `land_cover.png` + `topography.png`, and a
    `sample_weights.json` blob — mirror `tests/test_tiling.py::_make_map_dir`
    exactly; do NOT re-implement the weights schema) sized large enough that
    `scripts.tiling.tile()` emits at least one full 21-tile pyramid (one 896
    root + 4x448 + 16x224), then call the real `tiling.tile(src, ...)` producer
    so `pyramid.json` is genuine (D-04: never hand-write geometry — let the
    Phase-2 producer emit it). Return the pyramid directory path (the dir
    containing `pyramid.json`), following the conftest `tiny_geotiff`
    return-path idiom. Keep it fully offline (Pillow + tiling only; no
    torch/transformers/timm import so it works even when those are absent). Also
    add a `stub_vision_config` fixture returning a minimal object exposing
    `hidden_size`, `patch_size`, `num_hidden_layers` for offline shape tests
    where the real PaliGemma checkpoint is unavailable (RESEARCH Assumption A1 /
    Environment Availability fallback).
  </action>
  <verify>
    <automated>python -m pytest tests/test_tiling.py -x -q -m "not integration"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/conftest.py` defines a `mini_pyramid` fixture and a `stub_vision_config` fixture (grep both names present)
    - The fixture calls the real `scripts.tiling.tile` producer (no hand-written pyramid.json)
    - `pytest tests/test_tiling.py -x -q -m "not integration"` stays green (no regression to shared conftest)
  </acceptance_criteria>
  <done>Offline mini-pyramid + stub-config fixtures available to all seg tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Seg test scaffolds (5 unit + 1 gated integration)</name>
  <files>tests/test_seg_backbones.py, tests/test_seg_decoder.py, tests/test_seg_recursive.py, tests/test_seg_dataset.py, tests/test_seg_smoke.py, tests/integration/test_seg_online.py, tests/integration/__init__.py</files>
  <read_first>
    - 03-VALIDATION.md section Per-Task Verification Map (exact behaviors + commands each test must cover)
    - 03-PATTERNS.md section Test files (analogs: test_tiling.py geometry asserts, test_split.py leakage guard, test_satellite_online.py gated idiom)
    - tests/test_tiling.py lines 1-12, 50-74, 105-135 (offline docstring + box/quadrant geometry asserts)
    - tests/test_split.py lines 97-119 (no-train/test-intersection leakage assertion)
    - tests/integration/test_satellite_online.py (gated @pytest.mark.integration + skip-with-reason + sys.path block)
  </read_first>
  <behavior>
    - test_seg_backbones.py: SigLIP backbone emits (B,C,h,w) at declared strides for 224/448/896; `test_no_gemma` asserts no `language_model` param-name and no `gemma` module-type reachable from EITHER variant; Variant-B widened patch-embed is 15-ch with zero-init extras and preserved RGB weights across all 3 backbones; DINOv2+Swin satisfy the same Backbone protocol (declared feature_strides/feature_channels).
    - test_seg_decoder.py: shared decoder + 2 heads emit (B,9,H,W)+(B,3,H,W) at full tile resolution from arbitrary declared-stride feature lists.
    - test_seg_recursive.py: a known one-hot blob placed in a parent quadrant lands in the correct child after pyramid.json-box crop+resize (extends test_tiling test_nested_alignment); 896 cold-start prior is all-zeros; Variant-A 12-ch prior-encoder injects at decoder working resolution.
    - test_seg_dataset.py: dataset surfaces sample_weights unchanged + correct batch shapes; asserts no enumerated path contains a `test/` component (mirror test_tiling.py:213-215).
    - test_seg_smoke.py: both Variant A and Variant B run an end-to-end forward on the `mini_pyramid` fixture — correct (B,9,H,W)+(B,3,H,W) shapes, no NaN, torch.no_grad only.
    - test_seg_online.py: @pytest.mark.integration; real Phase-1 adapter (`--adapter-dir`/`--model-id`) + real Phase-2-tile forward; skip-with-reason if adapter or Phase-2 data absent (OQ1 RESOLVED).
  </behavior>
  <action>
    Create all six test files plus `tests/integration/__init__.py` if missing.
    Each file opens with the project module-docstring convention (offline /
    tmp_path / decision-IDs covered — mirror tests/test_tiling.py lines 1-12;
    integration file mirrors test_satellite_online.py including its sys.path
    block). Write the test bodies as real assertions against the
    not-yet-existing `seg.*` API (contracts fixed by RESEARCH Pattern 5 / D-02 /
    D-03a — define them here so downstream tasks implement against the test,
    TDD-style). Where a target module does not yet exist, the test must fail by
    ImportError or a clear assertion (NOT silently skip) so it is a live Nyquist
    gate — EXCEPT the integration test, which must `pytest.skip(reason=...)`
    when the Phase-1 adapter / Phase-2 data are absent. Use the `mini_pyramid`
    and `stub_vision_config` conftest fixtures for offline runs. Cover BOTH
    D-03a variants with independent assertions (Variant A: frozen backbones +
    decoder-level 12-ch prior; Variant B: 15-ch widened trainable patch-embed,
    zero-init extras, RGB weights preserved, all 3 backbones). Do NOT write any
    training loop, optimizer, `.backward()`, or overfit assertion — D-06:
    construction-only; smoke is forward-only under `torch.no_grad` (RESEARCH
    Anti-Patterns).
  </action>
  <verify>
    <automated>python -m pytest tests/test_seg_backbones.py tests/test_seg_decoder.py tests/test_seg_recursive.py tests/test_seg_dataset.py tests/test_seg_smoke.py tests/integration/test_seg_online.py --co -q</automated>
  </verify>
  <acceptance_criteria>
    - All 6 test files import/collect cleanly (`pytest --co -q` exits 0 — collection error = scaffold not importable)
    - Each unit file contains real `from seg.X import ...` references and live assertions (not bare `pytest.skip`/`xfail` stubs), covering its 03-VALIDATION.md row
    - `tests/test_seg_backbones.py` contains an independent assertion for Variant A AND Variant B (Gemma-absence + Variant-B 15-ch widened patch-embed)
    - `tests/integration/test_seg_online.py` is `@pytest.mark.integration` and skips-with-reason when artifacts absent
  </acceptance_criteria>
  <done>All seg behaviors have an importable, asserting Nyquist gate; integration test gated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package index → build env | `timm` / transformers pins resolved from PyPI; supply-chain trust on declared versions |
| local filesystem → test process | conftest fixtures read/write only under pytest `tmp_path` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | requirements.txt dependency pins | mitigate | Pin `timm>=1.0.27` to a PyPI-verified version (RESEARCH §Standard Stack, verified 2026-05-15); no unpinned floating add |
| T-03-02 | Tampering | conftest fixture filesystem writes | mitigate | All writes confined to pytest `tmp_path` (mirror `tiny_geotiff` idiom); no repo-tree mutation |
| T-03-03 | Information Disclosure | gated integration test paths | accept | Test only reads user-supplied `--adapter-dir`; skips when absent; no secret material in repo (low-value, local-only) |
</threat_model>

<verification>
- `pytest -q -m "not integration"` full offline suite green (Phase-2 tests included — no conftest regression)
- `pytest tests/test_seg_*.py --co -q` collects all 5 unit scaffolds without import error
- `grep -v '^#' requirements.txt | grep -c timm` >= 1
</verification>

<success_criteria>
- `timm` declared; transformers pin resolved with documented PaliGemma submodule path
- `scripts/seg/` is an importable package
- `mini_pyramid` + `stub_vision_config` fixtures usable by every seg test, fully offline
- 6 seg test files exist; 5 unit collect+fail as live Nyquist gates; integration gated/skips-with-reason
- No training/optimizer/backward code introduced (D-06 construction-only respected)
</success_criteria>

<output>
After completion, create `.planning/phases/03-build-a-dense-semantic-segmentation-pipeline/03-01-SUMMARY.md`
</output>
