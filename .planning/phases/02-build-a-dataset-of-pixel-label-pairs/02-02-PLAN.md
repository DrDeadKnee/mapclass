---
phase: 02-build-a-dataset-of-pixel-label-pairs
plan: 02
type: execute
wave: 1
depends_on: ["02-01"]
files_modified:
  - scripts/tiling.py
  - scripts/build_dataset.py
  - tests/test_tiling.py
  - tests/test_build_dataset.py
  - .planning/PROJECT.md
autonomous: true
requirements: [PHASE-02, EVAL-01]
nyquist_compliant: true

must_haves:
  truths:
    - "tiling.tile() writes a complete nested-pyramid tree to a _GCSWriter target"
    - "Per-pyramid tile writes run under ThreadPoolExecutor(max_workers=32)"
    - "build_dataset.py reads raw geojson + manifest.json from GCS and HARD-FAILS on manifest mismatch BEFORE load_or_create_split"
    - "split.json is written to and read from gs://mapclass-training-northeast1/data/synthetic/split.json and is frozen on re-run"
    - "stratified_split groups by manifest template, not template_key()"
    - "A --refreeze-split flag deletes the GCS split.json and recomputes"
    - "build_dataset.py writes pyramid output to gs://.../data/synthetic/{train,test}/ — no canonical local copy; --local-ok required to override"
    - "PROJECT.md records D-06/D-17/D-18 as reversed and re-points split.json to GCS"
  artifacts:
    - path: "scripts/tiling.py"
      provides: "_GCSWriter-aware tile() with 32-thread write pool"
      contains: "ThreadPoolExecutor"
    - path: "scripts/build_dataset.py"
      provides: "GCS raw read, manifest-before-split hard-fail, GCS split.json, --refreeze-split, --local-ok"
      contains: "validate_manifest"
    - path: ".planning/PROJECT.md"
      provides: "reversed D-06/D-17/D-18 wording + GCS split.json location"
      contains: "REVERSED"
  key_links:
    - from: "scripts/build_dataset.py"
      to: "scripts/gcs_io.py"
      via: "import _GCSWriter, validate_manifest"
      pattern: "from gcs_io import"
    - from: "scripts/build_dataset.py"
      to: "validate_manifest"
      via: "call ordered strictly before load_or_create_split"
      pattern: "validate_manifest.*load_or_create_split"
    - from: "scripts/tiling.py"
      to: "scripts/gcs_io.py"
      via: "_GCSWriter as out_root"
      pattern: "_GCSWriter"
---

<objective>
Migrate the synthetic pipeline to GCS-canonical persistence: refactor
`tiling.py` to write pyramids through the `_GCSWriter` shim with a 32-thread
write pool (RW-01), and wire `build_dataset.py` to read raw GeoJSON +
`manifest.json` from GCS, hard-fail on manifest mismatch BEFORE the split
freezes (RW-02), and persist `split.json` + pyramids to GCS (D-17/D-18
reversed). Finally update PROJECT.md to record the reversed decisions.

Purpose: This is the core of the rework — it closes the root-cause defect
(local-only writes destroyed by ephemeral compute) for the synthetic family
and makes the EVAL-01 hold-out tamper-resistant.
Output: GCS-writing tiling.py + build_dataset.py, manifest-before-split
ordering, frozen GCS split.json, updated PROJECT.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-build-a-dataset-of-pixel-label-pairs/02-CONTEXT.md
@.planning/phases/02-build-a-dataset-of-pixel-label-pairs/02-RESEARCH.md
@.planning/phases/02-build-a-dataset-of-pixel-label-pairs/02-PATTERNS.md
@.planning/phases/02-build-a-dataset-of-pixel-label-pairs/02-01-SUMMARY.md

<interfaces>
<!-- Contracts delivered by Wave 0 (02-01) — import, do not reinvent. -->

From scripts/gcs_io.py (02-01):
- class _GCSWriter(fs, prefix): __truediv__, mkdir, name, write_bytes, write_text, open(mode)
- validate_manifest(gcs_filenames: list[str], manifest: dict) -> dict[str,str]  (sys.exit(1) on mismatch)
- GCS_PROJECT = "narrative-campaign"; BUCKET = "mapclass-training-northeast1"
- DATA_PREFIX = "mapclass-training-northeast1/data"

tiling.py touch points (verified against current source):
- L146: out = Path(out_root) if out_root is not None else map_dir / "pyramids"
- L151 / L159: out.mkdir(parents=True, exist_ok=True)
- L160: weights_blob = (map_dir / _WEIGHTS_FILE).read_bytes()
- L165-166: pdir = out / pid ; pdir.mkdir(...)
- L172-174: img.crop(box).save(pdir / t["image"]) (+ land_cover, topography)
- L185: (pdir / "pyramid.json").write_text(json.dumps(manifest, indent=2))
- L189-195: shutil.copyfile(map_dir/_WEIGHTS_FILE, pdir/_WEIGHTS_FILE) + read_bytes integrity check

build_dataset.py touch points (verified):
- L72-74: _SPLIT_SEED=42, _TEST_FRACTION=0.15, _SPLIT_FILENAME="split.json" (UNCHANGED — D-15/D-16)
- L80 _sanitize_stem, L85 template_key, L104 stratified_split, L131 load_or_create_split (L138 split_path=Path(out_dir)/_SPLIT_FILENAME)
- L207: tiling.tile(out)
- L219: geojsons = sorted(raw_dir.glob("*.geojson"))
- L238-248: collision guard sys.exit(1)
- L277-285: --raw-dir / --out-dir argparse defaults
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor tiling.py to write via _GCSWriter with 32-thread pool</name>
  <read_first>
    - scripts/tiling.py (read the full file — current state, all 6 touch points L146-195)
    - scripts/gcs_io.py (02-01 _GCSWriter contract — the analog this task consumes)
    - .planning/phases/02-build-a-dataset-of-pixel-label-pairs/02-PATTERNS.md (tiling.py MODIFY section lines 87-141; ThreadPoolExecutor pattern lines 424-433)
    - .planning/phases/02-build-a-dataset-of-pixel-label-pairs/02-RESEARCH.md (RW-01 touch-point table; RW-01b cost/throughput lines ~262-296)
    - scripts/build_historical_dataset.py (lines 138-144 — the canonical ThreadPoolExecutor+as_completed re-raise idiom to mirror)
    - tests/test_tiling.py (current state — extend, keep existing geometry tests passing)
  </read_first>
  <behavior>
    - test_tile_to_gcs_writer: tile(fixture_map_dir, out_root=_GCSWriter(local_fs, tmp/"train")) produces pyramid dirs each with image/land_cover/topography PNGs + pyramid.json + sample_weights.json; structure identical to local-Path output
    - test_tile_local_path_unchanged: tile(fixture, out_root=None) still produces local pyramids (no regression)
    - existing tiler-geometry tests in tests/test_tiling.py remain green
  </behavior>
  <action>
    Modify `scripts/tiling.py`. At L146: branch — if `out_root` is a
    `_GCSWriter` instance use it directly; if a `str` starting with `gs://`
    construct `_GCSWriter(fs, bare_path_without_scheme)` (instantiate
    `fs = gcsfs.GCSFileSystem(project=GCS_PROJECT)` once, lazy-import gcsfs same
    as gcs_checkpoint.py); if `None` keep `map_dir / "pyramids"` local Path.
    Import `_GCSWriter` from `gcs_io`. The `.mkdir()`, `__truediv__`, and
    `.write_text()` call sites (L151/159/165-166/185) work unchanged because
    `_GCSWriter` implements those methods (no-op mkdir for GCS).

    Touch point 4 (L172-174): replace each `img.crop(box).save(pdir /
    t["image"])` with buffer-then-write: `buf = io.BytesIO();
    crop.save(buf, format="PNG"); (pdir / name).write_bytes(buf.getvalue())`.
    Touch point 6 (L189-195): replace `shutil.copyfile` with `(pdir /
    _WEIGHTS_FILE).write_bytes(weights_blob)` (weights_blob already loaded at
    L160). Keep the `read_bytes()` integrity check ONLY when `pdir` resolves to
    a local `Path` (pipe_file is atomic — no partial-write risk on GCS).

    Wrap the per-pyramid tile-write loop (current sequential loop ~L163-197) in
    `ThreadPoolExecutor(max_workers=32)`, building a task list of (writer, name,
    payload) then submitting writes and draining via `as_completed` with
    `fut.result()` to re-raise — mirror build_historical_dataset.py L138-144.
    Create one `_GCSWriter` instance per pyramid (NOT shared across threads —
    the shim is not thread-safe; the underlying gcsfs.GCSFileSystem is). Expose
    `max_workers: int = 32` as a `tile()` keyword arg.
  </action>
  <verify>
    <automated>cd /home/drdreadknee/mapclass && pytest tests/test_tiling.py -x</automated>
  </verify>
  <acceptance_criteria>
    - scripts/tiling.py imports _GCSWriter from gcs_io
    - tiling.py body contains "ThreadPoolExecutor(max_workers=32)" (RW-01b mitigation)
    - tile() branches on isinstance(out_root, _GCSWriter) at the L146 site
    - shutil.copyfile no longer used for the GCS weights write path (replaced by write_bytes)
    - `pytest tests/test_tiling.py::test_tile_to_gcs_writer -x` exits 0
    - `pytest tests/test_tiling.py -x` exits 0 (all existing geometry tests still green)
  </acceptance_criteria>
  <done>
    tiling.tile() writes complete pyramids through _GCSWriter under a 32-thread
    pool; local-Path path preserved; tiler tests green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: build_dataset.py GCS raw/split + manifest-before-split + PROJECT.md update</name>
  <read_first>
    - scripts/build_dataset.py (full file — load_or_create_split L131-165, build L214+, collision guard L238-248, argparse L277-285)
    - scripts/gcs_io.py (validate_manifest + _GCSWriter contracts from 02-01)
    - scripts/seg/gcs_checkpoint.py (fs.exists / fs.cat / fs.pipe_file / fs.ls idioms lines 131-179)
    - .planning/phases/02-build-a-dataset-of-pixel-label-pairs/02-PATTERNS.md (build_dataset.py MODIFY section lines 145-203)
    - .planning/phases/02-build-a-dataset-of-pixel-label-pairs/02-RESEARCH.md (RW-02 validation order lines ~328-372; Pitfall R-1/R-3 lines ~731-768)
    - .planning/PROJECT.md (the <decisions> block + Key Decisions table — locate D-06/D-17/D-18 wording to reverse; if explicit D-06/D-17/D-18 IDs absent, edit the DECISION-* entries the CONTEXT.md maps them to and add a rework note)
    - tests/test_build_dataset.py (current state if any — extend/create)
  </read_first>
  <behavior>
    - test_manifest_fails_before_split: a mis-named raw file -> SystemExit raised, and load_or_create_split was NOT reached (split.json never written to mock GCS)
    - test_split_uses_manifest_template: stratified_split grouping key comes from manifest entries, not template_key()
    - test_split_frozen_in_gcs: first build writes split.json to mock GCS; second build with extra sources reads the frozen split (does not recompute)
    - test_refreeze_split_recomputes: --refreeze-split deletes GCS split.json then recomputes
    - test_local_ok_required: out-dir without gs:// scheme and no --local-ok -> hard error
  </behavior>
  <action>
    Modify `scripts/build_dataset.py`. Keep `_SPLIT_SEED=42`,
    `_TEST_FRACTION=0.15`, `_sanitize_stem`, `template_key`, `stratified_split`
    core algorithm and the collision guard UNCHANGED (D-15/D-16 preserved).

    Replace the raw listing at L219 with GCS: `fs =
    gcsfs.GCSFileSystem(project=GCS_PROJECT)`, `raw_gcs =
    f"{DATA_PREFIX}/synthetic/raw"`, list objects via `fs.ls(raw_gcs)`, keep
    basenames ending `.geojson`. `fs.cat(f"{raw_gcs}/manifest.json")` -> parse.
    Insert `id_to_template = validate_manifest(gcs_filenames, manifest)` in the
    slot AFTER the collision guard (L238-248) and STRICTLY BEFORE
    `load_or_create_split` (L250 call site). This ordering is a correctness
    invariant — a mis-named file must never reach the split.

    Rework `load_or_create_split` to GCS: `split_gcs =
    f"{DATA_PREFIX}/synthetic/split.json"`; if `fs.exists(split_gcs)` ->
    `json.loads(fs.cat(split_gcs).decode())` and return frozen `set(data["test"])`;
    else compute and `fs.pipe_file(split_gcs, json.dumps({...}).encode())`.
    Modify `stratified_split` to accept `id_to_template: dict[str,str]` and
    group by `id_to_template.get(sid, template_key(sid))` (manifest
    authoritative; template_key only an unreached fallback). Preserve the
    WR-08 stratification-group print before freezing.

    At the L207 tiling call, pass `out_root=_GCSWriter(fs,
    f"{DATA_PREFIX}/synthetic/{split_name}/{src_id}__{style}/pyramids")`;
    `out` stays the local scratch Path for reads. Change argparse: `--raw-dir`
    default `"gs://mapclass-training-northeast1/data/synthetic/raw"`,
    `--out-dir` default `"gs://mapclass-training-northeast1/data/synthetic"`;
    add `--local-ok` (without it, a non-`gs://` out-dir hard-errors — Pitfall
    R-1 guard) and `--refreeze-split` (deletes the GCS split.json via
    `fs.rm` then forces recompute — Pitfall R-3 escape hatch).

    Update `.planning/PROJECT.md`: in the decisions block / Key Decisions
    table, append a rework note marking D-06/D-17/D-18 (mapped via 02-CONTEXT.md
    to the DECISION-* entries) as REVERSED — tiles stream to GCS not
    canonical-on-disk (D-06), `gs://.../data/synthetic/{train,test}/` +
    split.json canonical with filesystem separation now transient scratch
    (D-17), split.json relocated to `gs://.../data/synthetic/split.json` with
    the prior frozen split GONE and a NEW split freezing on regenerated Azgaar
    sources (D-18). Add the dated rework line referencing 02-CONTEXT.md.
  </action>
  <verify>
    <automated>cd /home/drdreadknee/mapclass && pytest tests/test_build_dataset.py -x && grep -n "REVERSED" .planning/PROJECT.md</automated>
  </verify>
  <acceptance_criteria>
    - In build_dataset.py source, the line number of the validate_manifest call is LESS THAN the line number of the load_or_create_split call (manifest-before-split invariant)
    - build_dataset.py imports validate_manifest and _GCSWriter from gcs_io
    - load_or_create_split reads/writes split.json via fs.exists/fs.cat/fs.pipe_file at "mapclass-training-northeast1/data/synthetic/split.json"
    - stratified_split grouping uses id_to_template (manifest), with template_key only as .get() fallback
    - argparse exposes --refreeze-split and --local-ok; default --out-dir starts with "gs://"
    - .planning/PROJECT.md contains "REVERSED" and references D-06/D-17/D-18 reversal + GCS split.json location
    - `pytest tests/test_build_dataset.py::test_manifest_fails_before_split -x` exits 0
    - `pytest tests/test_build_dataset.py::test_split_uses_manifest_template -x` exits 0
    - `pytest tests/test_build_dataset.py::test_split_frozen_in_gcs -x` exits 0
    - `pytest tests/test_build_dataset.py -x` exits 0
  </acceptance_criteria>
  <done>
    Synthetic build reads GCS raw + manifest, hard-fails before split, freezes
    GCS split.json, streams pyramids to GCS; PROJECT.md records the reversals.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-authored raw/manifest.json + filenames → build_dataset.py | untrusted input that drives stratification |
| build_dataset.py → GCS bucket | ADC write of pyramids + split.json |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-10 | Tampering | mis-named raw file skews EVAL-01 hold-out | mitigate | validate_manifest hard-fail ordered strictly before load_or_create_split; asserted by test_manifest_fails_before_split |
| T-02-11 | Tampering | partial/aborted GCS upload mid-pyramid | mitigate | pipe_file atomic per object; collision guard + missing-file SKIP guard precede tile(); verify_pull (02-05) spot-checks completeness |
| T-02-12 | Information disclosure | silent local-only write recreating the root-cause defect | mitigate | --out-dir defaults to gs://; non-gs:// requires explicit --local-ok (Pitfall R-1) |
| T-02-13 | Tampering | split frozen on incomplete raw set | mitigate | manifest must list ALL raw files (hard-fail); --refreeze-split is the only deliberate recompute path (Pitfall R-3) |
| T-02-SC | Tampering | pip installs | accept | No new packages — gcsfs/fsspec already audited [OK]; no install task |
</threat_model>

<verification>
- `pytest tests/test_tiling.py tests/test_build_dataset.py -x` green
- `grep -n "ThreadPoolExecutor(max_workers=32)" scripts/tiling.py` non-empty
- validate_manifest call precedes load_or_create_split call in build_dataset.py
- `grep -n "REVERSED" .planning/PROJECT.md` non-empty
</verification>

<success_criteria>
- tiling.tile() writes complete pyramids to _GCSWriter under 32-thread pool
- build_dataset.py reads GCS raw + manifest, hard-fails before split, freezes GCS split.json, streams pyramids to GCS
- --refreeze-split + --local-ok flags present
- PROJECT.md records D-06/D-17/D-18 reversal + GCS split.json relocation
</success_criteria>

<output>
Create `.planning/phases/02-build-a-dataset-of-pixel-label-pairs/02-02-SUMMARY.md` when done
</output>
