---
phase: 04-fine-tune-and-evaluate-the-segmentation-model-then-upload-to
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/seg/train_utils.py
  - tests/test_seg_training.py
  - tests/test_seg_eval.py
  - tests/test_seg_gcs.py
autonomous: true
requirements: [EVAL-02, EVAL-03]
must_haves:
  truths:
    - "Weighted joint loss is a scalar that decreases when overfitting a single pyramid"
    - "The teacher-forced coarse prior is detached (no gradient flows through both c2f passes)"
    - "The train→val carve is constructed from train/ roots only and never indexes test/"
    - "All three offline test files exist and fail loudly (not silently skip) until their target modules ship"
  artifacts:
    - path: "scripts/seg/train_utils.py"
      provides: "weighted_joint_loss, build_lc_weight_tensor, train_step_variant_a, train_step_variant_b, make_optimizer, carve_train_val"
      min_lines: 120
    - path: "tests/test_seg_training.py"
      provides: "TestWeightedJointLoss, TestTeacherForcedDetach, TestValCarve"
      contains: "def test_"
    - path: "tests/test_seg_eval.py"
      provides: "Scaffold for NLL formula + leakage guard (filled in plan 03)"
      contains: "_require_eval"
    - path: "tests/test_seg_gcs.py"
      provides: "Scaffold for GCS round-trip (filled in plan 02)"
      contains: "_require_gcs"
  key_links:
    - from: "scripts/seg/train_utils.py"
      to: "scripts/biome_mapping.py"
      via: "from biome_mapping import LANDCOVER_CLASSES"
      pattern: "LANDCOVER_CLASSES"
    - from: "scripts/seg/train_utils.py"
      to: "scripts/seg/recursive.py"
      via: "crop_prior_to_child / cold_start_prior reuse"
      pattern: "crop_prior_to_child|cold_start_prior"
    - from: "tests/test_seg_training.py"
      to: "scripts/seg/train_utils.py"
      via: "import guard"
      pattern: "from seg.train_utils import"
---

<objective>
Wave 0 for Phase 4. Create the three offline test files (training / eval / gcs) as
loud-failing scaffolds (Nyquist gate for the whole phase), and implement the
CPU-testable training building blocks: per-sample weighted joint loss, the
teacher-forced coarse-to-fine training step (Variant A and Variant B), the
AdamW param-group builder, and the seeded train→val carve.

Purpose: Every downstream Phase-4 task (training loop, eval harness, GCS I/O)
maps to an offline test that already exists. The loss + train-step + val-carve
are pure tensor logic and fully verifiable on CPU with the existing
`mini_pyramid` / `stub_vision_config` fixtures — no GPU, no GCS.
Output: `scripts/seg/train_utils.py` + three test files; `test_seg_training.py`
fully exercised offline; `test_seg_eval.py` / `test_seg_gcs.py` scaffolded
(filled in plans 03 / 02).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-fine-tune-and-evaluate-the-segmentation-model-then-upload-to/04-CONTEXT.md
@.planning/phases/04-fine-tune-and-evaluate-the-segmentation-model-then-upload-to/04-RESEARCH.md
@.planning/phases/04-fine-tune-and-evaluate-the-segmentation-model-then-upload-to/04-PATTERNS.md
@.planning/phases/04-fine-tune-and-evaluate-the-segmentation-model-then-upload-to/04-VALIDATION.md

<read_first>
- scripts/seg/model.py — SegModelVariantA.forward(rgb, prior) and SegModelVariantB.forward(x15) signatures; backbone.requires_grad_(False) set at construction; prior_encoder/decoder/heads default requires_grad=True
- scripts/seg/recursive.py lines 74-220 — cold_start_prior(batch, tile_size), crop_prior_to_child(parent_prob, parent_tile, child_tile), the depth-first _predict walk train_step must mirror with grads enabled
- scripts/seg/dataset.py lines 61-183 — PyramidDataset(pyramid_root) constructor (accepts a single dir, a parent dir, or an iterable of pyramid dirs), EVAL-01 test/ guard at lines 125-134, __getitem__ returns {image,(3,H,W) f32; land_cover,(H,W) i64; topography,(H,W) i64; sample_weights, dict}
- scripts/seg/heads.py — LandCoverHead 9-class, TopographyHead 3-class, raw logits (no softmax)
- scripts/biome_mapping.py — LANDCOVER_CLASSES (9 names, ordering), TOPO_CLASSES (3)
- tests/conftest.py lines 306-362 — mini_pyramid fixture (real 21-tile 896→448→224 pyramid + sample_weights.json under tmp_path) and stub_vision_config fixture (hidden_size=1152, patch_size=14)
- tests/test_seg_smoke.py — import-guard idiom (_import_error / _require_*), class-per-concern layout, mini_pyramid + stub_vision_config usage
- tests/test_seg_recursive.py — parametrize-over-variant idiom; synthetic-logits assertion style
- tests/test_seg_dataset.py lines 36-48, 140-194 — import-guard pattern and EVAL-01 leakage-guard test pattern
</read_first>

<interfaces>
From scripts/seg/recursive.py (Phase-3, unchanged — train_utils REUSES these):
  cold_start_prior(batch: int, tile_size: int) -> Tensor          # (batch,12,S,S) zeros
  crop_prior_to_child(parent_prob: Tensor, parent_tile: dict, child_tile: dict) -> Tensor
  # recursive_predict(model, pyramid_dir, read_tile_fn=None) -> {tile_id: (1,12,S,S)}  (eval-time; not used here)

From scripts/seg/model.py (Phase-3, unchanged):
  SegModelVariantA(backbone_name=..., stub_config=...).forward(rgb, prior) -> (lc_logits (B,9,H,W), topo_logits (B,3,H,W))
  SegModelVariantB(backbone_name=..., stub_config=...).forward(x15) -> (lc_logits, topo_logits)   # x15 = cat([rgb, prior12], dim=1)

From scripts/seg/dataset.py (Phase-3, unchanged):
  PyramidDataset(pyramid_root)  # raises ValueError if any path contains 'test/'

sample_weights.json schema (locked, Phase-2):
  {"land_cover_weights": {<9 LANDCOVER_CLASSES names>: float}, "topography_weight": float, "source": str, "map_file": str}

New contracts THIS plan defines (downstream plans import these):
  scripts/seg/train_utils.py:
    build_lc_weight_tensor(sw_dict: dict, device) -> Tensor                       # (9,) ordered by LANDCOVER_CLASSES
    weighted_joint_loss(lc_logits, topo_logits, lc_targets, topo_targets, sample_weights_batch: list[dict], device) -> Tensor  # scalar
    train_step_variant_a(model, pyramid, optimizer, device) -> float             # one pyramid, teacher-forced, returns loss float
    train_step_variant_b(model, pyramid, optimizer, device) -> float
    make_optimizer(model, lr=1e-4, weight_decay=1e-2) -> torch.optim.AdamW       # 2 param groups, requires_grad-filtered
    carve_train_val(train_root_dirs, val_frac=0.2, seed=42) -> (train_ds, val_ds)  # random_split over PyramidDataset(train/ only)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the three offline Phase-4 test files</name>
  <files>tests/test_seg_training.py, tests/test_seg_eval.py, tests/test_seg_gcs.py</files>
  <read_first>tests/test_seg_smoke.py, tests/test_seg_recursive.py, tests/test_seg_dataset.py (lines 36-48, 140-194), tests/conftest.py (lines 306-362)</read_first>
  <action>
Create all three test files using the import-guard idiom from `test_seg_smoke.py`
lines 28-40 (module-level `_import_error` try/except; `_require_<x>()` calls
`pytest.fail(..., pytrace=False)` — fail LOUDLY, never `pytest.skip`, so the
Nyquist gate is honoured offline).

`tests/test_seg_training.py`: guard imports `from seg.train_utils import
weighted_joint_loss, build_lc_weight_tensor, train_step_variant_a,
train_step_variant_b, make_optimizer, carve_train_val`. Define three classes
(empty bodies for now beyond the scaffold tests in Task 3): `TestWeightedJointLoss`,
`TestTeacherForcedDetach`, `TestValCarve`. Task 3 fills these.

`tests/test_seg_eval.py`: guard imports `from evaluate_seg import
evaluate_joint_nll, load_test_pyramid_dirs` (module ships in plan 03). Add a
single placeholder `TestNLLFormula` class with one test that calls
`_require_eval()` then `pytest.skip("filled in 04-03")` AFTER the require — so
it fails loud only once evaluate_seg exists but is incomplete. Keep this minimal;
plan 03 replaces the body.

`tests/test_seg_gcs.py`: guard imports `from seg.gcs_checkpoint import
gcs_save_checkpoint, gcs_latest_checkpoint, validate_config_name` (module ships
in plan 02). Add placeholder `TestGCSCheckpointRoundTrip` mirroring the
test_seg_eval scaffold approach. Plan 02 replaces the body.

Mark NOTHING `gpu`/`integration` here — all three files are offline/CPU. Use the
existing `pytest.ini` markers; do not add new markers.
  </action>
  <verify>
    <automated>python -m pytest -q tests/test_seg_training.py tests/test_seg_eval.py tests/test_seg_gcs.py --collect-only 2>&1 | grep -E "test session|error" | head -5</automated>
  </verify>
  <acceptance_criteria>
- All three files import-collect without ImportError at module scope (guard absorbs the missing target modules)
- `_require_training` / `_require_eval` / `_require_gcs` each call `pytest.fail(..., pytrace=False)` — NOT `pytest.skip`
- `pytest --collect-only` lists `TestWeightedJointLoss`, `TestTeacherForcedDetach`, `TestValCarve` (and the two placeholders)
  </acceptance_criteria>
  <done>Three test files exist; collection succeeds; failing-loud guards in place.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement train_utils — weighted joint loss, optimizer, val carve</name>
  <files>scripts/seg/train_utils.py</files>
  <read_first>scripts/seg/recursive.py (cold_start_prior, crop_prior_to_child), scripts/seg/dataset.py (PyramidDataset ctor + EVAL-01 guard lines 125-134), scripts/biome_mapping.py (LANDCOVER_CLASSES order), scripts/finetune_paligemma.py lines 43-45 (sys.path bootstrap) and 251-257 (trainable-only optimizer)</read_first>
  <behavior>
- build_lc_weight_tensor: given a sample_weights dict, returns a (9,) float32 tensor ordered exactly by LANDCOVER_CLASSES; raises a descriptive ValueError if any of the 9 class keys is missing (RESEARCH §Security: validate keys before building tensor)
- weighted_joint_loss: returns a scalar tensor; for uniform logits and all-1.0 weights it equals mean(CE_lc) + mean(CE_topo); per-class weight tensor passed to F.cross_entropy(weight=...); topography_weight applied as scalar multiplier after the topo CE; averaged over batch B
- make_optimizer: returns AdamW with exactly two param groups (decay / no-decay where no_decay matches {"bias","norm","LayerNorm"}); only params with requires_grad=True are included; a frozen backbone param never appears in any group
- carve_train_val: builds PyramidDataset from train/ roots only and random_split's it 80/20 with a fixed seed; calling it with a path containing test/ propagates PyramidDataset's ValueError (EVAL-01)
  </behavior>
  <action>
Create `scripts/seg/train_utils.py` with the sys.path bootstrap idiom
(`_HERE = Path(__file__).parent` — note this file lives in scripts/seg/, so add
scripts/ to sys.path for the `biome_mapping` import; mirror how other seg modules
resolve it).

Implement `build_lc_weight_tensor`, `weighted_joint_loss`,
`train_step_variant_a`, `train_step_variant_b`, `make_optimizer`,
`carve_train_val` exactly per the `<interfaces>` signatures and 04-PATTERNS.md
"Weighted joint loss pattern" (lines 169-197), "AdamW param-group pattern"
(lines 199-212), and "Val-carve pattern" (lines 94-104).

`weighted_joint_loss`: `F.cross_entropy(lc_logits[i:i+1], lc_targets[i:i+1],
weight=lc_class_w, reduction='mean')` then `+ F.cross_entropy(topo...,
reduction='mean') * topo_w`; sum over batch; divide by B. Validate
`sw["land_cover_weights"]` has all 9 LANDCOVER_CLASSES keys and
`sw["topography_weight"]` is present before tensor construction (RESEARCH
§Known Threat Patterns — `sample_weights.json` missing keys → descriptive
ValueError, not KeyError).

`make_optimizer`: filter `named_parameters()` by `requires_grad`; two groups,
weight_decay=0.0 for the no_decay set. Implement train-step functions in Task 3
(stub them here with `raise NotImplementedError` bodies and the correct
signatures so the module imports cleanly — Task 3 fills them).

`carve_train_val`: `ds = PyramidDataset(train_root_dirs)`; `gen =
torch.Generator().manual_seed(seed)`; `random_split(ds, [n_train, n_val],
generator=gen)`. Do NOT add any test/ enumeration path. Add a one-line module
docstring noting EVAL-01: this module never touches test/.
  </action>
  <verify>
    <automated>python -m pytest -q tests/test_seg_training.py -x -k "WeightedJointLoss or ValCarve" 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
- `from seg.train_utils import weighted_joint_loss, build_lc_weight_tensor, make_optimizer, carve_train_val` succeeds (test_seg_training guard passes)
- weighted_joint_loss on uniform logits + all-1.0 weights ≈ -log(1/9) topo handled per formula (assert in Task 3 tests)
- build_lc_weight_tensor raises ValueError (not KeyError) when a class key is missing
- make_optimizer excludes any requires_grad=False param from all groups
- carve_train_val raises ValueError if handed a test/ path (delegates to PyramidDataset guard)
  </acceptance_criteria>
  <done>train_utils.py importable; loss/optimizer/carve verified offline; train-step stubs present with correct signatures.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Teacher-forced c2f train-step + fill test_seg_training</name>
  <files>scripts/seg/train_utils.py, tests/test_seg_training.py</files>
  <read_first>scripts/seg/recursive.py lines 197-220 (the _predict depth-first walk to mirror), scripts/seg/model.py (VariantA forward(rgb,prior) / VariantB forward(x15)), tests/conftest.py lines 306-362 (mini_pyramid + stub_vision_config), tests/test_seg_smoke.py lines 97-171 (overfit + grad-assertion idiom)</read_first>
  <behavior>
- train_step_variant_a: walks the pyramid 896→448→224 depth-first mirroring recursive.py _predict; coarse logits are softmaxed AFTER .detach() before being cached as the child prior (crop_prior_to_child receives a detached tensor); current-tile lc_logits/topo_logits keep grad and feed weighted_joint_loss; one backward + optimizer.step + zero_grad per pyramid; returns the float loss
- train_step_variant_b: identical walk but builds x15 = cat([rgb, prior12], dim=1) and calls model(x15)
- TestWeightedJointLoss::test_loss_scalar: loss is a 0-dim tensor
- TestWeightedJointLoss::test_loss_decreases_on_overfit: ≥5 train_step calls on one mini_pyramid → final loss < initial loss
- TestTeacherForcedDetach::test_prior_detached: the cached prob for the 896 root has requires_grad=False after a step (detach proven)
- TestValCarve::test_val_contains_no_test_paths: carve_train_val over a train/ fixture yields val_ds whose every sample pdir lacks a 'test' path component
  </behavior>
  <action>
Replace the `train_step_variant_a` / `train_step_variant_b`
`NotImplementedError` stubs in `scripts/seg/train_utils.py` with the
teacher-forced walk from 04-PATTERNS.md lines 139-167 / 04-RESEARCH.md Pattern 2
(lines 299-374). Critical (RESEARCH Pitfall 2): cache
`prob_cache[tile_id] = torch.cat([softmax(lc_logits.detach()),
softmax(topo_logits.detach())], dim=1)`; pass
`prob_cache[parent_id]` (already detached) into `crop_prior_to_child`. The
per-tile loss uses the NON-detached `lc_logits`/`topo_logits` so grads flow for
the current tile only. Accumulate per-tile losses across the 21 tiles, one
`.backward()` / `optimizer.step()` / `optimizer.zero_grad()` per pyramid.

Then fill `tests/test_seg_training.py`: implement `TestWeightedJointLoss`
(scalar + uniform-logit value check + overfit-decrease using
`SegModelVariantA(backbone_name="siglip", stub_config=stub_vision_config)` and
the `mini_pyramid` fixture), `TestTeacherForcedDetach` (assert
`prob_cache`/cached-prior `requires_grad is False` — reuse the smoke-test
grad-assert idiom), `TestValCarve` (build a tiny train/-only fixture from
`mini_pyramid`, assert no `'test'` in any val sample pdir parts). All offline,
CPU, stub backbone — no GPU, no network, runtime < 60s.
  </action>
  <verify>
    <automated>python -m pytest -q tests/test_seg_training.py -x 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
- `tests/test_seg_training.py` fully green offline (`-m "not integration and not gpu"`)
- test_loss_decreases_on_overfit: final loss strictly < first loss over ≥5 steps
- test_prior_detached: cached coarse prob `requires_grad is False`
- test_val_contains_no_test_paths: zero val samples under a `test/` path
- Total runtime for this file < 90s on CPU
  </acceptance_criteria>
  <done>Teacher-forced train-step implemented and proven detached; training tests green offline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sample_weights.json → loss tensor | Untrusted-shape JSON dict crosses into tensor construction |
| filesystem path → PyramidDataset | A path argument could reference test/ (correctness-security: EVAL-01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | `build_lc_weight_tensor` | mitigate | Validate all 9 LANDCOVER_CLASSES keys + `topography_weight` present before tensor build; raise descriptive ValueError (not KeyError) on missing keys |
| T-04-02 | Tampering | `carve_train_val` / EVAL-01 | mitigate | Construct PyramidDataset from train/ roots only; rely on its lines 125-134 hard guard that raises ValueError if any indexed path contains a `test/` component; add a regression test (TestValCarve) asserting zero test/ paths in the val split |
| T-04-03 | Denial of Service (self) | teacher-forced train-step | mitigate | `.detach()` the coarse softmax before caching as child prior — prevents BPTT-through-both-passes memory blowup (RESEARCH Pitfall 2); regression test TestTeacherForcedDetach asserts requires_grad is False |
</threat_model>

<verification>
- `python -m pytest -q -m "not integration and not gpu" tests/test_seg_training.py` green
- `tests/test_seg_eval.py` / `tests/test_seg_gcs.py` collect (loud-fail guards, bodies filled by plans 03/02)
- `python -c "from seg.train_utils import weighted_joint_loss, train_step_variant_a, train_step_variant_b, make_optimizer, carve_train_val"` (with scripts/ on path) succeeds
</verification>

<success_criteria>
- train_utils.py provides loss + both teacher-forced train-steps + optimizer + val-carve, all CPU-verifiable
- Three offline test files exist; training file fully green; eval/gcs scaffolded with loud-fail guards
- EVAL-01 zero-leakage preserved (val carve never enumerates test/)
- Teacher-forced detach proven by an automated test
</success_criteria>

<output>
After completion, create `.planning/phases/04-fine-tune-and-evaluate-the-segmentation-model-then-upload-to/04-01-SUMMARY.md`
</output>
