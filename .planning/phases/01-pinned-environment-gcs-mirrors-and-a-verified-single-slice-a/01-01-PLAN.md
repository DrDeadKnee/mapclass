---
phase: 01-pinned-environment-gcs-mirrors-and-a-verified-single-slice-a
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - .python-version
  - src/mapclass/__init__.py
  - src/mapclass/config.py
  - third_party/dynamicLRP/VENDOR_SHA
  - third_party/dynamicLRP/src/lrp_engine/
  - tests/__init__.py
  - tests/conftest.py
  - pytest.ini
  - notebooks/00_vit_repro.ipynb
  - .gitignore
autonomous: false
requirements: [ENV-01]
user_setup: []

must_haves:
  truths:
    - "A fresh venv built from the pinned requirements.txt installs torch==2.7.1 and transformers==4.52.3 without resolver conflict"
    - "The vendored dynamicLRP LRPEngine imports from third_party/dynamicLRP/src on sys.path with no network access"
    - "The recorded vendor SHA in third_party/dynamicLRP/VENDOR_SHA equals 405e74243ecaa1f615f418fdc8ba24c3c5889b1e"
    - "The reference ViT.ipynb attribution flow runs end-to-end and produces a structured (non-noise) relevance heatmap on a sample image"
  artifacts:
    - path: "requirements.txt"
      provides: "dynamicLRP requirements.txt pins verbatim plus project additions"
      contains: "torch==2.7.1"
    - path: "third_party/dynamicLRP/VENDOR_SHA"
      provides: "Recorded source commit SHA of the vendored engine"
      contains: "405e74243ecaa1f615f418fdc8ba24c3c5889b1e"
    - path: "third_party/dynamicLRP/src/lrp_engine/lrp.py"
      provides: "Vendored LRPEngine implementation"
      contains: "class LRPEngine"
    - path: "src/mapclass/config.py"
      provides: "Central config: bucket, model id, GCS prefixes, device, vendored-SHA constant"
      contains: "mapclass-training-northeast1"
    - path: "notebooks/00_vit_repro.ipynb"
      provides: "De-risk spike: reproduces the reference ViT.ipynb heatmap on the pinned stack"
  key_links:
    - from: "notebooks/00_vit_repro.ipynb"
      to: "third_party/dynamicLRP/src/lrp_engine"
      via: "sys.path.append + from lrp_engine import LRPEngine"
      pattern: "lrp_engine"
    - from: "src/mapclass/config.py"
      to: "third_party/dynamicLRP/VENDOR_SHA"
      via: "VENDOR_SHA constant mirrors the file content"
      pattern: "405e74243ecaa1f615f418fdc8ba24c3c5889b1e"
---

<objective>
Stand up the reproducible pinned toolchain and prove it before any SigLIP-2 work.
Vendor keeinlev/dynamicLRP's `src/lrp_engine/` in-tree at the fixed SHA
`405e74243ecaa1f615f418fdc8ba24c3c5889b1e` (D-08), build `requirements.txt` from
dynamicLRP's `requirements.txt` verbatim plus project additions, establish the
`src/mapclass/` package skeleton with `config.py`, scaffold `pytest`, and
reproduce the reference `ViT.ipynb` attribution end-to-end on the pinned stack.

Purpose: This is the toolchain-proof gate (ROADMAP Success Criterion 1). The
SigLIP-2 swap (Plan 03) must not begin until the reference ViT attribution is
proven to run on this exact pinned environment — a failure here localizes the
problem to the stack, not the SigLIP-2 adaptation.
Output: requirements.txt, .python-version, vendored engine + VENDOR_SHA,
src/mapclass/config.py + package init, pytest scaffold, notebooks/00_vit_repro.ipynb.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-pinned-environment-gcs-mirrors-and-a-verified-single-slice-a/01-CONTEXT.md
@.planning/phases/01-pinned-environment-gcs-mirrors-and-a-verified-single-slice-a/01-RESEARCH.md
@CLAUDE.md
@README.md

<interfaces>
<!-- Verified LRPEngine API (RESEARCH.md Pattern 1, from src/lrp_engine/lrp.py master) -->
<!-- Executor uses these directly — no codebase exploration needed -->

LRPEngine.__init__(params_to_interpret=None, no_recompile=False, use_gamma=False,
  conv_gamma=100.0, mm_gamma=1.0, use_z_plus=False, use_attn_lrp=False,
  use_bilinear_mm=False, relevance_filter=1.0, starting_relevance=None,
  topk=1, dtype=torch.float32, with_grad=False)

LRPEngine.run(output_tuple_or_tensor) -> (checkpoint_vals, param_node_vals)
  # 2-tuple of lists; param_node_vals[i] positionally matches params_to_interpret[i]

@staticmethod LRPEngine.get_model_operations(model_output) -> op-set names/count/graph

Canonical ViT.ipynb usage (verbatim pattern, RESEARCH.md Pattern 1):
  img_tensor = imgs_list[0].unsqueeze(0).to(device).requires_grad_()
  output = vit_model(img_tensor)
  lrp_engine = LRPEngine(use_gamma=True, no_recompile=True)
  lrp_engine.params_to_interpret = [img_tensor]
  lrp_output = lrp_engine.run(output.logits)   # returns ([], [relevance_tensor])
  relevance_map = patch_relevance[-1].reshape(14,14)   # 224/16=14 for ViT
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin environment, vendor dynamicLRP at the fixed SHA, scaffold the package and pytest</name>
  <files>requirements.txt, .python-version, .gitignore, src/mapclass/__init__.py, src/mapclass/config.py, third_party/dynamicLRP/VENDOR_SHA, third_party/dynamicLRP/src/lrp_engine/, pytest.ini, tests/__init__.py, tests/conftest.py</files>
  <read_first>
    - .planning/phases/01-pinned-environment-gcs-mirrors-and-a-verified-single-slice-a/01-RESEARCH.md (Standard Stack — verbatim requirements.txt; Installation block; Wave 0 Gaps; D-08 vendoring)
    - .planning/phases/01-pinned-environment-gcs-mirrors-and-a-verified-single-slice-a/01-CONTEXT.md (D-08 vendor-not-submodule rationale; Claude's Discretion on module layout)
    - CLAUDE.md (Technology Stack section — pinned table, "What NOT to Use", Installation)
    - /home/drdreadknee/mapclass/.gitignore (confirm third_party/dynamicLRP/ is NOT gitignored before vendoring)
    - /home/drdreadknee/mapclass/.python-version (current value 3.10)
  </read_first>
  <action>
    Create `requirements.txt` containing dynamicLRP's requirements.txt pins VERBATIM
    (einops==0.8.1, scikit_learn==1.7.0, torch==2.7.1, tqdm==4.66.4,
    transformers==4.52.3, omegaconf==2.3.0, matplotlib==3.8.0, seaborn==0.13.2,
    timm==1.0.20) then the conservatively-pinned previously-unpinned LRP deps
    (datasets>=3,<4, captum>=0.7,<0.9) then project additions
    (torchvision==0.22.1, Pillow>=10.3,<12, google-cloud-storage>=3.0,<4,
    requests, ipykernel, jupyterlab>=4.4,<5, pytest>=8,<9). Confirm `.python-version`
    is `3.10` (leave as-is if already correct).

    Vendor the engine: clone keeinlev/dynamicLRP at the EXACT commit
    `405e74243ecaa1f615f418fdc8ba24c3c5889b1e`, copy ONLY `src/lrp_engine/` into
    `third_party/dynamicLRP/src/lrp_engine/` (preserve internal package structure
    including `promises/`, `lrp_prop_fcns.py`, `model_specific/`), and remove the
    cloned working tree. Write `third_party/dynamicLRP/VENDOR_SHA` containing the
    single line `405e74243ecaa1f615f418fdc8ba24c3c5889b1e`. Do NOT add
    third_party/dynamicLRP/ to .gitignore — it must be tracked (D-08). Do NOT use
    `pip install git+...` (no packaging metadata exists).

    Create the package skeleton: `src/mapclass/__init__.py` (empty) and
    `src/mapclass/config.py` exposing module-level constants:
    GCS_BUCKET="mapclass-training-northeast1", GCS_DATA_PREFIX="data/",
    GCS_MODELS_PREFIX="models/", MODEL_REPO_ID="google/siglip2-so400m-patch14-384",
    MODEL_GCS_DIR="models/siglip2-so400m-patch14-384", DEVICE resolved via
    `torch.cuda.is_available()`, VENDOR_SHA="405e74243ecaa1f615f418fdc8ba24c3c5889b1e",
    LOCAL_IMAGE_CACHE_DIR (a gitignored local path), MANIFEST_PATH="metadata/rumsey_manifest.json".

    Add a pytest scaffold: `pytest.ini` (testpaths=tests), `tests/__init__.py`,
    and `tests/conftest.py` that prepends `third_party/dynamicLRP/src` and `src`
    to `sys.path` so `from lrp_engine import LRPEngine` and `import mapclass`
    resolve in tests without installation. Add `.venv/` and the local image cache
    dir to `.gitignore` if not already present.
  </action>
  <verify>
    <automated>test -f requirements.txt && grep -qx 'torch==2.7.1' requirements.txt && grep -qx 'transformers==4.52.3' requirements.txt && grep -qx '405e74243ecaa1f615f418fdc8ba24c3c5889b1e' third_party/dynamicLRP/VENDOR_SHA && test -f third_party/dynamicLRP/src/lrp_engine/lrp.py && grep -q 'class LRPEngine' third_party/dynamicLRP/src/lrp_engine/lrp.py && test -f src/mapclass/config.py && grep -q 'mapclass-training-northeast1' src/mapclass/config.py && python3 -c "import configparser,sys; c=configparser.ConfigParser(); c.read('pytest.ini'); sys.exit(0)" && git check-ignore third_party/dynamicLRP/VENDOR_SHA; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - `requirements.txt` contains the exact lines `torch==2.7.1` and `transformers==4.52.3` (grep -qx succeeds)
    - `requirements.txt` contains all 9 verbatim dynamicLRP pins plus `pytest`, `torchvision==0.22.1`, `google-cloud-storage>=3.0,<4`
    - `third_party/dynamicLRP/VENDOR_SHA` is exactly the single line `405e74243ecaa1f615f418fdc8ba24c3c5889b1e`
    - `third_party/dynamicLRP/src/lrp_engine/lrp.py` exists and contains `class LRPEngine`
    - `third_party/dynamicLRP/src/lrp_engine/promises/` and `model_specific/` directories exist (full engine package vendored, not just lrp.py)
    - `git check-ignore third_party/dynamicLRP/VENDOR_SHA` exits non-zero (path is TRACKED, not gitignored)
    - `src/mapclass/config.py` defines VENDOR_SHA whose value equals the VENDOR_SHA file content
    - `.python-version` content is exactly `3.10`
    - No `git+https` or `git submodule` reference to dynamicLRP exists anywhere in the repo
  </acceptance_criteria>
  <done>requirements.txt pinned, engine vendored at the exact SHA with recorded VENDOR_SHA (tracked), src/mapclass package + config.py + pytest scaffold in place</done>
</task>

<task type="auto">
  <name>Task 2: Build the pinned venv and reproduce the reference ViT.ipynb attribution end-to-end</name>
  <files>notebooks/00_vit_repro.ipynb</files>
  <read_first>
    - requirements.txt (created in Task 1 — the exact pin set being installed)
    - third_party/dynamicLRP/src/lrp_engine/lrp.py (vendored — confirm LRPEngine.__init__ / run signatures match the <interfaces> block before wiring)
    - .planning/phases/01-pinned-environment-gcs-mirrors-and-a-verified-single-slice-a/01-RESEARCH.md (Pattern 1 — canonical ViT.ipynb usage; Open Question 1 re scalar target; Code Examples)
    - CLAUDE.md (SigLIP-2 + dynamicLRP integration sketch; "What NOT to Use")
  </read_first>
  <action>
    Create `.venv` with Python 3.10 and `pip install -r requirements.txt`. If the
    torchvision==0.22.1 ↔ torch==2.7.1 pairing or any pin fails to resolve, STOP
    and surface the exact resolver error (do NOT float pins to make it pass —
    pins are hard per D-08/CLAUDE.md). Verify `torch.__version__` starts with
    `2.7.1` and `transformers.__version__ == "4.52.3"` from the venv. Register the
    venv kernel via `ipykernel install --user --name mapclass --display-name
    "mapclass (.venv)"` (per README).

    Author `notebooks/00_vit_repro.ipynb` that reproduces the reference
    dynamicLRP `ViT.ipynb` attribution flow on the pinned stack: append
    `third_party/dynamicLRP/src` to sys.path, `from lrp_engine import LRPEngine`,
    load a timm ViT (the ViT used by the reference notebook — `timm==1.0.20` is
    pinned), run a forward on one sample image with
    `img_tensor = sample.unsqueeze(0).to(device).requires_grad_()`,
    `output = vit_model(img_tensor)`,
    `engine = LRPEngine(use_gamma=True, no_recompile=True)`,
    `engine.params_to_interpret = [img_tensor]`,
    `ckpt_vals, param_vals = engine.run(output.logits)`, reshape
    `param_vals[0]` per the verified `224/16=14` ViT grid, and `plt.imshow` the
    relevance map alpha-blended on the sample image. Print
    `LRPEngine.get_model_operations(output)` op count as a coverage probe
    artifact. The notebook MUST run top-to-bottom against the .venv kernel and
    produce a visibly structured (non-uniform) relevance heatmap.

    This is a `checkpoint:human-verify` gate (next task) — the executor runs the
    notebook to completion and captures the rendered heatmap before the human
    judges it.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import torch,transformers; assert torch.__version__.startswith('2.7.1'), torch.__version__; assert transformers.__version__=='4.52.3', transformers.__version__" && .venv/bin/python -c "import sys; sys.path.insert(0,'third_party/dynamicLRP/src'); from lrp_engine import LRPEngine; assert hasattr(LRPEngine,'run') and hasattr(LRPEngine,'get_model_operations')" && .venv/bin/jupyter nbconvert --to notebook --execute --ExecutePreprocessor.timeout=1200 --output /tmp/00_vit_repro_executed.ipynb notebooks/00_vit_repro.ipynb && test -f /tmp/00_vit_repro_executed.ipynb</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python` reports `torch.__version__` starting with `2.7.1` AND `transformers.__version__ == "4.52.3"`
    - `from lrp_engine import LRPEngine` succeeds from the vendored path with NO network access (offline import)
    - `notebooks/00_vit_repro.ipynb` executes top-to-bottom via nbconvert with zero cell exceptions (exit code 0)
    - The executed notebook contains a rendered matplotlib relevance heatmap image output (not an empty/error cell)
    - The notebook prints a `get_model_operations` op-count value > 0 as a coverage probe
    - The relevance map is reshaped to the verified ViT grid (`224/16 = 14`), proving the engine returned an input-shaped tensor in `param_vals[0]`
  </acceptance_criteria>
  <done>.venv builds from pinned reqs with no resolver conflict; 00_vit_repro.ipynb runs end-to-end and renders a structured ViT relevance heatmap on the pinned stack</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify the pinned toolchain reproduces the reference ViT heatmap</name>
  <action>Present the executed `notebooks/00_vit_repro.ipynb` and pinned environment to the user for visual judgment of the reproduced ViT relevance heatmap. This is a human-only verification gate — the executor has already built the environment and run the notebook; no further code action. Pause and wait for the resume signal.</action>
  <what-built>
    A fully pinned environment (torch==2.7.1, transformers==4.52.3, dynamicLRP
    vendored at SHA 405e74243ecaa1f615f418fdc8ba24c3c5889b1e) and an executed
    `notebooks/00_vit_repro.ipynb` reproducing the reference ViT.ipynb dynamic-LRP
    attribution heatmap on that pinned stack. This is the toolchain-proof gate
    (ROADMAP Success Criterion 1) — the SigLIP-2 work in Plan 03 must not start
    until this is confirmed.
  </what-built>
  <how-to-verify>
    1. From the repo root, start JupyterLab on the VM: `.venv/bin/jupyter lab` (or
       open the already-executed `/tmp/00_vit_repro_executed.ipynb`).
    2. Open `notebooks/00_vit_repro.ipynb`, Restart Kernel & Run All against the
       `mapclass (.venv)` kernel.
    3. Confirm every cell runs without error.
    4. Look at the final cell's heatmap overlaid on the ViT sample image.
    Expected: a structured, non-uniform relevance heatmap that visibly
    concentrates on the object in the sample image — matching the qualitative
    look of the reference dynamicLRP ViT.ipynb output (not flat, not pure noise).
  </how-to-verify>
  <resume-signal>Type "approved" if the ViT heatmap reproduced and looks structured, or describe what is wrong (flat / noise / cell errors / wrong versions)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vendored third-party code → repo execution | dynamicLRP source is executed in-process; supply-chain integrity rests on the pinned SHA |
| Public package registry (PyPI) → venv | pip installs execute setup code; pin set is the integrity control |
| Local filesystem → JupyterLab kernel | notebook runs arbitrary Python; single-user private VM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | Vendored dynamicLRP `src/lrp_engine/` | mitigate | Vendor at exact SHA 405e74243ecaa1f615f418fdc8ba24c3c5889b1e, record it in tracked `VENDOR_SHA` + `config.VENDOR_SHA`; copy only `src/lrp_engine/`, no clone/init at runtime (D-08). Engine updates are deliberate manual re-vendor |
| T-01-02 | Tampering | pip dependency resolution | mitigate | All core pins verbatim from dynamicLRP requirements.txt at the pinned SHA (authoritative source, RESEARCH Package Legitimacy Audit); `[ASSUMED]` project additions (torchvision/Pillow/google-cloud-storage/jupyterlab) gated by the build step failing loudly on resolver conflict instead of floating pins |
| T-01-03 | Elevation | Notebook arbitrary code execution | accept | Single-researcher private GCP VM, no served surface, no untrusted input in this plan (no Rumsey bytes yet). No additional control warranted |
| T-01-SC2 | Tampering | `[ASSUMED]` project-addition packages | mitigate | The pinned-build step (Task 2) fails loudly on any resolver conflict; human-verify checkpoint reviews the environment before SigLIP-2 work proceeds; no `[SLOP]`/`[SUS]` packages per RESEARCH audit |
</threat_model>

<verification>
- `requirements.txt` contains every verbatim dynamicLRP pin plus project additions; `torch==2.7.1`, `transformers==4.52.3` exact.
- `third_party/dynamicLRP/VENDOR_SHA` == `405e74243ecaa1f615f418fdc8ba24c3c5889b1e`; engine package tracked (not gitignored).
- `.venv` builds from pinned reqs with no resolver conflict; reported torch/transformers versions match pins.
- `notebooks/00_vit_repro.ipynb` runs top-to-bottom and renders a structured ViT relevance heatmap (human-verified).
</verification>

<success_criteria>
ROADMAP Success Criterion 1 met: a fresh environment from the pinned
requirements.txt with dynamicLRP vendored at the fixed SHA runs the reference
ViT attribution end-to-end and reproduces a structured heatmap — the toolchain
is proven before the SigLIP-2 swap.
</success_criteria>

<output>
Create `.planning/phases/01-pinned-environment-gcs-mirrors-and-a-verified-single-slice-a/01-01-SUMMARY.md` when done
</output>
