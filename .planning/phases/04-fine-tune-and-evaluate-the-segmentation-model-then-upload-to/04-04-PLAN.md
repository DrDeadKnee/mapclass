---
phase: 04-fine-tune-and-evaluate-the-segmentation-model-then-upload-to
plan: 04
type: execute
wave: 3
depends_on: ["04-01", "04-02", "04-03"]
files_modified:
  - scripts/finetune_seg.py
  - tests/test_seg_training.py
  - .planning/phases/04-fine-tune-and-evaluate-the-segmentation-model-then-upload-to/04-HUMAN-UAT.md
autonomous: true
requirements: [EVAL-02, EVAL-03]
must_haves:
  truths:
    - "finetune_seg.py runs one offline probe-mode pass over a tiny train/ fixture with a stub backbone (no GPU, no GCS, no network)"
    - "The training loop constructs PyramidDataset from train/ roots only and never enumerates test/"
    - "Probe mode times one epoch, prints GPU-hrs/epoch + projected full-grid cost, and exits without committing to the full grid"
    - "Checkpoints are written through seg.gcs_checkpoint and resume auto-detects the latest step"
    - "A deferred GPU-host gate artifact lists exact hand-commands for the real probe/train/eval run"
  artifacts:
    - path: "scripts/finetune_seg.py"
      provides: "train(args), probe-mode early-exit + cost projection, GCS checkpoint + auto-resume wiring, CLI"
      min_lines: 150
      exports: ["train", "main"]
    - path: ".planning/phases/04-fine-tune-and-evaluate-the-segmentation-model-then-upload-to/04-HUMAN-UAT.md"
      provides: "GPU-host deferred gate: probe/train/eval hand-commands, pretrained-flag callout, resume verification, push-before-pull discipline"
      contains: "gcloud auth application-default login"
  key_links:
    - from: "scripts/finetune_seg.py"
      to: "scripts/seg/train_utils.py"
      via: "weighted_joint_loss / train_step_* / make_optimizer / carve_train_val"
      pattern: "from seg.train_utils import"
    - from: "scripts/finetune_seg.py"
      to: "scripts/seg/gcs_checkpoint.py"
      via: "gcs_save_checkpoint / gcs_latest_checkpoint"
      pattern: "from seg.gcs_checkpoint import"
    - from: "scripts/finetune_seg.py"
      to: "scripts/seg/dataset.py"
      via: "PyramidDataset(train/ roots only) via carve_train_val"
      pattern: "carve_train_val"
---

<objective>
Wire the training loop entrypoint scripts/finetune_seg.py from the plan-01/02
building blocks: per-config training over the Phase-2 pyramid dataset with the
weighted teacher-forced step, periodic GCS checkpointing + auto-resume, a
seeded train-to-val carve, and the D-03/D-04 cost-probe mode (one epoch,
measure GPU-hrs/epoch, project the full grid, exit). Author the deferred
GPU-host gate artifact 04-HUMAN-UAT.md (analogous to 02-HUMAN-UAT.md) with
exact hand-commands for the real GPU run.

Purpose: SC#1 (end-to-end fine-tune honouring sample_weights),
D-03/D-04/D-05 (cost-probe = SigLIP Variant B), D-06 (training is GPU-host;
wiring is CPU-safe), D-07/D-09 (GCS per-config + auto-resume). The real GPU
training run + live GCS + real-test eval are the deferred GPU-host gate; the
loop wiring, probe-mode plumbing, and zero-leakage are CPU-verifiable now with
a stub backbone over the mini_pyramid fixture.
Output: scripts/finetune_seg.py + offline probe-mode test + 04-HUMAN-UAT.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-fine-tune-and-evaluate-the-segmentation-model-then-upload-to/04-CONTEXT.md
@.planning/phases/04-fine-tune-and-evaluate-the-segmentation-model-then-upload-to/04-RESEARCH.md
@.planning/phases/04-fine-tune-and-evaluate-the-segmentation-model-then-upload-to/04-PATTERNS.md
@.planning/phases/02-build-a-dataset-of-pixel-label-pairs/02-HUMAN-UAT.md

<read_first>
- 04-PATTERNS.md finetune_seg.py section (lines 25-237) — imports, device/dtype, trainable-only optimizer, DataLoader, val-carve, GCS write/resume, teacher-forced step, CLI/argparse (--probe, --pretrained/--no-pretrained, --amp)
- 04-RESEARCH.md Open Questions 1-3 (lines 818-849) — one pyramid per training step; probe = 1 full epoch over train_ds; AMP optional flag default fp32
- 04-RESEARCH.md Pitfall 1 (627-643), Pitfall 5 (690-704), Pitfall 6 (705-718) — EVAL-01 val-loop leakage; no recursive_predict in training; DINOv2/Swin pretrained flag
- 04-RESEARCH.md Assumptions A5/A6 — verify timm tags on GPU host
- scripts/finetune_paligemma.py lines 33-56, 211-330 — imports, device/dtype, optimizer, grad-clip+step, CLI scaffold (closest analog)
- scripts/seg/train_utils.py (plan 01) — weighted_joint_loss, train_step_variant_a/b, make_optimizer, carve_train_val
- scripts/seg/gcs_checkpoint.py (plan 02) — gcs_save_checkpoint, gcs_latest_checkpoint, config_prefix
- .planning/phases/02-build-a-dataset-of-pixel-label-pairs/02-HUMAN-UAT.md — deferred networked-host gate format to mirror
- tests/conftest.py 306-362 — mini_pyramid + stub_vision_config
</read_first>

<interfaces>
Consumed (plans 01/02/03 — already shipped):
  seg.train_utils: weighted_joint_loss, train_step_variant_a, train_step_variant_b, make_optimizer, carve_train_val
  seg.gcs_checkpoint: gcs_save_checkpoint(config,step,state)->path, gcs_latest_checkpoint(config)->(step,state|None), config_prefix(backbone,variant)
  seg.model: SegModelVariantA / SegModelVariantB
  evaluate_seg: NOT imported by training (EVAL-01; eval is a separate run — RESEARCH Pitfall 5)

New contracts THIS plan defines:
  scripts/finetune_seg.py:
    train(args) -> None        # per-config training; probe early-exit; GCS ckpt + resume
    main()  # argparse: --backbone {siglip,dinov2,swin} --variant {A,B} --epochs --lr
            #           --ckpt-every --probe --pretrained/--no-pretrained --amp
            #           --train-root --offline-stub (CI only)

Probe-mode contract (D-03/D-04/D-05): with --probe, run exactly 1 epoch over
train_ds, wall-time it, compute projected_full_cost = epoch_hrs * epochs * 6,
print a structured PROBE RESULT block, then sys.exit(0) WITHOUT training the
rest of the grid. The plan-05 checkpoint:decision gate reads these numbers.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement finetune_seg.py training loop + probe mode</name>
  <files>scripts/finetune_seg.py, tests/test_seg_training.py</files>
  <read_first>04-PATTERNS.md lines 25-237; 04-RESEARCH.md Open Questions 1-3 + Pitfall 1/5/6; scripts/finetune_paligemma.py 33-56/211-330; scripts/seg/train_utils.py; scripts/seg/gcs_checkpoint.py</read_first>
  <behavior>
- train(args): builds train_ds/val_ds via carve_train_val(args.train_root) (train/ only — EVAL-01); instantiates SegModelVariantA or B for the chosen backbone; make_optimizer; on launch calls gcs_latest_checkpoint(config) and resumes step/model/optimizer if a checkpoint exists; epoch loop runs one pyramid per step via train_step_variant_a/b; every args.ckpt_every steps calls gcs_save_checkpoint AFTER optimizer.step; runs a flat (NOT recursive, NOT test/) val pass per epoch as a progress signal
- probe mode (args.probe): run exactly 1 epoch over train_ds, time it with time.perf_counter, print a PROBE RESULT block (measured epoch hrs labelled CPU-wall vs GPU-hrs; projected_full = epoch_hrs * args.epochs * 6), then sys.exit(0) before any further epoch
- offline-stub mode (args.offline_stub): stub-config construction + no-pretrained so the loop runs on CPU with mini_pyramid; GCS calls go through a mockable path
- TestProbeMode::test_probe_runs_one_epoch_and_exits: train() with probe + offline_stub over a tiny train/ fixture completes one epoch, prints PROBE RESULT, raises SystemExit(0), enumerates no test/ path
  </behavior>
  <action>
Create scripts/finetune_seg.py per 04-PATTERNS.md lines 25-237. sys.path
bootstrap (scripts/ on path). Import from seg.train_utils, seg.gcs_checkpoint,
seg.model. Device/dtype per finetune_paligemma.py 211-213.

train(args): (1) train_ds, val_ds = carve_train_val(args.train_root) — train/
only, EVAL-01 (RESEARCH Pitfall 1/5: never pass test/ here, never call
recursive_predict anywhere in training/val). (2) Instantiate
SegModelVariantA/B(backbone_name=args.backbone, pretrained=args.pretrained);
the offline-stub path uses the no-pretrained stub construction the Phase-3
smoke tests use. (3) opt = make_optimizer(model, lr=args.lr). (4) cfg =
config_prefix(args.backbone, args.variant); step, state =
gcs_latest_checkpoint(cfg); if state: load model+optimizer, resume from step.
(5) Epoch loop, one pyramid per step via train_step_variant_a/b; grad-clip per
finetune_paligemma.py 279-283; every args.ckpt_every steps
gcs_save_checkpoint(cfg, step, {step, model_state_dict, optimizer_state_dict,
config: cfg}) AFTER optimizer.step (Pitfall 4 naming contract). (6) Per-epoch
val: a flat DataLoader pass over val_ds computing weighted_joint_loss only —
NO recursive_predict, NO test/.

Probe mode: if args.probe, run exactly ONE epoch, t = perf_counter() around
it, compute epoch_hrs, projected = epoch_hrs * args.epochs * 6, print a line
beginning PROBE RESULT with measured_epoch_hrs, projected_full_grid_hrs, and
configs=6, then sys.exit(0). Add --amp (optional; default fp32 first probe —
RESEARCH Open Question 3) and --offline-stub (CI only). CLI exactly per
04-PATTERNS.md lines 214-237.

Then add TestProbeMode to tests/test_seg_training.py:
test_probe_runs_one_epoch_and_exits builds a tiny train/-only pyramid dir from
the mini_pyramid fixture, calls train() with an argparse.Namespace
(probe=True, offline_stub=True, backbone="siglip", variant="B", epochs=2,
ckpt_every=1, lr=1e-4, amp=False, pretrained=False, train_root=<fixture>),
asserts SystemExit code 0, captured stdout contains PROBE RESULT, and that no
opened path contains a test/ component (assert via the carve, or a
monkeypatched open spy). Patch finetune_seg.gcs_save_checkpoint to a no-op.
Offline, CPU, stub backbone, runtime < 90s.
  </action>
  <verify>
    <automated>python -m pytest -q tests/test_seg_training.py -x -k "Probe" 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
- scripts/finetune_seg.py imports with scripts/ on path; exports train, main
- Probe mode runs exactly one epoch, prints a PROBE RESULT block, exits 0
- carve_train_val is the ONLY dataset path; no test/ enumerated; no recursive_predict in training/val (RESEARCH Pitfall 1/5)
- GCS save/resume wired through seg.gcs_checkpoint (mocked in the test)
- TestProbeMode green offline; full tests/test_seg_training.py still green
  </acceptance_criteria>
  <done>Training loop + probe mode wired and offline-proven; GCS resume + EVAL-01-safe val loop in place.</done>
</task>

<task type="auto">
  <name>Task 2: Author the deferred GPU-host gate artifact 04-HUMAN-UAT.md</name>
  <files>.planning/phases/04-fine-tune-and-evaluate-the-segmentation-model-then-upload-to/04-HUMAN-UAT.md</files>
  <read_first>.planning/phases/02-build-a-dataset-of-pixel-label-pairs/02-HUMAN-UAT.md (format to mirror); 04-RESEARCH.md Environment Availability + Runtime State Inventory + Pitfall 4/6 + Assumptions A5/A6; 04-CONTEXT.md D-06/D-07/D-09</read_first>
  <action>
Create 04-HUMAN-UAT.md mirroring 02-HUMAN-UAT.md (separate GPU box; exact
hand-commands; push-before-tell-pull; target the changed subset; DEFERRED
gate, NOT run on the planning VM). Header line: Status: DEFERRED — GPU-host
gate, not run on the planning VM. Sections:

1. Preconditions on the GPU host: pip install gcsfs google-cloud-storage
   (RESEARCH Environment Availability); gcloud auth application-default login
   (V4 IAM, no key files in git); git pull after the planning VM pushes
   (push-before-pull).
2. timm tag verification (RESEARCH A5/A6): the exact python -c commands to
   list dinov2 and swin timm models BEFORE training, confirming the tags
   Dinov2Backbone/SwinBackbone use still resolve.
3. Cost-probe command (D-03/D-04): exact python scripts/finetune_seg.py
   --backbone siglip --variant B --probe --epochs N --train-root
   data/synthetic/train --ckpt-every K; record the printed PROBE RESULT line
   (GPU-hrs/epoch + projected full-grid hrs) — this is the input to the
   plan-05 checkpoint:decision gate.
4. Pretrained-flag callout (RESEARCH Pitfall 6): DINOv2/Swin runs MUST pass
   --pretrained (default True); --no-pretrained is CI-only and yields a
   meaningless EVAL-03 baseline.
5. Full grid run (post-gate): the per-config commands for whatever breadth the
   user picks at the plan-05 gate, each writing to
   gs://mapclass-training-northeast1/models/<backbone>-<variant>/.
6. Preemption/resume verification (D-09): interrupt a run mid-training,
   relaunch the same command, confirm resume from the latest GCS step (not 0).
7. Eval run (EVAL-02/03): python scripts/evaluate_seg.py per config over
   data/synthetic/test via split.json, producing metrics/<config>-nll.json.
8. Hand-back: which artifacts to git add (metrics JSON + URI manifest +
   report — D-08; NEVER weights) and push back.

No code in this task; it is the operational runbook.
  </action>
  <verify>
    <automated>python -m pytest -q tests/test_seg_training.py -x 2>&1 | tail -2</automated>
    <manual>Confirm 04-HUMAN-UAT.md contains: DEFERRED status header, gcloud ADC auth, gcsfs install, timm-tag verification, exact probe command, PROBE RESULT capture, DINOv2/Swin --pretrained callout, resume verification, eval run, D-08 hand-back (weights never committed)</manual>
  </verify>
  <acceptance_criteria>
- 04-HUMAN-UAT.md exists with header Status: DEFERRED — GPU-host gate
- Contains all 8 sections above; mirrors the 02-HUMAN-UAT.md deferred-gate structure
- Calls out the DINOv2/Swin --pretrained requirement and the D-08 weights-never-committed rule
  </acceptance_criteria>
  <done>Deferred GPU-host runbook authored; every real-GPU/GCS/test-eval behavior has an exact hand-command.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| training loop vs EVAL-01 | Any test/ enumeration or recursive_predict call during training/val violates the locked zero-leakage invariant |
| GCS credentials on GPU host | gcloud ADC establishes IAM; no key files enter git |
| resumed checkpoint to optimizer/model | A GCS-loaded state dict is trusted into the live training process |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-10 | Tampering / Info Disclosure | training/val data path | mitigate | carve_train_val is the only dataset constructor (train/ roots only, PyramidDataset hard guard); training never imports evaluate_seg nor calls recursive_predict (RESEARCH Pitfall 1/5); regression test asserts no test/ path opened in probe mode |
| T-04-11 | Information Disclosure | GCS auth on GPU host | mitigate | gcloud auth application-default login (IAM/ADC) per 04-HUMAN-UAT.md; project-private bucket; no service-account key committed (V4 access control) |
| T-04-12 | Tampering | resumed checkpoint deserialization | accept | Only own-produced checkpoints under the project-private bucket are resumed (D-08, mitigated upstream in plan 02 T-04-06); training trusts its own GCS prefix only |
</threat_model>

<verification>
- python -m pytest -q -m "not integration and not gpu" tests/test_seg_training.py green (incl. TestProbeMode)
- scripts/finetune_seg.py imports with scripts/ on path
- 04-HUMAN-UAT.md exists with the full deferred GPU-host runbook
- Real GPU training / live GCS / real test/ eval are DEFERRED to the GPU-host gate, not run on the planning VM
</verification>

<success_criteria>
- SC#1 wiring complete: end-to-end weighted teacher-forced training over train/ only, GCS checkpoint + auto-resume, probe mode
- D-03/D-04/D-05 probe-mode cost projection implemented and offline-proven
- D-06 GPU-host gate artifact authored with exact hand-commands and the pretrained-flag callout
- EVAL-01 preserved end-to-end (no test/ in training/val)
</success_criteria>

<output>
After completion, create `.planning/phases/04-fine-tune-and-evaluate-the-segmentation-model-then-upload-to/04-04-SUMMARY.md`
</output>
