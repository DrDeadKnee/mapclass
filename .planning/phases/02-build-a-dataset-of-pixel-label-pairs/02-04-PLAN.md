---
phase: 02-build-a-dataset-of-pixel-label-pairs
plan: 04
type: execute
wave: 2
depends_on: ["02-01", "02-02"]
files_modified:
  - scripts/build_satellite_dataset.py
  - tests/test_satellite.py
autonomous: true
requirements: [PHASE-02]
nyquist_compliant: true

must_haves:
  truths:
    - "build_satellite_dataset.py writes coverage_summary.json + resolved_scenes.json to gs://.../data/satellite/"
    - "build_satellite_dataset.py build writes the dataset (pyramids) to gs://.../data/satellite/dataset/ via _GCSWriter"
    - "raw COG byte-range reads (S2/WorldCover/DEM) stay network→in-memory (never local disk)"
    - "the existing ThreadPoolExecutor build path passes the shared fs through, thread-safe"
    - "no canonical local dataset copy; defaults are gs:// URIs"
  artifacts:
    - path: "scripts/build_satellite_dataset.py"
      provides: "GCS-canonical satellite outputs (RW-04)"
      contains: "_GCSWriter"
    - path: "tests/test_satellite.py"
      provides: "GCS manifest + GCS dataset write coverage"
      contains: "def test_satellite_dataset_written_to_gcs"
  key_links:
    - from: "scripts/build_satellite_dataset.py"
      to: "scripts/gcs_io.py"
      via: "import _GCSWriter; tiling.tile(out_root=_GCSWriter(...))"
      pattern: "from gcs_io import"
    - from: "scripts/build_satellite_dataset.py"
      to: "gs://.../data/satellite/resolved_scenes.json"
      via: "fs.pipe_file of the resolved scene manifest"
      pattern: "resolved_scenes"
---

<objective>
Bring the satellite pipeline under GCS-canonical persistence (RW-04):
`build_satellite_dataset.py` writes `coverage_summary.json`,
`resolved_scenes.json`, and the built dataset to
`gs://.../data/satellite/...` via `_GCSWriter`. Raw COG byte-range reads
(Sentinel-2 / WorldCover / Copernicus DEM via GDAL VSI-CURL) stay
network→in-memory and never touch local disk (unchanged — ephemeral by
design).

Purpose: Satellite is the third of the three families that must survive
ephemeral compute. Closes the root-cause defect for the satellite family.
Output: GCS-writing build_satellite_dataset.py + offline GCS-write tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-build-a-dataset-of-pixel-label-pairs/02-CONTEXT.md
@.planning/phases/02-build-a-dataset-of-pixel-label-pairs/02-RESEARCH.md
@.planning/phases/02-build-a-dataset-of-pixel-label-pairs/02-PATTERNS.md
@.planning/phases/02-build-a-dataset-of-pixel-label-pairs/02-01-SUMMARY.md
@.planning/phases/02-build-a-dataset-of-pixel-label-pairs/02-02-SUMMARY.md

<interfaces>
<!-- Contracts from 02-01 (gcs_io) and the 02-02 tiling.py refactor. -->

From scripts/gcs_io.py: class _GCSWriter(fs, prefix); GCS_PROJECT,
BUCKET="mapclass-training-northeast1", DATA_PREFIX="mapclass-training-northeast1/data"

From scripts/tiling.py (post-02-02): tile(map_dir, out_root=_GCSWriter(...),
max_workers=32) writes pyramids to GCS; local-Path path unchanged.

build_satellite_dataset.py touch points (verified against source):
- L57-59: _DEFAULT_SUMMARY/_DEFAULT_MANIFEST/_DEFAULT_OUT = Path("data/satellite/...")
- L90: summary = coverage.build_summary(summary_path)
- L154: manifest_path.write_text(json.dumps({"scenes": resolved}, indent=2))
- L219: tiling.tile(sample_dir)
- L248-255: ThreadPoolExecutor(max_workers=workers) + as_completed (already present)
- L276-288: --summary / --manifest / --out-dir argparse defaults
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: build_satellite_dataset.py — GCS summary/manifest + GCS dataset write (RW-04)</name>
  <read_first>
    - scripts/build_satellite_dataset.py (full file — defaults L57-59, build_summary L90, manifest write L154, tiling L219, ThreadPoolExecutor L248-255, argparse L276-288)
    - scripts/gcs_io.py (_GCSWriter contract from 02-01)
    - scripts/tiling.py (post-02-02 _GCSWriter-aware tile() signature)
    - scripts/build_historical_dataset.py (sibling role-match analog — same fs-through-ThreadPoolExecutor pattern as 02-03 produces)
    - scripts/seg/gcs_checkpoint.py (fs.pipe_file write idiom lines 127-133; lazy-import lines 37-39)
    - .planning/phases/02-build-a-dataset-of-pixel-label-pairs/02-PATTERNS.md (build_satellite_dataset.py MODIFY section lines 246-287)
    - .planning/phases/02-build-a-dataset-of-pixel-label-pairs/02-RESEARCH.md (RW-04 Satellite Pipeline table lines ~473-481)
    - tests/test_satellite.py (current state — extend/create; mirror tests/test_seg_gcs.py mock pattern)
  </read_first>
  <behavior>
    - test_coverage_summary_written_to_gcs: coverage-scan writes coverage_summary.json under mapclass-training-northeast1/data/satellite/ in the mock store
    - test_resolved_scenes_written_to_gcs: search writes resolved_scenes.json to GCS, not a local path
    - test_satellite_dataset_written_to_gcs: build routes tiling.tile through _GCSWriter at .../data/satellite/dataset/<sample>/pyramids
    - test_cog_reads_stay_in_memory: no local-disk write of raw COG bytes (no .tif staged locally)
  </behavior>
  <action>
    Modify `scripts/build_satellite_dataset.py`. Apply the gcs_checkpoint.py
    lazy-import block; instantiate `fs =
    gcsfs.GCSFileSystem(project=GCS_PROJECT)` once per entrypoint; import
    `_GCSWriter` from `gcs_io`.

    Change `_DEFAULT_SUMMARY`, `_DEFAULT_MANIFEST`, `_DEFAULT_OUT` (L57-59)
    from local `Path("data/satellite/...")` to the gs:// strings
    `"gs://mapclass-training-northeast1/data/satellite/coverage_summary.json"`,
    `".../resolved_scenes.json"`, `".../dataset"`.

    Coverage summary (L90): `coverage.build_summary` currently writes
    `Path.write_text`; after it returns, read the produced JSON and
    `fs.pipe_file(f"{DATA_PREFIX}/satellite/coverage_summary.json",
    summary_bytes)` (write locally to scratch first if build_summary requires a
    Path, then pipe — do NOT modify coverage.py). Manifest (L154): replace
    `manifest_path.write_text(json.dumps({"scenes": resolved}, indent=2))` with
    `fs.pipe_file(f"{DATA_PREFIX}/satellite/resolved_scenes.json",
    json.dumps({"scenes": resolved}, indent=2).encode())`.

    Dataset write: at the L219 `tiling.tile(sample_dir)` call inside the
    per-scene worker, pass `out_root=_GCSWriter(fs,
    f"{DATA_PREFIX}/satellite/dataset/{sample_dir.name}/pyramids")`;
    `sample_dir` stays local scratch for reads. Thread the shared `fs` through
    the per-scene worker so the existing
    `ThreadPoolExecutor(max_workers=workers)` loop at L248-255 passes one
    shared thread-safe `gcsfs.GCSFileSystem` (per-scene `_GCSWriter`
    constructed inside the worker, not shared across threads).

    Raw COG byte-range reads (S2/WorldCover/DEM via GDAL VSI-CURL) remain
    unchanged — network→in-memory, never staged to local disk. Add a
    `--local-ok` flag consistent with build_dataset.py: a non-`gs://`
    --out-dir without it hard-errors (Pitfall R-1 guard).
  </action>
  <verify>
    <automated>cd /home/drdreadknee/mapclass && pytest tests/test_satellite.py -x</automated>
  </verify>
  <acceptance_criteria>
    - build_satellite_dataset.py imports _GCSWriter from gcs_io
    - _DEFAULT_SUMMARY, _DEFAULT_MANIFEST, _DEFAULT_OUT all start with "gs://"
    - resolved_scenes.json + coverage_summary.json written via fs.pipe_file under data/satellite/
    - tiling.tile call inside the per-scene worker passes out_root=_GCSWriter(...) under data/satellite/dataset
    - no local .tif staging for raw COG reads (VSI-CURL in-memory path unchanged)
    - --local-ok present; non-gs:// --out-dir hard-errors without it
    - `pytest tests/test_satellite.py::test_satellite_dataset_written_to_gcs -x` exits 0
    - `pytest tests/test_satellite.py -x` exits 0
  </acceptance_criteria>
  <done>
    Satellite coverage/manifest + dataset stream to GCS via _GCSWriter; raw COG
    reads stay in-memory; tests green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build_satellite_dataset.py → GCS bucket | ADC write of manifests + dataset pyramids |
| S2/WorldCover/DEM COG endpoints → in-memory | network input, never persisted locally |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-30 | Information disclosure | silent local-only dataset write (root-cause defect) | mitigate | gs:// defaults; non-gs:// requires --local-ok (Pitfall R-1) |
| T-02-31 | Tampering | partial pyramid upload on preemption | mitigate | pipe_file atomic per object; 02-05 verify_pull spot-checks completeness |
| T-02-32 | Spoofing | GCS auth | mitigate | ADC-only lazy-gcsfs pattern from gcs_checkpoint.py; no secrets in source |
| T-02-33 | Tampering | malformed scene manifest | accept | Scene manifest is machine-generated from STAC results (not user-authored); low-value target |
| T-02-SC | Tampering | pip installs | accept | No new packages — gcsfs/fsspec already audited [OK]; no install task |
</threat_model>

<verification>
- `pytest tests/test_satellite.py -x` green
- `grep -n "_GCSWriter" scripts/build_satellite_dataset.py` non-empty
- `grep -n "satellite/resolved_scenes.json" scripts/build_satellite_dataset.py` non-empty
</verification>

<success_criteria>
- coverage_summary.json + resolved_scenes.json written to GCS
- build streams dataset pyramids to GCS via _GCSWriter, shared fs through ThreadPoolExecutor
- raw COG reads stay network→in-memory
- gs:// defaults + --local-ok guard
</success_criteria>

<output>
Create `.planning/phases/02-build-a-dataset-of-pixel-label-pairs/02-04-SUMMARY.md` when done
</output>
