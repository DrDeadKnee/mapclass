---
phase: 02-build-a-dataset-of-pixel-label-pairs
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - scripts/gcs_io.py
  - tests/test_gcs_io.py
  - tests/test_manifest.py
  - tests/conftest.py
autonomous: true
requirements: [PHASE-02, EVAL-01]
nyquist_compliant: true

must_haves:
  truths:
    - "scripts/gcs_io.py is importable on a box without gcsfs (lazy import)"
    - "_GCSWriter writes bytes/text/PNG to any fsspec filesystem (local fs in tests)"
    - "validate_manifest hard-fails (sys.exit(1)) on unlisted, phantom, regex, and template mismatch"
    - "pull_dataset_from_gcs is idempotent across repeated calls"
    - "verify_pull raises RuntimeError on missing map dir or truncated pyramid"
    - "The full Wave 0 offline test suite is green against mock/local fs"
  artifacts:
    - path: "scripts/gcs_io.py"
      provides: "_GCSWriter shim, pull_dataset_from_gcs, verify_pull, validate_manifest, GCS_PROJECT/BUCKET/DATA_PREFIX constants"
      contains: "class _GCSWriter"
    - path: "tests/test_gcs_io.py"
      provides: "_GCSWriter + pull/verify unit coverage"
      contains: "def test_gcs_writer_writes_bytes"
    - path: "tests/test_manifest.py"
      provides: "validate_manifest hard-fail coverage"
      contains: "def test_validate_manifest_hard_fails"
    - path: "tests/conftest.py"
      provides: "shared mock fsspec / local-fs fixtures"
      contains: "fixture"
  key_links:
    - from: "scripts/gcs_io.py"
      to: "scripts/seg/gcs_checkpoint.py"
      via: "copied lazy-import + GCS_PROJECT + fs.ls/pipe_file idioms"
      pattern: "GCS_PROJECT"
    - from: "tests/test_gcs_io.py"
      to: "gcs_io.gcsfs"
      via: "mock.patch of the lazy gcsfs import"
      pattern: "mock.patch.*gcs_io.gcsfs"
---

<objective>
Build the Wave 0 foundation for the GCS-canonical Phase 2 rework: the new
`scripts/gcs_io.py` module (the single GCS I/O abstraction reused by every
downstream plan) plus the offline test scaffolds that gate every later wave.

Purpose: All five rework plans share one GCS I/O surface. Defining and
testing it first eliminates the scavenger-hunt anti-pattern — Waves 1-3
import concrete, tested contracts (`_GCSWriter`, `pull_dataset_from_gcs`,
`verify_pull`, `validate_manifest`) instead of inventing them.
Output: `scripts/gcs_io.py`, `tests/test_gcs_io.py`, `tests/test_manifest.py`,
updated `tests/conftest.py` with shared mock-fsspec fixtures.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-build-a-dataset-of-pixel-label-pairs/02-CONTEXT.md
@.planning/phases/02-build-a-dataset-of-pixel-label-pairs/02-RESEARCH.md
@.planning/phases/02-build-a-dataset-of-pixel-label-pairs/02-PATTERNS.md
@.planning/phases/02-build-a-dataset-of-pixel-label-pairs/02-VALIDATION.md

<interfaces>
<!-- Canonical GCS I/O template the executor MUST mirror. Do not invent auth. -->

From scripts/seg/gcs_checkpoint.py (the authoritative analog):
- Lazy import (lines 37-39): try: import gcsfs / except ModuleNotFoundError: gcsfs = None
- Constant (line 45): GCS_PROJECT = "narrative-campaign"
- fs instantiation (lines 131, 171): fs = gcsfs.GCSFileSystem(project=GCS_PROJECT)
- Atomic write (lines 131-133): with fs.open(path, "wb") as fh: fh.write(...)
- Bare-prefix listing (lines 174-179): files = fs.ls(bare_prefix) wrapped in
  try/except FileNotFoundError; bare prefix has NO gs:// scheme

From tests/test_seg_gcs.py (lines 52-121, the mock template):
- class _MockGCSFileSystem with _store: dict[str, bytes]
- Patched via mock.patch("seg.gcs_checkpoint.gcsfs", _MockGCSModule)

New constants to add in gcs_io.py alongside GCS_PROJECT:
- BUCKET = "mapclass-training-northeast1"
- DATA_PREFIX = "mapclass-training-northeast1/data"

Manifest schema (RESEARCH.md RW-02): version "1", entries dict keyed by
"<file>.geojson" -> {"template": "<tmpl>"}. Filename regex:
^[a-z][a-z0-9_]*_[0-9]{2}\.geojson$
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create scripts/gcs_io.py — _GCSWriter shim + validate_manifest</name>
  <read_first>
    - scripts/seg/gcs_checkpoint.py (lazy-import lines 37-39, GCS_PROJECT line 45, fs.open lines 131-133, fs.ls lines 174-179 — copy these idioms verbatim)
    - .planning/phases/02-build-a-dataset-of-pixel-label-pairs/02-RESEARCH.md (RW-01 _GCSWriter design lines ~166-208; RW-02 validate_manifest lines ~654-694)
    - .planning/phases/02-build-a-dataset-of-pixel-label-pairs/02-PATTERNS.md (scripts/gcs_io.py NEW section lines 25-83; Hard-Fail Pattern lines 413-422)
    - scripts/build_dataset.py (collision-guard sys.exit(1) idiom lines 238-248; _sanitize_stem line 80; template_key line 85)
  </read_first>
  <behavior>
    - test_gcs_writer_writes_bytes: _GCSWriter(local_fs, tmp/"a").write_bytes(b"x") then read back == b"x"
    - test_gcs_writer_write_text: write_text writes utf-8 encoded bytes
    - test_gcs_writer_truediv: (_GCSWriter(fs,"a")/"b")._prefix == "a/b"; .name == "b"
    - test_gcs_writer_pillow_compat: PIL.Image.new(...).save(_GCSWriter(local_fs,p).open("wb"), format="PNG") round-trips to a valid PNG
    - test_validate_manifest_hard_fails: SystemExit on each of regex mismatch, unlisted file, phantom manifest entry, template mismatch
    - test_validate_manifest_returns_id_to_template: clean inputs return {sanitized_id: template}
    - test_gcs_io_importable_without_gcsfs: module imports with gcs_io.gcsfs patched to None
  </behavior>
  <action>
    Create `scripts/gcs_io.py`. Copy the lazy-import block (`try: import gcsfs`
    / `except ModuleNotFoundError: gcsfs = None`) and `GCS_PROJECT =
    "narrative-campaign"` verbatim from `seg/gcs_checkpoint.py`. Add `BUCKET =
    "mapclass-training-northeast1"` and `DATA_PREFIX =
    "mapclass-training-northeast1/data"`.

    Implement `class _GCSWriter` with: `__init__(self, fs, prefix)` storing the
    passed-in fs (NEVER instantiate GCSFileSystem inside the shim — callers pass
    it, mirroring gcs_checkpoint single-instance discipline) and
    `prefix.rstrip("/")`; `__truediv__(name)` returning a new `_GCSWriter`;
    `mkdir(parents=True, exist_ok=True)` delegating to
    `self._fs.mkdirs(self._prefix, exist_ok=True)` (no-op for GCS, real for
    local fs); `name` property = last path segment; `write_bytes(data)` ->
    `self._fs.pipe_file(self._prefix, data)`; `write_text(text,
    encoding="utf-8")` -> pipe_file of encoded bytes; `open(mode="wb")` ->
    `self._fs.open(self._prefix, mode)`. Docstring must state instances are NOT
    thread-safe (one per-pyramid in the pool).

    Implement `validate_manifest(gcs_filenames, manifest) -> dict[str, str]`
    using `_FILENAME_RE = re.compile(r"^[a-z][a-z0-9_]*_[0-9]{2}\.geojson$")`.
    Accumulate errors for: (a) any filename failing `_FILENAME_RE.fullmatch`;
    (b) any filename not in `manifest["entries"]`; (c) any manifest entry not
    present in gcs_filenames (phantom); (d) any entry whose template !=
    `fn.rsplit("_",1)[0]`. On any error, print `FATAL: manifest validation
    failed — aborting before split is computed (RW-02):`, print each error,
    then `sys.exit(1)` — exactly the loudness of the build_dataset.py collision
    guard; NEVER `raise ValueError`, NEVER `print("WARNING")`. On clean input
    return `{_sanitize_stem(fn[:-8]): entries[fn]["template"] for fn in
    gcs_filenames}` (import `_sanitize_stem` from build_dataset or replicate
    its `re.sub(r"[^\w-]", "_", ...)` rule).
  </action>
  <verify>
    <automated>cd /home/drdreadknee/mapclass && python -c "import sys; sys.path.insert(0,'scripts'); import gcs_io; print(gcs_io._GCSWriter, gcs_io.BUCKET, gcs_io.DATA_PREFIX)" && pytest tests/test_gcs_io.py -k "writer or importable" -x && pytest tests/test_manifest.py::test_validate_manifest_hard_fails -x</automated>
  </verify>
  <acceptance_criteria>
    - scripts/gcs_io.py imports successfully with gcs_io.gcsfs patched to None
    - class _GCSWriter defined with __truediv__, mkdir, name, write_bytes, write_text, open
    - validate_manifest body contains sys.exit(1) and contains neither "raise ValueError" nor "WARNING"
    - GCS_PROJECT == "narrative-campaign"; BUCKET == "mapclass-training-northeast1"; DATA_PREFIX == "mapclass-training-northeast1/data"
    - `pytest tests/test_gcs_io.py -k "writer or importable" -x` exits 0
    - `pytest tests/test_manifest.py::test_validate_manifest_hard_fails -x` exits 0
  </acceptance_criteria>
  <done>
    gcs_io.py importable offline; _GCSWriter + validate_manifest implemented;
    writer/importable/hard-fail tests green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add pull_dataset_from_gcs + verify_pull, mock fixtures, Wave 0 suite</name>
  <read_first>
    - scripts/gcs_io.py (the module from Task 1 — extend, do not rewrite)
    - scripts/seg/gcs_checkpoint.py (fs.ls bare-prefix lines 174-179; fs.open lines 131-133)
    - .planning/phases/02-build-a-dataset-of-pixel-label-pairs/02-RESEARCH.md (RW-03 pull/verify design lines ~380-432)
    - .planning/phases/02-build-a-dataset-of-pixel-label-pairs/02-PATTERNS.md (Test Mock for gcsfs lines 446-463)
    - tests/test_seg_gcs.py (lines 52-121 — _MockGCSFileSystem._store pattern to mirror)
    - tests/conftest.py (current state — add fixtures, do not break existing)
  </read_first>
  <behavior>
    - test_pull_idempotent: two consecutive pull_dataset_from_gcs calls succeed, second overwrites without error
    - test_verify_raises_on_missing_map: split test list has map_id with no local dir -> RuntimeError
    - test_verify_raises_on_truncated_pyramid: spot-checked pyramid dir with < 60 PNGs -> RuntimeError
    - test_verify_passes_on_complete: full pyramid (>=60 files + pyramid.json) -> no raise
    - test_concurrent_tile_writes: 32-thread pool writing distinct _GCSWriter instances -> no key loss/corruption
  </behavior>
  <action>
    Extend `scripts/gcs_io.py` with `pull_dataset_from_gcs(subset,
    local_scratch, gcs_prefix="gs://mapclass-training-northeast1/data/
    synthetic") -> Path`: instantiate `fs =
    gcsfs.GCSFileSystem(project=GCS_PROJECT)`, build `remote =
    f"{gcs_prefix}/{subset}/"`, `local_root = local_scratch / subset`,
    `local_root.mkdir(parents=True, exist_ok=True)`, `fs.get(remote,
    str(local_root), recursive=True)` (idempotent — overwrites on re-run, safe
    after preemption), return `local_root`.

    Add `verify_pull(local_root, split_json, subset) -> None`: for each
    `map_id in split_json[subset]` assert `(local_root / map_id).is_dir()`
    else `raise RuntimeError(...)`; take a 10% sample of pyramid dirs, assert
    `pyramid.json` exists and PNG count `>= 60` else `raise RuntimeError(...)`.
    Use `raise RuntimeError` (NOT sys.exit) so callers can handle. No per-file
    checksums.

    In `tests/conftest.py` add shared fixtures: `local_fs` returning
    `fsspec.filesystem("file")`; `mock_gcs_module` providing a
    `_MockGCSFileSystem` (in-memory `_store: dict[str, bytes]`) with `open`,
    `ls`, `pipe_file`, `cat`, `exists`, `get` (materialises `_store` keys under
    a local dir), `mkdirs` — mirroring tests/test_seg_gcs.py lines 52-121. Do
    not remove/alter existing conftest fixtures.

    Write/extend `tests/test_gcs_io.py` and `tests/test_manifest.py` to cover
    the full behavior list of both tasks: writer bytes/text/truediv/pillow,
    concurrent 32-thread tile writes (assert no key loss), validate_manifest
    4 hard-fail cases + clean pass, pull idempotency, verify missing-map and
    truncated-pyramid raises. Patch via `mock.patch("gcs_io.gcsfs",
    mock_gcs_module)`.
  </action>
  <verify>
    <automated>cd /home/drdreadknee/mapclass && pytest tests/test_gcs_io.py tests/test_manifest.py -x && pytest tests/ -x --ignore=tests/integration --co -q >/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - pull_dataset_from_gcs and verify_pull defined in scripts/gcs_io.py
    - verify_pull body contains "raise RuntimeError" and does NOT contain "sys.exit"
    - tests/conftest.py exposes local_fs and mock_gcs_module fixtures; existing fixtures unchanged
    - `pytest tests/test_gcs_io.py::test_pull_idempotent -x` exits 0
    - `pytest tests/test_gcs_io.py::test_verify_raises_on_missing_map -x` exits 0
    - `pytest tests/test_gcs_io.py::test_verify_raises_on_truncated_pyramid -x` exits 0
    - `pytest tests/test_gcs_io.py::test_concurrent_tile_writes -x` exits 0
    - `pytest tests/test_gcs_io.py tests/test_manifest.py -x` exits 0
    - `pytest tests/ -x --ignore=tests/integration --co -q` collects with zero collection errors
  </acceptance_criteria>
  <done>
    pull/verify implemented; conftest mock fixtures added without regressing
    existing tests; test_gcs_io.py + test_manifest.py fully green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build/eval script → GCS bucket | ADC-authenticated I/O; no secrets in code |
| user-authored manifest.json → validate_manifest | untrusted filenames/templates cross here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Spoofing | GCS auth | mitigate | Copy ADC-only lazy-gcsfs pattern from gcs_checkpoint.py; assert no key/token literals in gcs_io.py source |
| T-02-02 | Tampering | bucket path injection via manifest filenames | mitigate | _FILENAME_RE fullmatch + 4-way cross-check in validate_manifest; sys.exit(1) before any path is constructed |
| T-02-03 | Repudiation | manifest hard-fail bypass | mitigate | validate_manifest uses sys.exit(1) only (no warn-and-continue); covered by test_validate_manifest_hard_fails |
| T-02-SC | Tampering | pip installs | accept | No new packages — gcsfs/fsspec already installed & audited [OK] (RESEARCH.md Package Legitimacy Audit); no install task here |
</threat_model>

<verification>
- `pytest tests/test_gcs_io.py tests/test_manifest.py -x` green
- `pytest tests/ -x --ignore=tests/integration --co -q` collects cleanly (no regression)
- `python -c "import gcs_io"` works with gcsfs absent (lazy import)
</verification>

<success_criteria>
- scripts/gcs_io.py exists with _GCSWriter, pull_dataset_from_gcs, verify_pull, validate_manifest, GCS_PROJECT/BUCKET/DATA_PREFIX
- Wave 0 test files green offline against mock/local fs
- conftest.py provides shared mock-fsspec fixtures without breaking existing tests
- No credentials in source; validate_manifest hard-fails with sys.exit(1)
</success_criteria>

<output>
Create `.planning/phases/02-build-a-dataset-of-pixel-label-pairs/02-01-SUMMARY.md` when done
</output>
