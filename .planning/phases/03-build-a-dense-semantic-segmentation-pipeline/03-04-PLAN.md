---
phase: 03-build-a-dense-semantic-segmentation-pipeline
plan: 04
type: execute
wave: 2
depends_on: [03-01, 03-02]
files_modified:
  - scripts/seg/decoder.py
  - scripts/seg/heads.py
autonomous: true
requirements: [PHASE-03]
nyquist_compliant: true

must_haves:
  truths:
    - "A shared UPerNet-style conv decoder consumes any backbone's declared-stride feature list (D-01)"
    - "Two thin task heads emit a dense (B,9,H,W) land-cover tensor and (B,3,H,W) topography tensor at full tile resolution (D-02, PHASE-03 SC#1)"
    - "The decoder adapts to ViT pseudo-levels AND Swin's 4 native levels without hard-coded level count (Pitfall 4)"
  artifacts:
    - path: "scripts/seg/decoder.py"
      provides: "shared PPM+FPN UPerNet-style conv decoder reading backbone.feature_strides/channels"
      contains: "class"
    - path: "scripts/seg/heads.py"
      provides: "two thin 1x1-conv task heads (9-class LC, 3-class topo)"
      contains: "class"
  key_links:
    - from: "scripts/seg/decoder.py"
      to: "scripts/seg/backbones.py"
      via: "decoder reads backbone.feature_strides / feature_channels dynamically"
      pattern: "feature_strides|feature_channels"
    - from: "scripts/seg/heads.py"
      to: "scripts/seg/decoder.py"
      via: "two 1x1-conv heads on the shared decoder trunk output"
      pattern: "Conv2d"
---

<objective>
Wave 2: implement `scripts/seg/decoder.py` (a shared UPerNet-style PPM+FPN conv
decoder, D-01) and `scripts/seg/heads.py` (two thin 1x1-conv task heads, D-02).
The decoder reads the plugged-in backbone's `feature_strides`/`feature_channels`
dynamically so the SAME code serves ViT pseudo-levels and Swin's 4 native levels
(the EVAL-03 apples-to-apples seam; Pitfall 4).

Purpose: This is the Conv-decoder + task-head tier of the Architectural
Responsibility Map. PHASE-03 SC#1 requires dense (B,9,H,W)+(B,3,H,W) outputs at
full tile resolution; D-01 mandates FPN/UPerNet (explicitly NOT linear-probe,
NOT DPT-reassemble) for terrain-boundary sharpness.
Output: `scripts/seg/decoder.py` + `scripts/seg/heads.py` satisfying
`tests/test_seg_decoder.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-build-a-dense-semantic-segmentation-pipeline/03-CONTEXT.md
@.planning/phases/03-build-a-dense-semantic-segmentation-pipeline/03-RESEARCH.md
@.planning/phases/03-build-a-dense-semantic-segmentation-pipeline/03-PATTERNS.md
@.planning/phases/03-build-a-dense-semantic-segmentation-pipeline/03-VALIDATION.md

<interfaces>
<!-- Backbone contract the decoder consumes (from 03-02 scripts/seg/backbones.py) -->
backbone.feature_strides: list[int]   # ViT [s]*k pseudo-levels ; Swin [4,8,16,32]
backbone.feature_channels: list[int]  # per-level channel dims
backbone.forward(x) -> list[Tensor]   # one (B,C,h,w) per declared stride

<!-- D-01/D-02/RESEARCH Pattern 3 -->
UPerNet recipe: PPM on deepest feature + lateral 1x1 convs + FPN top-down fuse
+ 3x3 smoothing + progressive upsample to tile HxW (NOT a single x14 bilinear).
Decoder reads feature_strides/channels dynamically; F.interpolate each level to
a common decoder resolution before fusion (Pitfall 4). Shared trunk -> 2 thin
1x1-conv heads: LC (B,9,H,W), topo (B,3,H,W). Topology/widths = Claude discretion (D-01).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Shared UPerNet-style PPM+FPN conv decoder</name>
  <files>scripts/seg/decoder.py</files>
  <read_first>
    - tests/test_seg_decoder.py (the contract — written in 03-01: decoder/head shapes from arbitrary declared-stride feature lists)
    - scripts/seg/backbones.py (the feature_strides/feature_channels/forward contract from 03-02)
    - 03-RESEARCH.md Pattern 3 (UPerNet PPM+FPN-on-ViT, progressive upsample for boundary sharpness), Pattern 5 (decoder reads strides/channels dynamically), Pitfall 4 (Swin-vs-ViT stride)
    - 03-CONTEXT.md D-01 (lightweight FPN/UPerNet conv decoder; NOT linear-probe, NOT DPT-reassemble; may internally fuse multi-scale) + Claude's Discretion (topology/widths/taps)
    - 03-PATTERNS.md section decoder.py/heads.py/model.py (spec-driven novel; scaffold docstring analog only)
  </read_first>
  <behavior>
    - Decoder constructor takes `feature_strides: list[int]` + `feature_channels: list[int]` (read off whatever backbone is plugged in) — never hard-codes level count or channel dims (Pitfall 4).
    - forward(features: list[Tensor]) applies PPM on the deepest level, lateral 1x1 convs, FPN top-down fuse, 3x3 smoothing; `F.interpolate` each level to a common decoder resolution before fusion.
    - Output is a single shared dense feature map upsampled progressively toward full tile resolution (boundary-sharp, NOT one x14 bilinear blow-up).
    - Works unchanged for a ViT k-pseudo-level list AND a Swin 4-level list.
  </behavior>
  <action>
    Create `scripts/seg/decoder.py` with the project decision-referenced module
    docstring. Implement a UPerNet-style decoder (D-01; RESEARCH Pattern 3):
    a PPM (pyramid pooling) module on the deepest feature, lateral 1x1 convs per
    level, FPN top-down fusion, 3x3 smoothing convs, then a progressive upsample
    head (e.g. staged bilinear + 3x3 conv or conv-transpose stages — boundary
    sharpness is the D-01 driver, so NOT a single x14 bilinear). The constructor
    MUST accept `feature_strides`/`feature_channels` lists and build lateral
    convs + interpolation targets dynamically from them; `F.interpolate` every
    level to a common decoder working resolution before the FPN add (Pitfall 4 —
    Swin's 4 levels at strides 4/8/16/32 and a ViT's k equal-stride pseudo-levels
    must both work with zero code change). Channel widths, number of upsample
    stages, PPM bin sizes, norm/activation = Claude's Discretion within D-01 —
    pick sane modest values and document them in the docstring. Output a single
    shared dense feature map (the D-02 shared trunk). NO linear-probe upsample,
    NO DPT-reassemble (D-01 explicit exclusions). NO training loop / optimizer /
    `.backward()` (D-06).
  </action>
  <verify>
    <automated>python -m pytest tests/test_seg_decoder.py -k "decoder or shape or swin or vit" -x -q -m "not integration"</automated>
  </verify>
  <acceptance_criteria>
    - `from seg.decoder import` the decoder class succeeds
    - Given a synthetic ViT-style feature list (k equal-stride (B,C,h,w) maps) the decoder returns a single (B,C',H,W) shared feature map at the target tile resolution
    - Given a synthetic Swin-style 4-level list (strides 4/8/16/32, differing channels) the SAME decoder runs without shape error (Pitfall 4 case green)
    - Decoder constructor reads `feature_strides`/`feature_channels`; no hard-coded level count (grep confirms params, not literals)
  </acceptance_criteria>
  <done>Shared UPerNet PPM+FPN decoder adapts dynamically to ViT and Swin feature geometry.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Two thin task heads (9-class LC + 3-class topo)</name>
  <files>scripts/seg/heads.py</files>
  <read_first>
    - tests/test_seg_decoder.py (head output-shape cases: (B,9,H,W)+(B,3,H,W) at full tile res)
    - scripts/seg/decoder.py (the shared-trunk feature-map output from Task 1)
    - 03-CONTEXT.md D-02 (shared decoder trunk + two thin task-specific output layers; NOT two independent decoders)
    - 03-RESEARCH.md Pattern 3 (two 1x1-conv heads), Architectural Responsibility Map (two task heads tier)
  </read_first>
  <behavior>
    - A LandCoverHead and TopographyHead (or one dual-head module) each a thin 1x1 conv on the shared decoder trunk output.
    - Land-cover head emits (B,9,H,W); topography head emits (B,3,H,W); both at full tile resolution matching the decoder output spatial size.
    - Heads share the single decoder trunk (D-02) — no second decoder.
  </behavior>
  <action>
    Create `scripts/seg/heads.py` with the decision-referenced docstring.
    Implement two thin task heads (D-02): each a 1x1 Conv2d mapping the shared
    decoder trunk channels to 9 (land cover) and 3 (topography) class logits
    respectively, preserving spatial resolution (final upsample to tile HxW is
    the decoder's job; the head is a thin projection — keep it thin per D-02,
    do NOT add a second decoder). Provide them as a small module (or pair of
    modules) that takes the shared trunk feature map and returns the two logit
    tensors `(B,9,H,W)` and `(B,3,H,W)`. No softmax inside the head (the
    recursive orchestrator/Phase-4 owns probability/NLL); just logits. NO
    training loop / optimizer / `.backward()` (D-06).
  </action>
  <verify>
    <automated>python -m pytest tests/test_seg_decoder.py -x -q -m "not integration"</automated>
  </verify>
  <acceptance_criteria>
    - `from seg.heads import` the head module(s) succeeds
    - Feeding the shared decoder trunk output yields exactly `(B,9,H,W)` land-cover logits and `(B,3,H,W)` topography logits at the decoder's full tile resolution
    - Heads are thin (1x1 conv projection; no second decoder) — D-02 honored
    - `pytest tests/test_seg_decoder.py -x -q -m "not integration"` fully green
  </acceptance_criteria>
  <done>Two thin heads emit dense (B,9,H,W)+(B,3,H,W) from the shared trunk (PHASE-03 SC#1 shape).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backbone feature list → decoder | In-process tensor handoff; declared strides/channels are the contract (a wrong declaration is a correctness, not security, fault) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-10 | Tampering | hard-coded decoder level count vs Swin/ViT geometry | mitigate | Decoder reads `feature_strides`/`feature_channels` dynamically + `F.interpolate` to a common resolution before fusion (Pitfall 4) so a backbone swap cannot silently misalign features |
| T-03-11 | Denial of Service | unbounded decoder params on huge feature maps | accept | Single-process research pipeline, modest UPerNet widths (Claude discretion, documented); no untrusted-network input reaches the decoder |
</threat_model>

<verification>
- `pytest tests/test_seg_decoder.py -x -q -m "not integration"` green for both ViT-style and Swin-style feature lists
- `pytest -q -m "not integration"` full offline suite green (no regression)
- Manual grep: no `optim`/`.backward(`/linear-probe-only upsample in scripts/seg/decoder.py or heads.py (D-01/D-06)
</verification>

<success_criteria>
- Shared UPerNet PPM+FPN conv decoder consumes any backbone's declared-stride feature list (D-01, Pitfall 4)
- Two thin 1x1-conv heads emit dense (B,9,H,W)+(B,3,H,W) at full tile resolution (D-02, PHASE-03 SC#1)
- No linear-probe / DPT-reassemble / second-decoder (D-01/D-02 exclusions honored)
- No training/optimizer code (D-06)
</success_criteria>

<output>
After completion, create `.planning/phases/03-build-a-dense-semantic-segmentation-pipeline/03-04-SUMMARY.md`
</output>
