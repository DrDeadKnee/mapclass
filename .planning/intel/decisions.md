# Decisions

No ADRs in the current ingest set.

The mapclass project has not yet captured technical decisions in dedicated ADR documents. Several decisions that would normally live here are currently locked inside `README.md` (classified as SPEC) — those appear in `constraints.md` and `SYNTHESIS.md` as locked spec constraints rather than ADR decisions.

If future ADRs are added (e.g. promoting individual README decisions into `docs/adr/`), they will be synthesized here.
