---
phase: quick-260519-2v4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mapclass/models.py
  - src/mapclass/mirror_model.py
  - src/mapclass/overlay.py
  - src/mapclass/attribution.py
  - notebooks/_build_02_multimodel.py
  - notebooks/02_multimodel.ipynb
  - tests/test_models_registry.py
  - tests/test_overlay_grid.py
autonomous: true
requirements: [MODEL-01, MODEL-02, ATTR-01, ATTR-02, NB-01, NB-02, NB-03]

must_haves:
  truths:
    - "All 4 models (SigLIP-2, CLIP, ViT-b-16, PaliGemma-3B) load from gs://mapclass-training-northeast1/models/ with no HF runtime call"
    - "Each model attributes against its own query-conditioned target (SigLIP-2/CLIP logits_per_image; ViT class logit; PaliGemma answer-token logit)"
    - "overlay reconstructs each model's patch grid from its own patch size (not hardcoded 27x27), CLS-token stripped where present, keeping D-09 signed/zero-centered rendering"
    - "One notebook iterates the last 50 Rumsey maps x 4 models and renders labeled overlays grouped per map"
    - "A (map, model) whose LRP fails (op-coverage gap OR OOM) renders a captioned 'no heatmap' tile and the run continues"
    - "The notebook completes headless via nbconvert at exit 0"
  artifacts:
    - path: "src/mapclass/models.py"
      provides: "Plain per-model registry: {name: load_fn, build_inputs_fn, target_fn, patch_geom}"
      contains: "MODEL_REGISTRY"
    - path: "src/mapclass/overlay.py"
      provides: "Patch-grid reconstruction parameterized by (patch_size, img_dim, has_cls)"
      contains: "def to_patch_grid"
    - path: "notebooks/02_multimodel.ipynb"
      provides: "Executed last-50 x 4-model attribution notebook, exit 0"
    - path: "notebooks/_build_02_multimodel.py"
      provides: "Generator (source of truth) for 02_multimodel.ipynb"
  key_links:
    - from: "src/mapclass/models.py build_inputs_fn"
      to: "src/mapclass/attribution.py attribute()"
      via: "the SAME requires_grad pixel tensor object flows loader->forward->params_to_interpret"
      pattern: "requires_grad_\\(\\)"
    - from: "notebooks/02_multimodel.ipynb"
      to: "src/mapclass/models.py MODEL_REGISTRY"
      via: "iterate registry x manifest[-50:]"
      pattern: "MODEL_REGISTRY"
    - from: "src/mapclass/models.py load_fn"
      to: "gs://mapclass-training-northeast1/models/"
      via: "cache-first GCS download (no HF at runtime)"
      pattern: "GCS_MODELS_PREFIX|models/"
---

<objective>
Build ONE notebook that runs dynamic-LRP attribution for SigLIP-2, CLIP,
ViT-b-16, and PaliGemma-3B against the last 50 Rumsey maps, loading models +
maps from GCS, rendering per-model overlays grouped per map, with any failing
(map, model) shown as a captioned "no heatmap" tile and the run completing
headless at exit 0.

Purpose: v1.1 milestone deliverable — a working multi-model dynamic-LRP
comparison loop a human can eyeball. Per-model op-coverage / OOM gaps are
recorded RESULTS, not bugs to engineer around.

Output: `src/mapclass/models.py` (plain per-model registry), generalized
`overlay.py` (per-model patch geometry, keeps D-09) + `attribution.py`
(generalized target), generalized `mirror_model.py` (CLIP + ViT mirror),
`notebooks/02_multimodel.ipynb` (executed, exit 0) + its generator.

KEEP IT SIMPLE: plain functions in a dict registry. NO adapter Protocol, NO
PatchGeometry dataclass, NO conformance-test framework, NO pre-flight-guard
scaffold (user explicitly rejected all of these as over-complicated; they are
Out of Scope per REQUIREMENTS.md).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
@.planning/research/STACK.md
@.planning/research/SUMMARY.md
@.planning/archive/v1.0-milestone/phases/01-pinned-environment-gcs-mirrors-and-a-verified-single-slice-a/01-03-SUMMARY.md

# v1.0 code to REUSE/generalize (read before editing)
@src/mapclass/config.py
@src/mapclass/model_loader.py
@src/mapclass/data_loader.py
@src/mapclass/attribution.py
@src/mapclass/overlay.py
@src/mapclass/mirror_model.py
@src/mapclass/manifest.py
@notebooks/_build_01_single_slice.py

<interfaces>
<!-- Key contracts the executor needs — extracted from the v1.0 codebase. Use directly; no exploration needed. -->

From src/mapclass/config.py:
  REPO_ROOT, GCS_BUCKET="mapclass-training-northeast1", GCS_DATA_PREFIX="data/",
  GCS_MODELS_PREFIX="models/", DEVICE, LOCAL_MODEL_CACHE_DIR,
  LOCAL_IMAGE_CACHE_DIR, MANIFEST_PATH, VENDORED_LRP_SRC,
  VENDOR_SHA="405e74243ecaa1f615f418fdc8ba24c3c5889b1e".
  (SigLIP-2-specific: MODEL_REPO_ID, MODEL_GCS_DIR="models/siglip2-so400m-patch14-384".)

From src/mapclass/ingest_images.py:
  def get_bucket() -> google.cloud.storage.Bucket   # ADC-authenticated
  def verify_adc() -> None                            # fail-fast on missing ADC

From src/mapclass/manifest.py:
  def load_manifest(path=config.MANIFEST_PATH) -> list[dict]   # raw JSON list, len 1544, NO sort/filter

From src/mapclass/data_loader.py:
  def load_slice(entry, query, device=None, processor=None)
      -> (img_tensor, input_ids, attention_mask, pil)
  # img_tensor == inputs["pixel_values"].requires_grad_() — SAME object must
  # flow into forward AND engine.params_to_interpret. NO clone/detach/re-.to().
  # _cached_image_path(entry_id): local-disk-cache-first; GCS key data/<id>.jpg.
  # NOTE: load_slice is SigLIP-specific (padding="max_length", max_length=64,
  # processor(text=[q], images=[pil])). Generalize the image-cache part; build
  # per-model inputs in models.py build_inputs_fn (do not break tensor identity).

From src/mapclass/attribution.py:
  @dataclass AttributionResult: relevance, target_form, peak_vram_bytes, similarity
  def attribute(model, img_tensor, input_ids, attention_mask) -> AttributionResult
  # Engine knobs: use_gamma=False, no_recompile=True, relevance_filter=0.5.
  # engine.params_to_interpret = [img_tensor]; _ckpt,param = engine.run(target);
  # relevance = param[0]. SigLIP-2 raises RuntimeError at SplitWithSizesBackward0
  # (recorded op-coverage FINDING — Fallback Ladder DECLINED, do NOT re-attempt).

From src/mapclass/overlay.py:
  PATCH_SIZE=14, IMG_DIM=384, GRID=27 (HARDCODED — must be generalized)
  def to_patch_grid(relevance) -> (grid_signed, grid_mag)   # D-09: sum over ch, NO abs, NO min-max
  def composite(ax, pil, grid_signed, grid_mag=None, *, title=None, interpolation="nearest")
  def _sym_norm(arr)   # TwoSlopeNorm(vmin=-M, vcenter=0, vmax=M), white==0, cmap="bwr"

From src/mapclass/mirror_model.py:
  def mirror_model(local_dir=None) -> dict   # SigLIP-2 specific (config.MODEL_REPO_ID/MODEL_GCS_DIR)
  def download_snapshot(local_dir=None) -> str   # snapshot_download(repo_id=config.MODEL_REPO_ID)
  # Generalize to (repo_id, gcs_dir, token=None); idempotent skip-if-same-size.

From dynamicLRP (sys.path -> config.VENDORED_LRP_SRC):
  from lrp_engine import LRPEngine
  LRPEngine(use_gamma=..., no_recompile=..., relevance_filter=...)
  engine.params_to_interpret = [tensor]; engine.run(target) -> (ckpt_vals, param_vals)
</interfaces>

<gcs_state>
Verified 2026-05-19 against the live bucket:
- `models/siglip2-so400m-patch14-384/` — MIRRORED (v1.0)
- `models/paligemma-3b-mix-224/` — ALREADY PRESENT, full code+weights
  (config.json, model-0000{1,2,3}-of-00003.safetensors, tokenizer.*,
  preprocessor_config.json). NOT gated — do NOT add any HF-token / Gemma-license
  step for PaliGemma. Use the `mix-224` variant that is there
  (PaliGemmaForConditionalGeneration + PaliGemmaProcessor; image 224, patch 14
  -> 16x16=256 image tokens).
- `models/clip-vit-large-patch14/` — ABSENT (Task 2 mirrors it; NOT gated)
- `models/vit-base-patch16-224/` — ABSENT (Task 2 mirrors it; NOT gated)
- maps: `data/<id>.jpg` — 1,544 mirrored (v1.0). Use `load_manifest()[-50:]`
  (first-of-50 id `RUMSEY~8~1~291289~90063017`, last id
  `RUMSEY~8~1~344476~90112460`).
</gcs_state>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Plain per-model registry + generalized overlay/attribution</name>
  <files>src/mapclass/models.py, src/mapclass/overlay.py, src/mapclass/attribution.py, tests/test_models_registry.py, tests/test_overlay_grid.py</files>
  <behavior>
    - test_overlay_grid: to_patch_grid(relevance, patch_size, img_dim, has_cls) for
      SigLIP-2 (14/384/no-cls -> 27x27), CLIP-L/14 (14/224/cls -> 16x16),
      ViT-b-16 (16/224/cls -> 14x14): signed grid == sum over channels with NO
      .abs() and NO min-max (D-09 invariant preserved); has_cls=True strips token 0
      before the spatial reshape; output shape == (grid, grid).
    - test_models_registry: MODEL_REGISTRY has exactly the 4 keys
      ["siglip2","clip","vit_b16","paligemma"]; each value exposes callables
      load_fn, build_inputs_fn, target_fn and a patch_geom dict with keys
      patch_size, img_dim, has_cls; build_inputs_fn returns a pixel tensor whose
      .requires_grad is True (identity-preservation invariant — assert the
      returned tensor object is the one passed to the model forward, via an
      is-check on a tiny synthetic stub, NOT a Protocol/conformance framework).
  </behavior>
  <action>
Create `src/mapclass/models.py` — a PLAIN dict registry, no Protocol/dataclass.
`MODEL_REGISTRY: dict[str, dict]` with keys `siglip2`, `clip`, `vit_b16`,
`paligemma`. Each value is a plain dict:
  - `load_fn() -> (model, processor)`: cache-first GCS download (reuse the
    `_download_model_mirror` pattern from model_loader.py — generalize it to take
    a gcs_dir + a HF class), then `<Class>.from_pretrained(local_dir,
    attn_implementation="eager").to(config.DEVICE).eval()` + matching processor.
    Classes per research/STACK.md "Per-Model Attribution Target" table:
      * siglip2  -> AutoModel + AutoProcessor, gcs_dir models/siglip2-so400m-patch14-384
      * clip     -> CLIPModel + CLIPProcessor, gcs_dir models/clip-vit-large-patch14
      * vit_b16  -> ViTForImageClassification + ViTImageProcessor, gcs_dir models/vit-base-patch16-224
      * paligemma-> PaliGemmaForConditionalGeneration + PaliGemmaProcessor,
        gcs_dir models/paligemma-3b-mix-224, loaded torch_dtype=bfloat16
        (NOT gated; weights already in GCS — no HF token, no Gemma-license step).
  - `build_inputs_fn(model, processor, pil, query, device) -> dict` returning the
    kwargs for the forward PLUS the requires_grad pixel tensor. CRITICAL
    (ATTR-01): the returned pixel tensor MUST be
    `inputs["pixel_values"].requires_grad_()` and that SAME Python object must be
    what attribute() passes to both the forward and engine.params_to_interpret —
    NO .clone()/.detach()/re-.to() after requires_grad_(). Per-model input shape:
      * siglip2/clip: processor(text=[query], images=[pil], return_tensors="pt")
        — siglip2 also needs padding="max_length", max_length=64 (reuse the
        data_loader.py SIGLIP_MAX_LENGTH handling).
      * vit_b16: processor(images=[pil], return_tensors="pt") — NO text tower.
      * paligemma: processor(text="<image> "+query, images=[pil],
        return_tensors="pt").
  - `target_fn(model, output, inputs) -> scalar tensor` per research/STACK.md:
      * siglip2/clip: output.logits_per_image[0,0] (query-conditioned similarity).
      * vit_b16: output.logits[0, class_id] — class-conditioned, NOT
        text-conditioned. DOCUMENT the query->class choice in a module docstring
        and the panel label: fix a single ImageNet class id (use the model's
        config.label2id to pick a stable map-relevant class, e.g. the id whose
        label string best matches the fixed query "a river"; if no match, fix
        class_id and document it). This asymmetry MUST be surfaced in the panel
        title (Task 3), not hidden.
      * paligemma: output.logits[0, answer_pos, answer_token_id] — DOCUMENT the
        decision: teacher-force a single-token answer; answer_pos = last prompt
        position (logits[0, -1, :]), answer_token_id = the processor-tokenized
        first token of a fixed answer word for the fixed query (e.g. "yes" for a
        "<image> {query}?" presence prompt). Record the exact answer_pos /
        answer_token_id choice in the module docstring + panel label.
  - `patch_geom`: plain dict {patch_size, img_dim, has_cls}:
      * siglip2:  {14, 384, False}   (27x27, no CLS — matches v1.0)
      * clip:     {14, 224, True}    (16x16 + CLS)
      * vit_b16:  {16, 224, True}    (14x14 + CLS)
      * paligemma:{14, 224, False}   (16x16 image tokens; if PaliGemma never
        produces relevance — expected OOM/coverage gap — geom is unused, that's fine)

Generalize `src/mapclass/overlay.py`: change `to_patch_grid` signature to
`to_patch_grid(relevance, patch_size, img_dim, has_cls=False)`. Keep the D-09
invariant EXACTLY: signed grid = channel-sum with NO `.abs()`, NO min-max;
magnitude grid = `.abs()` channel-sum. Derive `grid = img_dim // patch_size`,
`valid_dim = grid * patch_size`, discard the right/bottom edge band as today.
When `has_cls=True`, the relevance is at the PIXEL tensor (1,3,H,W) for all
image models so the existing pixel-space reshape path still applies — the CLS
strip concern only matters if a model's relevance arrives token-shaped; keep the
pixel-space path and add a guarded branch that, if `relevance` is token-shaped
((1, ntok, ...)) and `has_cls`, drops index 0 before the grid reshape. Keep
`composite`, `_sym_norm`, `draw_patch_grid` unchanged (D-09 rendering: cmap
"bwr", symmetric TwoSlopeNorm, white==0). Update the SigLIP-2 callers/tests to
pass the explicit geometry.

Generalize `src/mapclass/attribution.py`: change `attribute()` to accept a
generic `(model, forward_inputs: dict, img_tensor, target_fn)` — run the forward
with `model(**forward_inputs)` in ONE scope, build the target via
`target_fn(model, output, forward_inputs)`, keep the existing
scalar_0d/slice_2d/reshape_1d target-form fallback, keep
`engine.params_to_interpret = [img_tensor]` (the SAME object), keep peak-VRAM
capture + empty_cache. Do NOT remove the SigLIP-2 RuntimeError behavior — it is
the recorded op-coverage FINDING; the Fallback Ladder stays DECLINED (no custom
Promise / LXT / captum / pre-pool). Preserve backward compat of existing tests
or update them in the same task.

Per CLAUDE.md "What NOT to Use": do NOT use `model.get_image_features()` /
pooled embeddings as any target (query-independent — wrong). Do NOT add
`sentencepiece`, do NOT add `accelerate`, do NOT bump any pin, do NOT modify
`third_party/dynamicLRP` (VENDOR_SHA must stay
`405e74243ecaa1f615f418fdc8ba24c3c5889b1e`).
  </action>
  <verify>
    <automated>cd /home/drdreadknee/mapclass && PYTHONPATH=src .venv/bin/python -m pytest tests/test_models_registry.py tests/test_overlay_grid.py tests/test_attribution_shape.py -x -q</automated>
  </verify>
  <done>MODEL_REGISTRY has the 4 plain-dict entries with load_fn/build_inputs_fn/target_fn/patch_geom; overlay.to_patch_grid is geometry-parameterized and still D-09 signed/zero-centered (no abs, no min-max); attribute() takes a generic target_fn and preserves the requires_grad tensor identity + SigLIP-2 RuntimeError finding; all listed tests pass; VENDOR_SHA unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Mirror CLIP + ViT-b-16 weights to GCS models/</name>
  <files>src/mapclass/mirror_model.py</files>
  <action>
Generalize `src/mapclass/mirror_model.py` so `download_snapshot` and
`mirror_model` take explicit `(repo_id, gcs_dir, local_dir=None, token=None)`
instead of reading the SigLIP-2-only `config.MODEL_REPO_ID` /
`config.MODEL_GCS_DIR`. Keep the idempotent skip-if-same-byte-size upload logic
verbatim. Add a `__main__` runner (or keep the existing one, generalized) that
mirrors the two NOT-gated repos:
  * `openai/clip-vit-large-patch14`  -> `models/clip-vit-large-patch14`
  * `google/vit-base-patch16-224`    -> `models/vit-base-patch16-224`
with `token=None` (both are public/non-gated per research/STACK.md). Do NOT add
any PaliGemma mirror step — PaliGemma is ALREADY at
`models/paligemma-3b-mix-224/` and is NOT gated; do NOT add an HF-token or
Gemma-license path anywhere. Do NOT add `accelerate`/`sentencepiece`, do NOT
bump pins.

Then RUN the mirror for both repos with the pinned venv so the GCS objects
exist before the notebook (Task 3) loads them:
`cd /home/drdreadknee/mapclass && PYTHONPATH=src .venv/bin/python -m mapclass.mirror_model`
(or the generalized runner). This is a one-time HF snapshot -> GCS upload using
the VM's ADC; it may take a few minutes (CLIP-L/14 ~1.7GB, ViT-b16 ~330MB).
  </action>
  <verify>
    <automated>cd /home/drdreadknee/mapclass && PYTHONPATH=src .venv/bin/python -c "
from mapclass.ingest_images import get_bucket
b=get_bucket()
clip=[x.name for x in b.list_blobs(prefix='models/clip-vit-large-patch14/')]
vit=[x.name for x in b.list_blobs(prefix='models/vit-base-patch16-224/')]
assert any('config.json' in n for n in clip), 'CLIP config.json missing in GCS'
assert any(n.endswith('.safetensors') or n.endswith('.bin') for n in clip), 'CLIP weights missing'
assert any('config.json' in n for n in vit), 'ViT config.json missing in GCS'
assert any(n.endswith('.safetensors') or n.endswith('.bin') for n in vit), 'ViT weights missing'
print('OK clip', len(clip), 'vit', len(vit))
"</automated>
  </verify>
  <done>mirror_model.py is generalized to (repo_id, gcs_dir, token=None); gs://mapclass-training-northeast1/models/clip-vit-large-patch14/ and .../vit-base-patch16-224/ both contain config.json + weights; re-running the mirror uploads nothing (idempotent); no PaliGemma/HF-token/Gemma step added; no pin or vendor changes.</done>
</task>

<task type="auto">
  <name>Task 3: Last-50 x 4-model notebook + headless execute (exit 0)</name>
  <files>notebooks/_build_02_multimodel.py, notebooks/02_multimodel.ipynb</files>
  <action>
Create `notebooks/_build_02_multimodel.py` (generator is source of truth;
the .ipynb is the committed executed artifact — same pattern as
`_build_01_single_slice.py`). The notebook:
  1. Prepends `config.VENDORED_LRP_SRC` to sys.path; asserts VENDOR_SHA ==
     `405e74243ecaa1f615f418fdc8ba24c3c5889b1e` (read-only sanity print, NOT a
     guard framework — one assert line).
  2. `from mapclass.manifest import load_manifest`; `maps = load_manifest()[-50:]`
     (the last 50 entries; NO sort/filter — manifest.py forbids it).
  3. `from mapclass.models import MODEL_REGISTRY`; fixed query string
     `QUERY = "a river"` (single fixed query — model and map are the only axes
     per PROJECT.md "Explore axes").
  4. Outer loop over the 4 models, inner loop over the 50 maps. SEQUENTIAL model
     handling for VRAM: load model -> for each map { load_slice image (reuse
     data_loader._cached_image_path / PIL open), build per-model inputs via
     registry build_inputs_fn, call the generalized attribute(); on success
     to_patch_grid(relevance, **patch_geom) + render the D-09 composite into a
     per-(map) row; on ANY Exception render a captioned "no heatmap —
     <model>: <short reason>" tile and CONTINUE } -> del model;
     torch.cuda.empty_cache(). Wrap the per-(map,model) body in
     `try/except Exception as e` (generalize the v1.0 caught-RuntimeError
     pattern from _build_01_single_slice.py) so SigLIP-2's known
     split_with_sizes RuntimeError AND a PaliGemma OOM both become captioned
     tiles, not crashes.
  5. Render grouped PER MAP: one figure (or one row) per map with 4 columns
     (siglip2 | clip | vit_b16 | paligemma), each panel titled with the model
     name + the attribution-target semantics (e.g. "ViT: class-conditioned (not
     text) — class <id>:<label>"; "PaliGemma: answer-token logit"; "SigLIP-2:
     logits_per_image (op-coverage gap expected)"). Failures show the captioned
     "no heatmap" tile in their column. 50 maps -> 50 rows; keep figures small
     (dpi modest) so the executed ipynb is not enormous — it is acceptable to
     emit per-map figures sequentially.
  6. Final summary cell: a small text table of (model -> #heatmaps / #no-heatmap)
     so the run outcome is legible. This is NOT the deferred "structured
     op-coverage report table" — just a tally so the human can eyeball coverage.

Then regenerate + execute headless with the registered kernel:
`cd /home/drdreadknee/mapclass && PYTHONPATH=src .venv/bin/python notebooks/_build_02_multimodel.py`
then
`cd /home/drdreadknee/mapclass && .venv/bin/jupyter nbconvert --to notebook --execute --inplace --ExecutePreprocessor.timeout=3600 --ExecutePreprocessor.kernel_name=mapclass notebooks/02_multimodel.ipynb`
The notebook MUST complete at exit 0 even though SigLIP-2 (known
split_with_sizes gap) and likely PaliGemma-3B (OOM on the L4 — expected,
recordable per STATE.md blocker) produce only "no heatmap" tiles. Coverage gaps
/ OOM are RECORDED RESULTS — do NOT add custom Promises / LXT / captum /
pre-pool / accelerate to "fix" them, do NOT bump pins, do NOT touch
third_party/dynamicLRP.
  </action>
  <verify>
    <automated>cd /home/drdreadknee/mapclass && .venv/bin/jupyter nbconvert --to notebook --execute --inplace --ExecutePreprocessor.timeout=3600 --ExecutePreprocessor.kernel_name=mapclass notebooks/02_multimodel.ipynb && PYTHONPATH=src .venv/bin/python -c "
import json
nb=json.load(open('notebooks/02_multimodel.ipynb'))
errs=[o for c in nb['cells'] if c['cell_type']=='code' for o in c.get('outputs',[]) if o.get('output_type')=='error']
assert not errs, f'{len(errs)} uncaught cell errors (notebook did not exit 0)'
print('notebook executed clean, 0 uncaught errors')
"</automated>
  </verify>
  <done>notebooks/_build_02_multimodel.py generates 02_multimodel.ipynb; the executed notebook iterates manifest[-50:] x the 4 models, renders per-model overlays grouped per map with target-semantics panel labels, shows captioned "no heatmap" tiles for failing (map,model) incl. SigLIP-2 and (expected) PaliGemma, and completes headless via nbconvert with 0 uncaught cell errors (exit 0); a final tally cell summarizes heatmap vs no-heatmap per model; VENDOR_SHA and pins unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| huggingface.co -> GCS models/ | One-time CLIP/ViT snapshot download then upload to the private bucket (Task 2). Pinned repo ids, trusted publisher (`openai`, `google`). |
| GCS -> VM (runtime) | Model weights + map JPEGs read from the private bucket via ADC; no HF call at runtime. |
| vendored dynamicLRP | Engine traverses the live torch autograd graph; integrity = VENDOR_SHA intact. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2v4-01 | Tampering | vendored `third_party/dynamicLRP` (autograd engine) | mitigate | Notebook asserts VENDOR_SHA == 405e74243ecaa1f615f418fdc8ba24c3c5889b1e; no task modifies third_party/; Fallback Ladder (custom Promise / LXT / captum) explicitly DECLINED and not implemented. |
| T-2v4-02 | Tampering | HF snapshot download for CLIP/ViT (Task 2) | accept | Pinned repo ids from trusted publishers (`openai/clip-vit-large-patch14`, `google/vit-base-patch16-224`), non-gated, one-time mirror into a private single-researcher bucket; standard `snapshot_download`. No new package. Low-value private target. |
| T-2v4-03 | Information disclosure | PaliGemma weights in private GCS | accept | Already present in the private bucket (not introduced by this plan); single-researcher bucket; no redistribution. No HF token handled (PaliGemma is not gated here and is not re-mirrored). |
| T-2v4-SC | Tampering | npm/pip/cargo installs | mitigate | NO package installs in this plan — frozen env reused as-is (zero requirements.txt delta per research/STACK.md "Net dependency delta: ZERO"). Any pip-install attempt is out of scope and forbidden; no [ASSUMED]/[SUS] packages introduced. |
</threat_model>

<verification>
- `tests/test_models_registry.py`, `tests/test_overlay_grid.py`,
  `tests/test_attribution_shape.py` pass with the pinned venv (Task 1).
- `gs://mapclass-training-northeast1/models/clip-vit-large-patch14/` and
  `.../vit-base-patch16-224/` contain config.json + weights (Task 2).
- `notebooks/02_multimodel.ipynb` executes headless via nbconvert with 0
  uncaught cell errors (exit 0), iterating last-50 maps x 4 models with
  captioned "no heatmap" tiles for failures (Task 3).
- `third_party/dynamicLRP` byte-unmodified; `VENDOR_SHA ==
  405e74243ecaa1f615f418fdc8ba24c3c5889b1e`; `requirements.txt` unchanged; no
  `accelerate`/`sentencepiece` added; no pin bumped.
</verification>

<success_criteria>
1. SigLIP-2, CLIP, ViT-b-16, PaliGemma-3B all load from
   `gs://mapclass-training-northeast1/models/` (CLIP+ViT mirrored in Task 2;
   PaliGemma read from existing `models/paligemma-3b-mix-224/`) via a plain dict
   registry — no Protocol/dataclass/conformance/guard framework. [MODEL-01]
2. Each model's query-conditioned target is wired
   (SigLIP-2/CLIP `logits_per_image[0,0]`; ViT class logit; PaliGemma
   answer-token logit) with the requires_grad pixel-tensor identity preserved
   through forward + dynamic LRP. [MODEL-02, ATTR-01]
3. `overlay` reconstructs each model's patch grid from its own patch size
   (not hardcoded 27x27), CLS-stripped where present, keeping D-09
   signed/zero-centered rendering. [ATTR-02]
4. One notebook iterates the last 50 manifest maps x the 4 models, rendering
   labeled overlays grouped per map; a failing (map, model) shows a captioned
   "no heatmap" tile and the run continues. [NB-01, NB-02]
5. The notebook completes headless via nbconvert at exit 0. [NB-03] —
   **milestone done**
</success_criteria>

<output>
Create `.planning/quick/260519-2v4-build-one-notebook-that-runs-dynamic-lrp/260519-2v4-SUMMARY.md` when done
</output>
