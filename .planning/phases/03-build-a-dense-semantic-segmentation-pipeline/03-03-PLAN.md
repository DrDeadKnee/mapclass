---
phase: 03-build-a-dense-semantic-segmentation-pipeline
plan: 03
type: execute
wave: 1
depends_on: [03-01]
files_modified:
  - scripts/seg/dataset.py
autonomous: true
requirements: [PHASE-03]
nyquist_compliant: true

must_haves:
  truths:
    - "The dataloader reads Phase-2 nested-pyramid tiles (image/land_cover/topography) into tensors (PHASE-03/D-06)"
    - "sample_weights.json is surfaced through the dataset byte-unchanged"
    - "The dataset never enumerates a path containing a test/ component (EVAL-01 zero-leakage preserved end-to-end)"
  artifacts:
    - path: "scripts/seg/dataset.py"
      provides: "PyramidDataset over the Phase-2 tree, train/-only"
      contains: "class PyramidDataset"
  key_links:
    - from: "scripts/seg/dataset.py"
      to: "scripts/tiling.py"
      via: "reads pyramid.json + per-tile image/land_cover/topography/sample_weights filenames"
      pattern: "pyramid\\.json"
    - from: "scripts/seg/dataset.py"
      to: "data split.json"
      via: "explicit train/ root or split.json filter; never glob across synthetic root"
      pattern: "split\\.json|train"
---

<objective>
Wave 1: implement `scripts/seg/dataset.py` — `PyramidDataset`, a
`torch.utils.data.Dataset` over the Phase-2 nested-pyramid tree (D-06). It reads
`pyramid.json` + per-tile image/land_cover/topography PNGs, surfaces
`sample_weights.json` unchanged, and is structurally incapable of touching the
frozen `test/` split (EVAL-01 zero-leakage).

Purpose: The dataloader is the Dataset/DataLoader tier in the Architectural
Responsibility Map; the Phase-3 smoke test and Phase-4 training both consume it.
This plan is independent of the backbone work (different file) and runs parallel
to 03-02.
Output: `scripts/seg/dataset.py` satisfying `tests/test_seg_dataset.py`
(split-safety, weight-surfacing, batch shapes).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-build-a-dense-semantic-segmentation-pipeline/03-CONTEXT.md
@.planning/phases/03-build-a-dense-semantic-segmentation-pipeline/03-RESEARCH.md
@.planning/phases/03-build-a-dense-semantic-segmentation-pipeline/03-PATTERNS.md
@.planning/phases/03-build-a-dense-semantic-segmentation-pipeline/03-VALIDATION.md

<interfaces>
<!-- ToonDataset class shape (finetune_paligemma.py:67-156) -->
torch.utils.data.Dataset: __init__ builds index list; __len__ -> len(samples);
__getitem__ -> dict[str, Tensor]; raise ValueError on empty index (lines 93-94 idiom)

<!-- Phase-2 contract (scripts/tiling.py) -->
_REQUIRED_FILES = ("image.png","land_cover.png","topography.png","sample_weights.json")
per-tile manifest keys: image="{id}_image.png", land_cover, topography
sample_weights.json propagated byte-identical per pyramid (tiling.py:187-196)

<!-- split.json schema (scripts/build_dataset.py:74,132-161) -->
_SPLIT_FILENAME = "split.json" ; {"test":[ids], "train":[ids]}
Phase-2 D-17: train/ and test/ are sibling dirs; glob("**/pyramid.json") finds BOTH (Pitfall 6)

<!-- leakage assertion to satisfy (test_tiling.py:213-215) -->
assert "test" not in out.parts ; dataset must never enumerate a test/ path
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PyramidDataset (Phase-2 tree reader, train/-only, weight surfacing)</name>
  <files>scripts/seg/dataset.py</files>
  <read_first>
    - tests/test_seg_dataset.py (the contract — written in 03-01: split-safety, weight surfacing, batch shapes)
    - scripts/finetune_paligemma.py lines 67-156 (ToonDataset skeleton: __init__/__len__/__getitem__ dict[str,Tensor], lines 93-94 empty-index ValueError idiom)
    - scripts/tiling.py lines 42-44 (_REQUIRED_FILES), 84-114 (per-tile manifest keys), 187-196 (sample_weights byte-identical propagation)
    - scripts/build_dataset.py lines 74, 132-161 (split.json schema, _SPLIT_FILENAME)
    - 03-PATTERNS.md section scripts/seg/dataset.py (analog assignments + split-safety contract) and section Test files (test_tiling.py:213-215 leakage assertion)
    - 03-RESEARCH.md Pitfall 6 (frozen-split leakage), section Architectural Responsibility Map (Dataset tier)
  </read_first>
  <behavior>
    - PyramidDataset(root) takes an explicit `train/` root (or filters by split.json) — constructor raises ValueError if the resolved index is empty (mirror ToonDataset lines 93-94).
    - __getitem__ returns a dict of tensors per tile: image (3,H,W float), land_cover label, topography label, and the sample_weights surfaced unchanged.
    - sample_weights.json is read per-pyramid and surfaced byte/structure-unchanged (no re-normalisation; Phase-4 consumes it).
    - The dataset never builds an index entry whose path contains a `test/` component (mirror test_tiling.py:213-215); a `glob("**/pyramid.json")` across the synthetic root is forbidden (Pitfall 6).
    - Reads pyramid.json via `json.loads((dir/"pyramid.json").read_text())`; indexes tiles by id; uses manifest image/land_cover/topography filename keys (never re-derive).
  </behavior>
  <action>
    Create `scripts/seg/dataset.py` with the project decision-referenced module
    docstring (mirror tiling.py lines 1-28 / finetune_paligemma.py 1-31). Define
    `class PyramidDataset(torch.utils.data.Dataset)` copying the ToonDataset
    skeleton shape (`__init__` builds a sample index list, `__len__` returns
    `len(self.samples)`, `__getitem__` returns `dict[str, Tensor]`; raise
    `ValueError` if the index is empty — finetune_paligemma.py lines 93-94
    idiom). `__init__` MUST take an explicit `train/` root or a `split.json`
    path and filter to train ids only; assert/guard that no indexed path
    contains a `test/` component (Pitfall 6 / test_tiling.py:213-215) — do NOT
    `glob("**/pyramid.json")` across the synthetic root. Read each pyramid's
    `pyramid.json` with `json.loads((pdir/"pyramid.json").read_text())`, index
    tiles by `id`, and load per-tile `image`/`land_cover`/`topography` PNGs by
    the manifest filename keys (never re-derive names). `__getitem__` returns
    image as a (3,H,W) float tensor, land_cover + topography as label tensors,
    and the per-pyramid `sample_weights.json` surfaced UNCHANGED (read with the
    same byte-fidelity Phase-2 guarantees; no re-normalisation — Phase-4 loss
    consumes it; CONTEXT §code_context Established Patterns). Dataloader
    internals (batching strategy, pyramid-aware sampling, prior-channel
    plumbing) are Claude's discretion under D-06 — keep them simple and
    documented. NO training loop / optimizer / `.backward()` (D-06).
  </action>
  <verify>
    <automated>python -m pytest tests/test_seg_dataset.py -x -q -m "not integration"</automated>
  </verify>
  <acceptance_criteria>
    - `from seg.dataset import PyramidDataset` succeeds
    - On the `mini_pyramid` fixture: `len(ds) > 0`, `__getitem__` returns a dict with image/land_cover/topography tensors of correct rank and the sample_weights surfaced unchanged
    - Constructing on an empty/`test/`-only tree raises `ValueError`
    - Leakage assertion green: no indexed sample path contains a `test/` component (test_seg_dataset.py split-safety case)
    - No optimizer/`.backward()`/training loop present (D-06)
  </acceptance_criteria>
  <done>PyramidDataset reads Phase-2 tiles, surfaces weights unchanged, train/-only with provable no-leakage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local Phase-2 PNG/JSON → dataset process | Untrusted local image/manifest parsing (Pillow PNG decode, json.loads of pyramid.json/sample_weights/split.json) |
| frozen split.json → train/test partition | Integrity boundary: a leaked test/ path breaks EVAL-01 end-to-end |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Tampering | frozen-split leakage through dataloader | mitigate | Constructor takes explicit `train/` root or split.json filter; runtime guard rejecting any indexed path containing a `test/` component (mirror test_tiling.py:213-215); `glob("**/pyramid.json")` forbidden |
| T-03-08 | Denial of Service | malformed Phase-2 PNG/JSON | accept | Inputs are locally-produced by the trusted Phase-2 pipeline; `json.loads` / Pillow raise on corruption (fail-fast is acceptable for a research dataloader; no remote input) |
| T-03-09 | Tampering | sample_weights re-normalisation | mitigate | Surface `sample_weights.json` byte/structure-unchanged; no mutation in the dataset (Phase-4 owns weighting) |
</threat_model>

<verification>
- `pytest tests/test_seg_dataset.py -x -q -m "not integration"` green
- `pytest -q -m "not integration"` full offline suite green (no regression)
- Manual grep: no `glob("**/pyramid.json")` and no `optim`/`.backward(` in scripts/seg/dataset.py
</verification>

<success_criteria>
- `PyramidDataset` reads the Phase-2 nested-pyramid tree into tensors
- `sample_weights.json` surfaced unchanged through the dataset
- Structurally train/-only: no enumerated path contains a `test/` component (EVAL-01 preserved)
- No training/optimizer code (D-06 construction-only)
</success_criteria>

<output>
After completion, create `.planning/phases/03-build-a-dense-semantic-segmentation-pipeline/03-03-SUMMARY.md`
</output>
