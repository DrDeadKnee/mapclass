---
phase: 04-fine-tune-and-evaluate-the-segmentation-model-then-upload-to
plan: 05
type: execute
wave: 4
depends_on: ["04-02", "04-03", "04-04"]
files_modified:
  - scripts/report_comparison.py
  - tests/test_seg_report.py
  - metrics/checkpoint_manifest.json
  - docs/04-comparison-report.md
autonomous: false
requirements: [EVAL-02, EVAL-03]
must_haves:
  truths:
    - "The user is presented measured GPU-hrs/epoch + projected full-grid cost and chooses grid breadth at a blocking gate"
    - "report_comparison.py reads per-config metrics JSON + the URI manifest and renders an A/B x backbone comparison report"
    - "Only lightweight artifacts (metrics JSON, URI manifest, report) are committed; no model weights"
  artifacts:
    - path: "scripts/report_comparison.py"
      provides: "render_report(args), main (CLI) — JSON metrics + manifest -> docs/04-comparison-report.md"
      min_lines: 70
      exports: ["render_report", "main"]
    - path: "tests/test_seg_report.py"
      provides: "TestReportRender — offline render from synthetic metrics JSON fixtures"
      contains: "render_report"
    - path: "metrics/checkpoint_manifest.json"
      provides: "Schema/seed for the gs:// best-checkpoint-per-config URI manifest (D-08)"
      contains: "mapclass-training-northeast1"
    - path: "docs/04-comparison-report.md"
      provides: "The written A/B x backbone comparison report (rendered; final numbers filled at the GPU-host gate)"
      contains: "joint"
  key_links:
    - from: "scripts/report_comparison.py"
      to: "metrics/"
      via: "reads <config>-nll.json + checkpoint_manifest.json"
      pattern: "nll.json|checkpoint_manifest"
    - from: "scripts/report_comparison.py"
      to: "docs/04-comparison-report.md"
      via: "markdown render output"
      pattern: "04-comparison-report"
---

<objective>
Deliver the two human-facing artifacts of Phase 4: (1) the blocking
cost-probe checkpoint:decision gate where the user picks grid breadth from the
measured probe numbers (D-05), and (2) the comparison-report renderer +
metrics/manifest scaffolding that turns per-config joint-NLL JSON into the
written A/B x backbone report (D-01/D-08, EVAL-02/EVAL-03).

Purpose: D-05 is a real blocking gate (mirrors the Phase-2 02-03/02-04
autonomous:false loss-weight gates) — the user answers with measured GPU cost
in hand, NOT a Claude auto-pick. D-08: only metrics JSON + gs:// URI manifest +
the report return to git; weights NEVER. The renderer is fully CPU-testable
from synthetic metrics fixtures; the FINAL numbers are filled when the GPU-host
gate (04-HUMAN-UAT.md) produces real metrics.
Output: scripts/report_comparison.py + offline test + seeded
metrics/checkpoint_manifest.json + rendered docs/04-comparison-report.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-fine-tune-and-evaluate-the-segmentation-model-then-upload-to/04-CONTEXT.md
@.planning/phases/04-fine-tune-and-evaluate-the-segmentation-model-then-upload-to/04-RESEARCH.md
@.planning/phases/04-fine-tune-and-evaluate-the-segmentation-model-then-upload-to/04-PATTERNS.md
@.planning/phases/04-fine-tune-and-evaluate-the-segmentation-model-then-upload-to/04-HUMAN-UAT.md

<read_first>
- 04-PATTERNS.md write_nll_metrics shape (lines 314-328) — {config, joint_nll, variant, backbone, eval_date}
- 04-RESEARCH.md §Architecture Patterns "Recommended Project Structure" (lines 200-225) — metrics/ + docs/04-comparison-report.md layout
- 04-PATTERNS.md report_comparison.py section (lines 332-351) — CLI/argparse scaffold (the only analog; rendering logic is novel — see "No Analog Found" lines 685-694)
- 04-CONTEXT.md D-05 (candidate breadths a/b/c) and D-08 (lightweight artifacts only)
- .planning/phases/02-build-a-dataset-of-pixel-label-pairs/02-03-PLAN.md lines 194-219 — the autonomous:false checkpoint:decision gate idiom to mirror (includes action/verify/done on the gate task)
- scripts/evaluate_seg.py (plan 03) — write_nll_metrics output shape this renderer consumes
</read_first>

<interfaces>
Consumed:
  metrics/<config>-nll.json   (written by evaluate_seg.write_nll_metrics, plan 03):
    {"config": "<backbone>-<variant>", "joint_nll": float, "variant": "A|B",
     "backbone": "siglip|dinov2|swin", "eval_date": "YYYY-MM-DD"}
  metrics/checkpoint_manifest.json (this plan seeds the schema):
    {"<config>": "gs://mapclass-training-northeast1/models/<config>/step_NNNNNNN.pt", ...}

New contracts THIS plan defines:
  scripts/report_comparison.py:
    render_report(args) -> Path   # reads metrics_dir/*.json + manifest -> writes output md
    main()  # argparse: --metrics-dir metrics/ --manifest metrics/checkpoint_manifest.json
            #           --output docs/04-comparison-report.md

D-05 gate candidate breadths (present verbatim):
  (a) full 6-config grid (siglip/dinov2/swin x A/B)
  (b) SigLIP A+B full + DINOv2/Swin single-variant
  (c) SigLIP A+B full + DINOv2/Swin short benchmark runs
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Cost-probe grid-breadth decision gate (D-05)</name>
  <decision>
After the SigLIP Variant B cost probe has been run on the GPU host (per
04-HUMAN-UAT.md section 3), how broad should the training grid be?
  </decision>
  <context>
D-05 (locked): the grid breadth is NOT committed upfront. The probe (SigLIP
Variant B, the SigLIP-path cost upper bound — D-04) produces a PROBE RESULT
line: measured GPU-hrs/epoch + projected full-grid hrs. This gate mirrors the
Phase-2 02-03/02-04 blocking loss-weight gates: the user decides with measured
numbers in hand, not a Claude auto-pick. Paste the probe PROBE RESULT output
before deciding. The chosen breadth determines which per-config runs section 5
of 04-HUMAN-UAT.md executes and which <config>-nll.json files Task 2 renders.
  </context>
  <options>
    <option id="option-a">
      <name>Full 6-config grid</name>
      <pros>Complete A/B x SigLIP/DINOv2/Swin comparison; strongest EVAL-03 evidence</pros>
      <cons>Highest GPU cost (projected_full_grid_hrs from the probe)</cons>
    </option>
    <option id="option-b">
      <name>SigLIP A+B full + DINOv2/Swin single-variant</name>
      <pros>Full primary-backbone A/B; benchmarks present at reduced cost</pros>
      <cons>No A/B contrast for the benchmark backbones</cons>
    </option>
    <option id="option-c">
      <name>SigLIP A+B full + DINOv2/Swin short benchmark runs</name>
      <pros>Lowest cost; still yields side-by-side EVAL-03 numbers</pros>
      <cons>Benchmark numbers are short-run (under-trained); flag as indicative in the report</cons>
    </option>
  </options>
  <action>Checkpoint only — present the three options above to the user, require them to paste the GPU-host probe PROBE RESULT line (measured GPU-hrs/epoch + projected full-grid hrs) first, then collect the breadth decision. No code is written in this task; the approved breadth feeds 04-HUMAN-UAT.md section 5 (which per-config runs to execute) and Task 2 (which <config>-nll.json files to render).</action>
  <verify>User has pasted the probe PROBE RESULT numbers and selected exactly one of option-a / option-b / option-c; both the numbers and the choice are recorded in the plan-05 SUMMARY.</verify>
  <done>A single grid-breadth option is approved with the probe numbers recorded; downstream GPU-host runs and the report are scoped to that breadth.</done>
  <resume-signal>Paste the probe PROBE RESULT line, then select: option-a, option-b, or option-c</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: report_comparison.py + offline render test + metrics scaffold</name>
  <files>scripts/report_comparison.py, tests/test_seg_report.py, metrics/checkpoint_manifest.json, docs/04-comparison-report.md</files>
  <read_first>04-PATTERNS.md lines 332-351 (CLI scaffold) + 685-694 (no analog — rendering is novel); 04-RESEARCH.md lines 200-225 (project structure); scripts/evaluate_seg.py write_nll_metrics shape; 04-CONTEXT.md D-08</read_first>
  <behavior>
- render_report: reads every metrics/*-nll.json in metrics_dir, joins with checkpoint_manifest.json, emits a markdown table (rows = config, columns = backbone | variant | joint_nll | checkpoint URI) sorted by joint_nll ascending; a header section names SigLIP as the locked primary and DINOv2/Swin as benchmarks; writes docs/04-comparison-report.md; returns the output Path
- render_report tolerates a partial grid (only the configs the user chose at the gate present) and notes any missing config explicitly rather than failing
- TestReportRender::test_renders_from_fixtures: given 3 synthetic <config>-nll.json + a manifest in tmp_path, render_report produces a markdown file containing each config, each joint_nll, and each gs:// URI, sorted ascending by NLL
- TestReportRender::test_partial_grid_noted: with only siglip-a/siglip-b present, the report renders and explicitly notes dinov2/swin absent
  </behavior>
  <action>
Create scripts/report_comparison.py with the CLI scaffold from 04-PATTERNS.md
lines 332-351 (sys.path bootstrap, argparse --metrics-dir/--manifest/--output).
Implement render_report: glob metrics_dir for *-nll.json, parse each
(config/backbone/variant/joint_nll/eval_date), load the manifest dict, build a
markdown report: a title, a one-line metric definition (joint per-pixel NLL =
-(log p_lc + log p_topo), lower is better), a primary/benchmark note (SigLIP
locked primary per PROJECT.md; DINOv2/Swin non-locked benchmarks — EVAL-03), a
results table sorted by joint_nll ascending with the gs:// checkpoint URI per
row, and a "configs not run" line listing any of the 6 canonical configs absent
from metrics_dir (so a partial grid from the D-05 gate is self-documenting).
Write to args.output. D-08: this script reads/writes only lightweight JSON +
markdown — never touches weights.

Seed metrics/checkpoint_manifest.json with the schema and the canonical config
keys mapped to placeholder gs:// URIs under
gs://mapclass-training-northeast1/models/<config>/ (the GPU-host gate replaces
placeholders with real step URIs). Render docs/04-comparison-report.md once
from whatever metrics JSON exist (likely empty on the planning VM) so the
artifact + structure are committed; the GPU-host gate re-runs it with real
numbers. Add a top-of-report note: numbers are final once the GPU-host gate
(04-HUMAN-UAT.md) produces real metrics; the planning-VM render is structural.

tests/test_seg_report.py: import-guard idiom; TestReportRender with the two
tests in <behavior>, building synthetic metrics JSON + manifest under tmp_path.
Fully offline, no torch needed, runtime < 15s.
  </action>
  <verify>
    <automated>python -m pytest -q tests/test_seg_report.py -x 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
- scripts/report_comparison.py imports with scripts/ on path; exports render_report, main
- render_report sorts by joint_nll ascending and includes the gs:// URI per row
- Partial grid (subset chosen at the D-05 gate) renders and notes missing configs
- metrics/checkpoint_manifest.json + docs/04-comparison-report.md committed; NO weights anywhere (D-08)
- tests/test_seg_report.py green offline
  </acceptance_criteria>
  <done>Comparison report renderer + metrics/manifest scaffold shipped and offline-proven; D-08 lightweight-only honoured.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| metrics JSON / manifest -> committed report | Lightweight artifacts cross into git; weights must never |
| user decision -> grid breadth | A blocking human gate sets downstream GPU cost |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-13 | Information Disclosure | git commit scope (D-08) | mitigate | report_comparison.py reads/writes only *-nll.json + checkpoint_manifest.json + the markdown report; no .pt path is ever read or written by this script; the commit step lists only metrics/ JSON + report + manifest |
| T-04-14 | Repudiation | D-05 decision provenance | mitigate | The checkpoint:decision gate requires the user to paste the probe PROBE RESULT line before selecting; the chosen breadth + numbers are recorded in the plan-05 SUMMARY for auditability |
</threat_model>

<verification>
- python -m pytest -q -m "not integration and not gpu" tests/test_seg_report.py green
- scripts/report_comparison.py imports; render_report produces docs/04-comparison-report.md
- metrics/checkpoint_manifest.json present with the canonical config schema; no *.pt referenced for commit
- The D-05 gate is autonomous:false and blocks until the user selects a breadth with probe numbers in hand
- Git commit (closeout) includes ONLY: metrics/*.json, metrics/checkpoint_manifest.json, docs/04-comparison-report.md, the new scripts/tests (D-08; weights never committed)
</verification>

<success_criteria>
- D-05 blocking cost-probe gate present, autonomous:false, mirrors the Phase-2 gate idiom
- D-01/EVAL-02/EVAL-03 comparison report renderer implemented and offline-proven
- D-08 honoured: only metrics JSON + URI manifest + report return to git
- Real final numbers deferred to the GPU-host gate (04-HUMAN-UAT.md); structure committed now
</success_criteria>

<output>
After completion, create `.planning/phases/04-fine-tune-and-evaluate-the-segmentation-model-then-upload-to/04-05-SUMMARY.md`
</output>
