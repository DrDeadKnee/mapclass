# MapClass pinned environment.
#
# Section 1: dynamicLRP requirements.txt pins, VERBATIM, from
#   github.com/keeinlev/dynamicLRP @ 405e74243ecaa1f615f418fdc8ba24c3c5889b1e
# (engine runs on the torch autograd graph; these pins are HARD — D-08 / CLAUDE.md
# "What NOT to Use". Do NOT float torch/transformers.)
einops==0.8.1
scikit_learn==1.7.0
torch==2.7.1
tqdm==4.66.4
transformers==4.52.3
omegaconf==2.3.0
matplotlib==3.8.0
seaborn==0.13.2
timm==1.0.20

# Section 2: dynamicLRP requirements.txt entries that upstream leaves UNPINNED
# (datasets, captum). Pinned conservatively here to protect the torch 2.7.1
# resolve (RESEARCH.md Standard Stack; CLAUDE.md "What NOT to Use").
datasets>=3,<4
captum>=0.7,<0.9

# Section 3: project additions (NOT in dynamicLRP requirements.txt).
# torchvision MUST match the torch 2.7.x minor line exactly (0.22.x <-> 2.7.x)
# or a silent segfault results (CLAUDE.md Version Compatibility).
torchvision==0.22.1
Pillow>=10.3,<12
google-cloud-storage>=3.0,<4
requests
ipykernel
jupyterlab>=4.4,<5
pytest>=8,<9
